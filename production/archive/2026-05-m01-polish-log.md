# M01 工作历史归档 (2026-05-04 ~ 2026-05-26)

> 从 production/active.md 归档。这些是 M01 美术/物理/手电/拼片的逐次调试记录,均已完成收口(M01 在 2026-05-26 收口)。
> active.md 只保留当前活跃线;历史细节查这里或 git log。

2026-05-26 M01 收口 / 合并准备：用户确认当前 preview 效果满意，希望最终用简单代码结构保留现有效果并自行判断合并方式。处理：不改变玩法表现，删除 `M01GreyboxBootstrap` 中上一版残留的 Kinematic 速度追鼠标路径（`game.deltaTime` / `M01_FRAGMENT_DRAG_MAX_VELOCITY` / `updateActiveFragmentDragVelocity()`），保留最终有效模型：拿起拼片时临时关闭 body/collider 并直接跟随指针，放下后再切回 Dynamic 或弱磁吸 Kinematic；`M01PhysicsBoundary` 从旧 `FLOOR_BOUNDS` 对象收成单一 `PHYSICS_GROUND_Y`，修正地面 PNG / 物理地面注释；`M01PhysicsPile` 注释改为 active nodes + sky-pile release。同步权威 `docs/design/game-design-spec.md` §5.2 到当前最终行为：开局 9 片从顶部小堆自由落体、落堆稳定前锁输入、玻璃透色保持目标 palette 与灰白边线；将两个 2026-05-21 physics plan 标为历史设计稿并修正其中最容易误导的 sequential drop / 速度拖拽 / 地面边界片段。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01PhysicsCollider.test.ts tests/cocos/M01PhysicsRandom.test.ts tests/cocos/M01PhysicsRotation.test.ts` 成功（86 tests）；`npm run typecheck` 成功；`git diff --check` 成功；刷新 `db://assets/scripts` 成功；全量 `npm test` 成功（24 files / 242 tests）；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`ok=true`、`usedFallback=false`、显色仍使用 `M01ArtSprite_light_mask_*`、graphics underlay alpha `0`、completion 成功、console/page errors 为空。合并建议：不要把当前 45 个试错提交直接 merge 到 `main`；保留此分支作为验证分支，最终用 PR squash merge 或从最终 diff 生成 clean integration branch。

2026-05-26 M01 review 修复：自审当前推送分支后发现两个需先处理的问题：1) `light-edge` overlay 初始化时异步加载贴图，通用 loader 成功后无条件 `active=true`，可能在未显色或刚取消显色时重新叠上边缘，造成边线加深；2) `package.json` 的项目版本误改为 `3.8.8`，但 `package-lock.json` 仍为 `0.1.0`，容易混淆 Cocos Creator 版本与 npm package 版本。修复：`syncArtSpriteFrameToResource()` 增加 `activateOnLoad` 选项，普通 art sprite 默认加载成功后激活，`light-edge` 初始化与同步都传 `{ activateOnLoad: false }`，edge 可见性只由 `syncArtEdgeSpriteState()` 的 reveal state 控制；`package.json` 项目 version 改回 `0.1.0`，保留 `creator.version = 3.8.8`。验证：先新增 `keeps light-edge overlay visibility controlled by reveal state, not async loading` 回归并确认旧代码红灯；修复后定向测试成功，M01 相关 8 文件测试成功（154 tests），`npm run typecheck` 成功，`npm ci --dry-run` 成功，`git diff --check` 成功；刷新 `db://assets/scripts` 成功；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`usedFallback=false`、显色仍使用 `M01ArtSprite_light_mask_circle`、graphics underlay alpha `0`、completion 成功、console/page errors 为空。

2026-05-26 M01 手电显色边缘不变黑修复：用户反馈显色后拼片边缘比灰白状态更黑，期望边缘颜色仍和灰白拼片一致。复查确认 root cause 是上一轮把边缘墨线像素也放进 `light-mask` 显色贴图，并将这些边线重绘为较深的固定墨色；运行时再叠加 Sprite tint 后，边缘就不可能等同灰白原图。修复：把显色拆成两层，`light-mask_*` 只保留半透明白色玻璃填充，边线像素 alpha 置 0；新增 `light-edge_*` overlay，从灰白 hidden 拼片逐像素抠出原边缘，运行时只在显色时启用，并保持 `Color(255,255,255,255)` 不参与染色。验证：新增回归确认 `light-edge` 可见像素与对应 hidden 灰白拼片 RGBA 逐像素一致，`light-mask` 填充仍保持半透明；刷新 `db://assets/resources/art/stage1-m01/runtime-sprites/hidden-fragments` 与 `db://assets/scripts` 成功；M01 相关 8 文件测试成功（153 tests），`npm run typecheck` 成功，`node --check scripts/m01-preview-smoke.mjs && node --check scripts/generate-m01-reference-texture-pieces.mjs` 成功；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`usedFallback=false`、运行时显色仍使用 `M01ArtSprite_light_mask_*`、graphics underlay alpha `0`、completion 成功、console/page errors 为空。

2026-05-25 M01 手电显色玻璃透色修复：用户看到上一轮精确 RGB 后反馈“饱和度太高，不像玻璃透色”，并指出显色后拼片的边也被弄没了，进一步确认“显色之后还要保有一定透明度，和之前灰白色拼片一样”。复查 light-mask 生成结果确认问题来自两点：1) 为防背景光污染把白色填充 alpha 固定到 `255`，导致显色变成实心彩块；2) 墨线判断阈值过低（只保留极深像素），原灰白拼片里大量灰褐边线被当成白底染掉。修复：将目标/显色共享 palette 调为低饱和玻璃色 `purple [167,140,166] / green [136,166,138] / orange [206,154,114]`；`renderLightMaskPiece()` 生成 light-mask 时保留原拼片 alpha，不再把白色填充改成不透明，同时把墨线阈值提高到 luminance `<180`，让外轮廓和内部手绘边重新保留。验证：重新生成后 light-mask 白色填充 alpha 中位数为 `140`，与灰白拼片水彩透明度一致；circle/triangle/hexagon 的保留边线像素约 `1276/1111/1141`。刷新 `db://assets/resources/art/stage1-m01/runtime-sprites/hidden-fragments` 与 `db://assets/scripts` 成功；`npm run smoke:m01-preview -- --enable-art-preview` 成功，运行时显色为紫 `167/140/166`、橘 `206/154/114`、绿 `136/166/138`，`M01ArtSprite_light_mask_*`、graphics underlay alpha `0`、`usedFallback=false`、completion 成功、console/page errors 为空；M01 相关 8 文件测试成功（152 tests），`npm run typecheck` 成功，`git diff --check` 成功。

2026-05-25 M01 手电显色最终屏幕色修复：用户继续指出 preview 里拼片显色和目标图颜色仍不一致，并追问真正原因。复查 runtime 后确认上一轮已把 `Sprite.color` 设为目标 palette，且显色时已切到 `M01ArtSprite_light_mask_*`，灰盒 underlay alpha 为 `0`；但屏幕像素探针显示红灯下紫色拼片最终像素约为 `188/161/185`，而 sprite tint 是 `139/105/156`。Root cause 是新 light-mask 继承了隐藏灰白水彩图的半透明填充 alpha（白色填充 alpha 中位数约 `140/255`），虽然灰底不再参与乘色，但后方手电光束 / 背景仍会透过 mask 把颜色洗亮。修复：`renderLightMaskPiece()` 生成 light-mask 时将非墨线白色填充 alpha 固定为 `255`，保留透明外部和深色墨线；显色拼片继续使用目标 palette，移动拼片后恢复 hidden sprite。另同步 smoke 收尾，避免 `browser.close()` 在本机 Chrome 上输出成功 JSON 后挂住。验证：生成后三张 light-mask 白色填充 alpha P10/P50/P90 均为 `255`；像素探针确认红灯下紫色拼片屏幕值为 `139/105/156`、橘色为 `199/126/75`，与目标原始 RGB 距离为 0；目标证据图自身因半透明叠在纸面上仍会有约 14-19 的背景混合误差。`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01PhysicsCollider.test.ts tests/cocos/M01PhysicsRandom.test.ts tests/cocos/M01PhysicsRotation.test.ts` 成功（152 tests），`node --check scripts/m01-preview-smoke.mjs && node --check scripts/generate-m01-reference-texture-pieces.mjs` 成功，`npm run typecheck` 成功，刷新 `db://assets/resources/art/stage1-m01/runtime-sprites/hidden-fragments` 与 `db://assets/scripts` 成功，`npm run smoke:m01-preview -- --enable-art-preview` 成功且 `usedFallback=false`、completion `completed=true`、console/page errors 为空。

2026-05-25 M01 手电显色 palette 对齐目标图：用户指出目标图形上的紫 / 橘 / 绿三种颜色应与拼片被灯光照亮后的显色一致，并确认可以通过调整灯光色值实现。复查后确认 runtime 当前不是换彩色 PNG，而是在灰白隐藏拼片 `M01ArtSprite_hidden_*` 上做 `sprite.color` tint；目标证据图使用旧 palette `purple [139,105,156] / green [92,145,112] / orange [199,126,75]`，但手电显色用 `M01_BASE_RGB × M01_BEAM_RGB` 临时乘法得出 `purple [108,75,104] / green [113,138,122] / orange [230,111,56]`，所以玩家看到的拼片显色与目标证据色偏离。修复：新增共享 `M01_TARGET_BLEND_RGB`，把 observed fragment blend tint 的 orange / green / purple 直接绑定到目标证据 palette；`colorForToken()`、`colorForManualTargetBlendOverlay()`、`colorForTargetOverlapEvidence()` 同步走 `colorForTargetBlendRgb()`，避免后续目标色和显色再次分叉；smoke 脚本也同步使用同一 target blend RGB。验证：先新增 observed tint / fixed floodlight 回归并确认旧代码红灯；修复后定向测试成功，M01 相关 5 文件测试成功（139 tests），`npm run typecheck` 成功，`node --check scripts/m01-preview-smoke.mjs` 成功，刷新 `db://assets/scripts` 成功；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`realInput.usedFallback=false`、completion `completed=true`、console/page errors 为空，runtime debug 确认红灯下 purple 为 `139/105/156`、orange 为 `199/126/75`，黄 / 蓝灯路径的 green 为 `92/145/112`。

2026-05-25 M01 拼片地面视觉贴合修复：用户反馈 preview 中部分拼片落在地面后看上去还和地平面有距离，个别又能贴到地面。复查确认不是重力问题，而是三层视觉 / 物理边界不一致叠加：`M01PhysicsBoundary` 的物理地面 top 为 `y=-270`，但地面 PNG 深色手绘地平线在显示后约为 `y=-274`；上一轮为减少穿透给所有 fragment 统一加了 `12px` collider padding，导致圆 / 六边形的不可见碰撞体先碰到地面；三角形 collider 以旧等边三角重心构造，垂直 bounds 是 `[-9,18]`，和 56px 可见 sprite box 不居中，所以不同形状贴地观感不一致。修复：把地面 PNG 的源图 ink row `6px` 对齐到物理 floor surface，`GROUND_SPRITE_CENTER_Y` 由 source height / display scale 推导；fragment collider padding 改为按形状配置，circle `+4` 匹配 60px 圆形显示，triangle / hexagon 为 `0`；triangle collider 改为在可见 56px 高度内居中，bounds 为 `[-18,18]`。验证：新增 triangle collider 与 visible ground ink 回归并先确认旧代码红灯；修复后定向测试成功，M01 相关 6 文件测试成功（118 tests），`npm run typecheck` 成功，刷新 `db://assets/scripts` 成功；Playwright runtime 探针确认 `physicsGroundTop=-270`、`visibleGroundInkApprox=-270`、落地 circle gap 约 `0.26px`、hexagon 约 `-0.13..-2.97px`（接触 / 微重叠），console/page errors 为空；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`realInput.usedFallback=false`、completion `completed=true`。

2026-05-25 M01 开局小堆式下落：用户反馈当前从天而降虽然不再逐片生成，但 9 个拼片横向平铺下落，不像“一小堆掉下来”。复查确认上一轮 `releaseAllPiecesFromSky()` 用 `spreadStep` 把 9 片均匀摊在 `dropOriginX ± jitterX`，且 `jitterX: 170`、纵向只 `6px` 漂移，因此第一眼像一排片子。修复：保留同时 active + 顶部自由落体，但把初始释放改成 `M01_PHYSICS_SKY_PILE_OFFSETS` 4-3-2 小堆模板；`jitterX` 从 `170` 收紧到 `82`，随机漂移降到 `5/4px`，释放坐标改为 `dropOriginX + pileOffset.x * jitterX` 与 `dropOriginY + pileOffset.y`，不再使用 `spreadStep` 横铺。验证：先更新“starts M01 physics fragments from the top edge as one natural free-fall release”回归并确认旧代码红灯（仍为 `jitterX: 170` / 无 pile offsets）；修复后定向测试成功，M01 相关 6 文件测试成功（116 tests），`npm run typecheck` 成功，刷新 `db://assets/scripts` 成功；Playwright runtime clean 探针确认 `layout_ready` 时 9 片全部 active，初始横向跨度约 `174px`、纵向跨度约 `124px`，速度 `-0.67 -> -4.64 -> -8.9` 连续加速，console/page errors 为空；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`realInput.usedFallback=false`、completion `completed=true`、console/page errors 为空。

2026-05-25 M01 开局九片顶部自由落体：用户反馈开局不应该“从空中现生成九个拼片”，而应该一开局九片已经从天而降；同时指出下降节奏忽快忽慢，不像自由落体。复查确认 root cause 有两层：`M01GreyboxBootstrap` 先把 fragment `active=false`，再由 `M01PhysicsPile` 用 `interPieceDelayMs` / `setTimeout` 逐片释放，所以视觉上像半空逐个生成；上一条为减少穿透把 `fixedTimeStep` 调到 `1/240`，在 Cocos preview 中造成物理时间推进偏慢、节奏不自然。修复：移除开局隐藏 fragment 与逐片 `dropOnePiece` 调度，`startDrop()` 改为 `releaseAllPiecesFromSky()`，9 个拼片加载后立即 active，并从顶部 `dropOriginY: 350`、横向 `jitterX: 170` 一次性释放；物理 cadence 改回实时 `M01_PHYSICS_FIXED_TIME_STEP = 1 / 60`，线性阻尼降到 `0.05`，同时保留 `12px` collider padding、bullet、真实 settle 检测、地面摩擦/反弹与左右墙。验证：新增“starts M01 physics fragments from the top edge as one natural free-fall release”回归并先确认旧代码红灯；修复后定向 scaffold 7 项成功，M01 相关 6 文件测试成功（116 tests），`npm run typecheck` 成功，刷新 `db://assets/scripts` 成功；Playwright runtime 探针确认 `layout_ready` 时 9 个 fragment 已全部 active 且位于顶部 `y≈329..359`，速度随时间从 `-0.33` 到 `-4.31`、`-8.9`、`-22.34` 连续增大，console/page errors 为空；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`realInput.usedFallback=false`、completion `completed=true`、console/page errors 为空。

2026-05-25 M01 拼片穿透二次修复 / 地面摩擦：用户反馈仍能看到图形穿透，并补充地面也应该有摩擦力。复查 runtime 后确认上轮 1/120 步进 + 4px collider padding 已降低但未消除小幅可见重叠，且开场 `physicsSettled` 仍按固定超时触发，可能在拼片还在微动或 solver 尚未完全分离时解锁输入。修复：`M01PhysicsPile` 将物理步进提高到 `1/240`，fragment collider 视觉 padding 提到 `12px`（56px token -> 68px collider，circle radius / polygon max point 为 34），为 fragment 开启 `bullet`、落堆期间关闭 `allowSleep`，并增加线性 / 角阻尼 `0.4 / 0.55`；settle 不再直接按 timeout 调 `onSettled`，而是等所有 fragment 线速度 `<=4`、角速度 `<=6` 连续 18 帧，最大窗口到期后才收尾。`M01GreyboxBootstrap` 将 settle 最大窗口更新为 `3600ms`；`M01PhysicsBoundary` 将地面 friction 独立设为 `0.82`、左右墙 friction 为 `0.25`，保留 ground restitution `0.12` / wall restitution `0.05`；`scripts/m01-preview-smoke.mjs` 改为等待真实 `physicsSettled`，不再固定等 3 秒。验证：新增/更新 scaffold 回归并确认旧代码红灯；修复后 `npm test -- tests/cocosProjectScaffold.test.ts -t "round M01 fragments|screen edges|subtle ground bounce|physics ground|settle before unlocking|visible pass-through"` 成功；M01 相关 5 文件测试成功（108 tests）；`npm run typecheck` 成功；`node --check scripts/m01-preview-smoke.mjs` 与 `npm test -- tests/m01PreviewSmokeScript.test.ts` 成功；刷新 `db://assets/scripts` 成功；Playwright runtime 探针确认 ground friction `0.82`、wall friction `0.25`、9 个 fragment `bullet=true`、`allowSleep=true`（settle 后恢复）、collider radius/maxAbs `34`、可见图形 overlap count 为 0、console/page errors 为空；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`realInput.usedFallback=false`、completion `completed=true`、console/page errors 为空。

2026-05-25 M01 圆形拼片堆叠真实性 / 屏幕边缘墙：用户反馈第一次从天而降堆叠时有些形态不符合物理规律，例如圆和圆靠在一起不应该能稳定靠住；随后补充要避免拼片滚出屏幕，左右需要有墙面在屏幕边缘。复查后确认初始投放仍是 `dropOriginX=320` 且 `jitterX=12`，几乎从同一竖线落下；所有形状 collider 也统一 `friction=0.6`，导致圆片和多边形一样容易靠摩擦“挂住”。修复：`M01GreyboxBootstrap` 将初始投放横向散布扩大到 `jitterX: 48`；`M01PhysicsPile` 区分圆片和多边形摩擦，圆形 `M01_PHYSICS_CIRCLE_FRICTION = 0.18`，三角/六边形保持 `0.6`；`M01PhysicsBoundary` 将左右墙从旧碎片池边界移到 960px 画布外侧，墙体中心 `x=-500/+500`、内边界正好 `x=-480/+480`，高度覆盖 `y=-270..360`。验证：先新增 scaffold 回归“screen edge walls”和“round fragments roll out of unstable vertical stacks”并确认旧代码红灯；修复后 `npm test -- tests/cocosProjectScaffold.test.ts -t "round M01 fragments|screen edges|subtle ground bounce|physics ground"` 成功；M01 相关 5 文件测试成功（107 tests）；`npm run typecheck` 成功；`git diff --check` 成功；刷新 `db://assets/scripts` 成功；Playwright runtime 探针确认左/右墙中心 `-500/+500`、圆形 friction `0.18`、多边形 friction `0.6`、所有拼片 x 均在 `[-480, 480]` 内；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`realInput.usedFallback=false`、completion `completed=true`、console/page errors 为空。

2026-05-25 M01 拼片落地轻微反弹：用户反馈自由落体拼片碰到地面完全不反弹，手感不够真实。复查当前 2D 物理参数后确认 fragment collider restitution 只有 `0.02`，地板/墙共用 `0.05`，视觉上接近“粘地”。修复：`M01PhysicsPile` 新增 `M01_PHYSICS_FRAGMENT_RESTITUTION = 0.08` 并统一用于 circle / polygon fragment collider；`M01PhysicsBoundary` 将 ground restitution 单独提高到 `0.12`，左右墙保持 `0.05`，避免侧墙反弹过强。验证：先新增 scaffold 回归“freely falling M01 fragments a subtle ground bounce”并确认旧代码红灯；修复后 `npm test -- tests/cocosProjectScaffold.test.ts -t "subtle ground bounce|physics ground|pass-through"` 成功；M01 相关 5 文件测试成功（105 tests）；`npm run typecheck` 成功；刷新 `db://assets/scripts` 成功；Playwright runtime 探针确认 ground restitution `0.12`、wall restitution `0.05`、9 个 fragment restitution `0.08`；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`realInput.usedFallback=false`、completion `completed=true`、console/page errors 为空。

2026-05-25 M01 拼片堆叠穿透感修复：用户反馈堆叠时会出现图形穿过，问是否违背物理定律。复查后判断不是“没有物理”，而是两层实现误差叠加：1) 手绘水彩 sprite 的可见外轮廓比 56px 标准 token 更松，原 collider 严格贴 56px 几何，边缘会视觉重叠；2) 2D 物理是离散步进，高速下落 / 互相挤压时可能产生 tunneling 或 solver 视觉穿透。修复：`M01PhysicsPile` 将物理步进从 `1/60` 提高到 `1/120`，并给 fragment collider 增加 `4px` 视觉 padding（56px token -> 60px collider，circle radius 30，triangle/hexagon 最大点位 30），同时保持 debug overlay 关闭。验证：新增 scaffold 回归“finer physics step and padded fragment colliders”；`npm test -- tests/cocosProjectScaffold.test.ts -t "pass-through|physics step|physics debug|physics ground"` 成功；M01 相关 5 文件测试成功（104 tests）；`npm run typecheck` 成功；`git diff --check` 成功；刷新 `db://assets/scripts` 成功；Playwright runtime 探针确认 preview 中 56px fragments 已使用 60px collider；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`realInput.usedFallback=false`、completion `completed=true`、console/page errors 为空。后续如果仍看到极端穿透，下一步应临时打开 collider overlay 对比可见水彩边界与真实碰撞体，而不是继续盲调重量或重力。

2026-05-25 M01 拼片拖拽物理手感修复：用户在 preview 里反馈“想捡起拼片，拼片乱飞”。复现后确认第一层 root cause 是 `M01GreyboxBootstrap.moveTokenDrag()` 把 `(target - node.position) / game.deltaTime` 直接写进 Kinematic `RigidBody2D.linearVelocity`，一次约 57px 的拖动会产生约 `3800 px/s` 的速度；鼠标停住后也没有逐帧收敛，拼片会继续冲出目标点。修复：新增 `M01_FRAGMENT_DRAG_MAX_VELOCITY = 480`、逐帧 `updateActiveFragmentDragVelocity()` 追踪当前拖拽目标、近目标 2px 内清零速度，且拖拽结束 / 自由放置时清掉物理速度并把自由放置拼片 park 成 Kinematic。用户随后指出“抓起某个拼片时还是会把其他拼片撞飞”，并猜测可能是重量 / 重力；复查发现 collider 已设置 `density = 1`、`friction = 0.6`、`restitution = 0.02`，真正的第二层 root cause 是 `beginTokenDrag()` / click-pick 路径没有保存鼠标点相对拼片中心的 grab offset，按下在拼片边缘会把拼片中心瞬移约 29px 到鼠标下方。二次修复：新增 active / held fragment offset，fragment pickup 不再 `node.setPosition(pointer)`，而是保持当前 node position，只在拖动时用 `pointer + offset` 作为物理追踪目标。验证：新增 scaffold 回归保护 grab offset；`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01PhysicsCollider.test.ts tests/cocos/M01PhysicsRandom.test.ts tests/cocos/M01PhysicsRotation.test.ts` 通过（71 tests）；`npm run typecheck` 通过；`git diff --check` 通过；真实 preview 探针确认边缘抓取 movement 从约 `29px` 降到约 `1.7px` 的自然 settling，邻近碎片最快速度约 `0.1 px/s`；`npm run smoke:m01-preview -- --enable-art-preview` 通过，`usedFallback=false`、completion `completed=true`、console/page errors 为空。注意：`npm run smoke:m01-preview-refresh` 的 `soft_reload_scene` 在本次 Cocos MCP 上超时，但单独刷新 `db://assets/scripts` 成功，预览服务已加载新 bundle，由 `?v=pickup-offset-fix-refreshed-20260525` runtime 探针确认。

2026-05-25 M01 拼片选中抖动修复：用户反馈“选中某个拼片后会快速抖动”。简单 click-select 的自动采样未复现静止抖动（50 帧位置 span 为 0、body speed 为 0），但复查真实拖拽链路确认第三层 root cause：被鼠标 / UI 控制的 held / active fragment 仍保留 `CircleCollider2D` / `PolygonCollider2D`，Box2D contact solver 会一边解碰撞、一边被代码强制追踪 pointer target，玩家从碎片堆里抓片时就容易表现为快速抖动或推挤周围拼片。修复：fragment 被按下拖拽时临时 `setFragmentColliderEnabled(node, false)`，click-pick 保持 held 状态期间也不参与碰撞；drop / reset / snap 成功路径统一重新启用 collider 并 `apply()`。这说明当前乱抖主因不是重量或重力数值，而是“被手抓住的拼片仍参与物理解算”。验证：runtime 探针确认 mouse down 与 held 状态 collider 为 `enabled=false`、drop 后恢复 `enabled=true`；`npm test -- tests/cocosProjectScaffold.test.ts -t "fragment"` 成功（14 passed）；`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01PhysicsCollider.test.ts tests/cocos/M01PhysicsRandom.test.ts tests/cocos/M01PhysicsRotation.test.ts` 成功（72 tests）；`npm run typecheck` 成功；`git diff --check` 成功；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`usedFallback=false`、completion `completed=true`、console/page errors 为空。

2026-05-25 M01 拼片 click-pick 双影闪烁修复：用户继续反馈“鼠标一点选某个拼片把它举起来，会闪现两个拼片一直闪”。复查后确认这是前一轮只关 collider 还不够：click-pick 后 held fragment 仍保留启用的 `RigidBody2D`，`moveHeldFragmentWithPointer()` 直接 `node.setPosition()` 时可能和 Cocos 物理 transform 同步互相覆盖；同时 art-preview 下 `shouldRenderArtPreviewUnderlay()` 对 fragment 的 `selected/highlighted` 状态会重新画标准灰盒几何底形，和水彩 sprite 叠出第二块拼片。修复：新增 `setFragmentPointerControl()`，鼠标控制期间同时禁用 collider 与 rigid body，drop / reset / snap 时恢复；active-drag velocity 更新只在 body 仍 enabled 时运行，否则直接移动 node；art-preview 下 fragment 不再绘制灰盒 underlay（debug/fallback 仍走已有早退）。验证：`npm test -- tests/cocosProjectScaffold.test.ts -t "fragment|duplicate greybox"` 成功（15 passed）；`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01PhysicsCollider.test.ts tests/cocos/M01PhysicsRandom.test.ts tests/cocos/M01PhysicsRotation.test.ts tests/cocos/M01GreyboxSession.test.ts` 成功（101 tests）；`npm run typecheck` 成功；`git diff --check` 成功；刷新 `db://assets/scripts` 成功；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`usedFallback=false`、completion `completed=true`、console/page errors 为空，真实输入 debug 中所有 fragment `graphicsFillAlphaByFragment = 0`，说明不再叠灰盒底形。

2026-05-25 M01 拼片自由放下恢复落地：用户反馈“选中举起了拼片，半空中把它放下，它没有自由落地下来”。Root cause 是上一条双影修复后，`place_fragment_freely` 仍调用 `parkFragmentBodyAtSnap()`，导致自由放下也被设成 Kinematic 停在半空；正确行为应区分自由落下和吸附停车。修复：新增 `releaseFragmentBodyToPhysics()`，free drop / reset 路径恢复 body enabled、collider enabled、`ERigidBody2DType.Dynamic`；只有 weak snap / target-piece snap 继续 `parkFragmentBodyAtSnap()`。验证：新增 scaffold 回归“freely dropped M01 fragments back into dynamic physics”；`npm test -- tests/cocosProjectScaffold.test.ts -t "freely dropped|fragment|duplicate greybox"` 成功（16 passed）；M01 相关 5 文件测试成功（102 tests）；`npm run typecheck` 成功；`git diff --check` 成功；刷新 `db://assets/scripts` 成功；`npm run smoke:m01-preview -- --enable-art-preview` 成功；额外 Playwright 探针把 `fragment_triangle_red_1` 拖到 `{x:0,y:0}` 松手后确认 `bodyType=Dynamic(2)`、body/collider enabled，0.65 秒内 y 从约 `-2.7` 掉到 `-160.5`。

2026-05-25 M01 物理地板全宽修复：用户反馈“拼片可以自由落体了，但是并没有落在地面就停止”。Root cause：可见 `M01GroundLine` 是整屏 960px 宽，但 `M01PhysicsGround` collider 只覆盖右侧碎片池 `x=200..440`；玩家在中间/左侧半空松手时，拼片会穿过看得到的地线继续下坠。修复：`M01PhysicsBoundary` 新增 `GROUND_COLLIDER_WIDTH = GROUND_DISPLAY_WIDTH`，ground collider 改为居中 `x=0`、宽 `960`，左右墙仍保留在碎片池两侧。验证：新增 scaffold 回归“spans the M01 physics ground across the full visible ground line”；`npm test -- tests/cocosProjectScaffold.test.ts -t "physics ground|freely dropped|physics boundary"` 成功；M01 相关 5 文件测试成功（103 tests）；`npm run typecheck` 成功；`git diff --check` 成功；刷新 `db://assets/scripts` 成功；Playwright 探针确认中央 x=0 放手后 ground 为 `{x:0,w:960}`，2.35 秒后拼片停在 `y≈-255` 且 `vx/vy=0`，不再掉到 `y<-600`；`npm run smoke:m01-preview -- --enable-art-preview` 成功，`usedFallback=false`、completion `completed=true`、console/page errors 为空。

2026-05-21 M01 拼片物理堆叠（box2d）落地：用户要求把 M01 的 9 个拼片从整齐 3×3 浮空网格改成"开场动画 + 物理小堆"。设计/计划评审三轮后实施。设计文档 `docs/plans/2026-05-21-m01-physics-pile-design.md`，实现计划 `docs/plans/2026-05-21-m01-physics-pile-plan.md`。本轮新增 5 个文件：`assets/scripts/cocos/M01PhysicsRandom.ts`（mulberry32 PRNG，3 个单元测试）、`M01PhysicsRotation.ts`（每形状的稳定休止角，3 个单元测试）、`M01PhysicsCollider.ts`（圆/三角/六边形 collider 几何，3 个单元测试，六边形 flat-top 修正）、`M01PhysicsBoundary.ts`（FRAGMENT_FLOOR 底+左+右静态墙）、`M01PhysicsPile.ts`（顺序投放控制器 + settle 检测）。修改 `M01GreyboxBootstrap.ts`：start() 末尾 attach boundary + pile + 锁输入 + "碎片正在落下..." 状态、`beginTokenDrag` 拒绝未 settle 的拖拽 + 切 Kinematic、`moveTokenDrag` 用 `linearVelocity = (target - pos)/dt` 推动以正确推开邻居、`resetTokenNode` 物理片改回 Dynamic 不 teleport、`weak_snap_fragment` 与 `snap_fragment_to_target_piece` 成功路径加 `parkFragmentBodyAtSnap`（保持 Kinematic 停在 target，玩家仍可拿回去）。`cc-shim.d.ts` 扩展 2D 物理类型导出。`M01GreyboxText.ts` 加 `physicsSettling` 文案键。开场动画约 2.5 秒（9 片 × 180ms 间隔 + 1200ms settle），每次 `Date.now()` 种子，吸附 ≠ 消费（物理 reset 后剩余拼片自由再下落）。验证：`npm test` 24 files / 220 tests passed；`npm run typecheck` 通过；`npm run smoke:m01-preview-refresh` 五步均 200；`npm run smoke:m01-preview -- --enable-art-preview` `ok: true`、`usedFallback: false`、completion 流程 `completed: true`、`consoleMessages` 与 `pageErrors` 为空、退出码 0。仍需用户手动 Cocos preview 验证开场动画落下、抽片连锁崩塌、吸附后再拿起这三种交互（Task 12 留给人）。

## Current Objective

**设计阶段冻结，转入原型阶段**。spec 已收口到 v1.9，接下来不再扩写 M11-M33，而是做两个关键原型验证：
- **P1-a 安全原型**：M01（分类与归纳）—— 验证基础管线（Cocos Creator + Arrog 式统一手绘墨线 + 低饱和配色 + 拖拽交互 + ToolCard 产出）
- **P1-b 危险原型**：M30 隐喻熔炉（概念融合）—— 验证 Stage 5 "命名仪式"能否产生真实认知动作体验。选 M30 而非 M31 的理由：M30 概念融合是 Stage 5 里打分最高、最典型的"范式生成"动作（Codex Round 4 独立评分 9/10），用最硬的关卡试金石失败了才真正证明 Stage 5 不成立。此原型若失败，Stage 5 整体砍掉或重构。

**当前执行焦点**：M01 第一关已完成本轮“完整可玩灰盒原型”阶段，可作为 P1-a 安全原型的当前基线。旧版主计划见 `docs/plans/2026-04-29-m01-overlap-evidence-greybox-plan.md`，已完成的非美术自动续跑计划见 `docs/plans/completed/2026-05-02-m01-non-art-autoloop-ralphex.md`，本轮 completion handoff 见 `docs/plans/completed/2026-05-02-m01-completion-autoloop-ralphex.md`。2026-05-02 曾短暂创建 M30 autoloop 计划，但未产生任何 M30 代码/配置改动；该计划已标记 PAUSED，不再作为当前 handoff。M30 及所有其他关卡仍暂停，直到 operator 明确下达恢复指令。

2026-05-15 M01 零框架审阅后修复：按本轮全代码审阅结果修复四处风险。1) 最新 `M02-flashlight.png` / runtime 手电图的三色按钮视觉中心已变化，旧热区仍停在红 `(359,53)` / 黄 `(360,42)` / 蓝 `(358,30)`；已更新为红 `(361,77)` / 黄 `(360,59)` / 蓝 `(359,43)`，并把按钮 hit size 抽成 `M01_FLASHLIGHT_ART_BUTTON_HIT_SIZE = 14x14`，smoke helper 同步。2) `syncArtSpriteFrame()` 增加 `requestedPath` 门禁，异步 `resources.load()` 回调返回时若 sprite 已切到新资源则忽略旧回调，避免手电显色时被旧 hidden sprite 反盖。3) Cocos 编辑器布尔属性从 `Boolean` 改为 `CCBoolean`，减少 3.8 属性类型警告。4) 失败校验灯返回的 `validationLightSeconds` 改为读取 config，不再硬编码 `2`。验证：先新增/更新回归并确认旧代码红灯；修复后定向 `npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/levels/stage1/M01MemoryGearController.test.ts` 成功（3 files / 85 tests）；最终 `npm test` 成功（21 files / 211 tests）；`npm run typecheck` 成功；`git diff --check` 成功。

2026-05-19 M01 拼片显色半透明 / 色相区分修复：用户确认当前颜色方向可以，但手电照亮后拼片失去隐藏态的半透明气质；随后指出黄/橘、蓝/紫区分不够。本轮保留 child Sprite 方案，但显色时不再切换到 colored fragment PNG；`M01GreyboxBootstrap.syncArtSpriteState()` 始终使用 `M01ArtSprite_hidden_*` 贴图，只通过 `sprite.color` 应用手电 / 混色 tint。当前实现改为用 `M01_BASE_RGB` 与 `M01_BEAM_RGB` 做逐通道 multiply，红/黄/蓝/橘/绿/紫全部由同一颜料模型推导，`OBSERVED_FRAGMENT_TINT_ALPHA = 255`；这样保持灰白水彩贴图的透明纹理气质，同时用更强的 RGB 差距拉开黄/橘与蓝/紫。`scripts/m01-preview-smoke.mjs` 同步从“检查 colored art sprite 名称”改为“检查 hidden sprite 名称 + observed art sprite RGBA 符合 multiply 推导色 + Graphics underlay alpha = 0”，防止未来回退到彩色贴片覆盖或低区分 tint。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（65 tests）；`npm run smoke:m01-preview-refresh && node scripts/m01-preview-smoke.mjs --enable-art-preview` 成功，真实预览中固定光照路径的 hidden sprite tint alpha 为 `255`，移动拼片后 `observedColorsByFragment` 清空且 hidden sprite 颜色回到白色未 tint 状态。

2026-05-19 M01 手电筒整支点击循环：用户反馈三颗手电按钮太小、容易点不到，要求“只要按到手电筒任意位置就亮一个灯，再按就切换一个灯，红黄蓝轮番亮”。本轮把 static `singleFlashlightTool` 美术层、art-preview 下的 flashlight token touch-end、以及非 art-preview 的手电点击路径都接到 `cycleFixedFlashlight()`；循环顺序为红 → 黄 → 蓝，当前未选中时第一次点击选红灯。smoke helper 不再依赖小按钮坐标，统一使用手电下半部整支热区测试点 `{ x: 360, y: 20 }` 连续点击三次，并分别断言红、黄、蓝固定泛光下的九片显色结果；completion 流程也改用同一个整支热区点亮手电。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（70 tests）；`npm run smoke:m01-preview-refresh && node scripts/m01-preview-smoke.mjs --enable-art-preview` 成功，真实预览 `usedFallback=false`，连续点击同一手电位置可依次得到 `flashlight_red` / `flashlight_yellow` / `flashlight_blue`，拖动拼片后灯光熄灭并回到灰白未显色。

2026-05-15 提示灯泡图标黑边版：用户提供 `/Users/danmac/Desktop/image-1778729785284.png`，要求加清楚黑色外轮廓并调整为适合作为以后提示按钮小灯泡图标的尺寸。本轮从源图透明前景 bbox `(129,126,335x468)` 裁切，等比缩放进 `256x256` RGBA 画布，保留透明底和水彩纸纹，新增约 `8px` 黑色手绘外轮廓与内侧边缘加深；同步覆盖 `assets/resources/art/icons/icon-hint.png` 与 `assets/art/icons/icon-hint.png`，并保留临时预览 `temp/icon-hint-outlined-256.png`。验证：三处输出均为 `256 x 256` RGBA；后续验证命令见本轮最终回复。

2026-05-15 提示图标 preview 缓存修复：用户反馈 preview 仍显示旧提示图标。Root cause：源 PNG 已更新，但 Cocos `library/b2/...icon-hint...png` 与 `library/cf/...icon-hint...png` 导入缓存仍是旧简笔灯泡；原 `npm run smoke:m01-preview-refresh` 只刷新 scripts 与 stage1 config，没有刷新 `db://assets/resources/art/icons`。本轮刷新 `db://assets/resources/art/icons` 与 `db://assets/art/icons` 后，两个 library PNG 的 SHA-256 均已变为新图 `0d284116646b915a11b5cb555a56e2bc9d12b7f9d1407fe5d1ceeb3eee65720b`；同时把 `scripts/m01-preview-refresh.mjs` 增加 `refresh_assets:resource-icons` 步骤，`tests/m01PreviewRefreshScript.test.ts` 增补回归。验证：`npm test -- tests/m01PreviewRefreshScript.test.ts` 成功；`npm run smoke:m01-preview-refresh` 成功且四个 MCP step 均 200；`git diff --check` 成功。当前进一步浏览器交互 smoke 被 Cocos preview 服务的既有 `preview-app/index.js` 500 阻塞，页面错误为 `Get http://127.0.0.1:7456/preview-app/index.js failed!`，需要重启/重新打开 Cocos Preview 后再做完整画面截图确认。

2026-05-15 提示灯泡比例 / 外轮廓修正：用户指出新灯泡在 preview 中变胖、外轮廓太黑。Root cause：Cocos 自动 trim 后图标 spriteFrame 为 `167x230` 左右的高瘦比例，但 `M01GreyboxBootstrap.addHintIcon()` 仍强制 `50x50` 显示，导致横向拉伸；上一版外轮廓使用近黑色粗 halo，和大螺母外线的深褐墨线不一致。本轮从 `/Users/danmac/Desktop/image-1778729785284.png` 重新生成 `256x256` 透明 PNG，主体按 `161x224` 等比放置，外轮廓改为大螺母同系深褐 `RGB(72,67,55)` 的细外描边；同步覆盖 `assets/resources/art/icons/icon-hint.png`、`assets/art/icons/icon-hint.png` 与临时预览 `temp/icon-hint-outline-gear-brown-256.png`。运行时新增 `M01_HINT_ICON_DISPLAY_SIZE = { width: 37, height: 50 }`，避免被拉胖；`npm run smoke:m01-preview-refresh` 现在同时刷新 resource/source icon 目录。验证：先新增 scaffold / refresh helper 红灯并确认失败；修复后 `npm test -- tests/cocosProjectScaffold.test.ts -t "hand-drawn lightbulb icon"` 成功，`npm test -- tests/m01PreviewRefreshScript.test.ts` 成功，`npm run smoke:m01-preview-refresh` 五步均 200，两个源 PNG 与两个 library PNG SHA-256 一致为 `c8880eb27f41ead8289d3433dce242da00a80cb068fcc77b34399ce5e742e1d2`，alpha bbox 为 `(45,13,212,243)`，`very_dark_pixels=0`；`npm run typecheck` 成功；`git diff --check` 成功。浏览器 preview 仍被当前 Cocos Preview 的 `preview-app/index.js` 500 阻塞，需重启/重新打开 preview 后再做画面级 smoke。

2026-05-13 M01 手电筒融合候选 / C 版接入：用户要求保留圆润三按钮手电的线条，但融合硬朗手电的米灰 / 旧铜 / 橄榄灰配色，并确保红黄蓝三个按钮保留在正面。本轮新增可复现脚本 `scripts/generate-m01-flashlight-candidates.mjs`，以 `flashlight_single_three_buttons-lemmy-pink-v3.png` 为形状母版、`m01-single-flashlight-tool.png` 为配色参考，生成 review-only 候选目录 `docs/design/generated-m01-art-slices/m01-flashlight-round-lines-hard-palette-v1/`。用户选择 C（`m01-flashlight-round-lines-hard-palette-v1-c-rice-gray-rim.png`），已同步覆盖 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-single-flashlight-tool.png`、`assets/art/stage1-m01/m01-single-flashlight-tool.png`、`docs/design/generated-m01-art-slices/m01-single-flashlight-tool-runtime-fixed.png` 与 `parts/flashlight_single_three_buttons.png`；Cocos refresh 更新了两处 `m01-single-flashlight-tool.png.meta` 的 trim 数据，以匹配 C 版更高的圆头轮廓。验证：五处 PNG 均为 `198x437` RGBA 且 SHA-256 一致；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（83 tests）；`npm run typecheck` 成功；`git diff --check` 成功；Cocos MCP 刷新 scripts / config / art surfaces 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`hasSingleFlashlightTool = true`、三色换灯 art sprite 正确、completion `completed = true`、console/page error 为空，截图见 `temp/m01-art-preview-clean-qa.png`。

2026-05-13 M01 手电筒 A 版临时预览：用户要求把手电筒从 C 切到 A 看效果。本轮将 `m01-flashlight-round-lines-hard-palette-v1-a-balanced-gear-bronze.png` 同步覆盖到 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-single-flashlight-tool.png`、`assets/art/stage1-m01/m01-single-flashlight-tool.png`、`docs/design/generated-m01-art-slices/m01-single-flashlight-tool-runtime-fixed.png` 与 `parts/flashlight_single_three_buttons.png`。这是本地预览切换，尚未提交 / 推送；如果用户确认 A，应再提交并推送。

2026-05-13 M01 手电筒 A 版黑墨边预览：用户要求给当前 A 版手电筒加类似大螺母的手绘黑边，以统一两者风格。本轮新增 `scripts/paint-m01-flashlight-ink-outline.mjs`，从 `m01-flashlight-round-lines-hard-palette-v1-a-balanced-gear-bronze.png` 可复现生成 `m01-flashlight-round-lines-hard-palette-v1-a-black-ink-outline.png`，只强化外轮廓与少量真实墨线，避免把水彩暗斑放大成脏块；同步覆盖 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-single-flashlight-tool.png`、`assets/art/stage1-m01/m01-single-flashlight-tool.png`、`docs/design/generated-m01-art-slices/m01-single-flashlight-tool-runtime-fixed.png` 与 `parts/flashlight_single_three_buttons.png`。验证：五处 PNG 均为 `198x437` RGBA 且 SHA-256 一致（`66d98a950dd49c539c9ee08ef36dd5981b54aac3f261a83ad5a88c32318d8473`）；`node --check scripts/paint-m01-flashlight-ink-outline.mjs` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（3 files / 84 tests）；`npm run typecheck` 成功；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`hasSingleFlashlightTool = true`、completion `completed = true`、console/page error 为空，截图见 `temp/m01-art-preview-clean-qa.png`。

2026-05-13 M01 最新稿线条加重修正：用户指出应改“最新的稿”，不能从旧 A 候选稿重做。本轮修正 `scripts/paint-m01-flashlight-ink-outline.mjs` 的输入为当前黑边稿 `m01-flashlight-round-lines-hard-palette-v1-a-black-ink-outline.png`，输出新的 `m01-flashlight-round-lines-hard-palette-v1-a-black-ink-outline-deepened.png`，再同步覆盖四处手电筒 runtime / 导出 PNG；不再直接从 `a-balanced-gear-bronze.png` 覆盖最新稿。拼片线条加重也落在当前 direct-slice 生成路径：`scripts/generate-m01-reference-texture-pieces.mjs` 对 21 张 hidden / colored runtime 拼片在保持透明 bbox 与比例不变的前提下向内加粗深色外轮廓，`tests/cocos/M01GreyboxArt.test.ts` 同步记录该生成期望并新增 outer contour 深色回归。验证：手电筒五处 PNG 均为 `198x437` RGBA 且 SHA-256 一致（`cedf859f63e5a2ad8d4a44659b2e27ddae02a8e3a94ba2f1ba49ace116937040`）；拼片 runtime PNG 仍为 `112x112` RGBA；`node --check scripts/paint-m01-flashlight-ink-outline.mjs` 成功；`node --check scripts/generate-m01-reference-texture-pieces.mjs` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 92 tests）；`npm run typecheck` 成功；`git diff --check` 成功；Cocos MCP refresh runtime-sprites / art stage1 / surfaces 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`hasSingleFlashlightTool = true`、三色贴图显色正常、completion `completed = true`、console/page error 为空，截图见 `temp/m01-art-preview-clean-qa.png`。

2026-05-13 M01 拼片轮廓二次加粗：用户反馈手电筒轮廓变化可见，但拼片变化不明显。本轮仅继续加重拼片 direct-slice 后处理，把 `thickenDirectPieceOutline()` 的内侧墨边从约 `3.2px` 提到约 `6.2px`，并使用更深墨色，保证缩到游戏内约 `56px` 尺寸后仍能看到接近大螺母的黑边；透明 bbox、Cocos untrim guard、圆 / 三角 / 六边形目标比例保持不变。同步更新 `tests/cocos/M01GreyboxArt.test.ts` 的生成期望和外轮廓 luminance 回归；由于粗黑边会拉低整张小贴图的平均色彩差，将彩色材料 channel-spread 下限按新线条权重微调。验证：`node scripts/generate-m01-reference-texture-pieces.mjs` 成功并重出 21 张 runtime 拼片与 contact sheet；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 92 tests）；`npm run typecheck` 成功；`git diff --check` 成功；Cocos MCP refresh `db://assets/resources/art/stage1-m01/runtime-sprites` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，三色贴图显色正常、completion `completed = true`、console/page error 为空，最新截图见 `temp/m01-art-preview-clean-qa.png`。

2026-05-13 M01 红光调浅：用户要求把红光颜色调浅。本轮只改运行时柔光颜色，不改红色拼片贴图或红灯显色逻辑；`assets/scripts/cocos/M01GreyboxBootstrap.ts` 中 `colorForBeam("red")` 从高饱和 `new Color(226, 58, 52, 150)` 调为更浅、更透明的暖粉红 `new Color(238, 116, 108, 104)`，黄 / 蓝光保持不变；`tests/cocosProjectScaffold.test.ts` 同步更新常量断言。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（4 files / 92 tests）；`npm run typecheck` 成功；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入仍为 `usedFallback = false`，红灯观察颜色与贴图显色正常，completion `completed = true`，console/page error 为空，截图见 `temp/m01-art-preview-clean-qa.png`。

2026-05-13 M01 固定手电换色 / 贴图显色修复：用户在 preview 复看发现切换手电灯色时没有按融合原则显色，也没有稳定显示对应美术贴片。Root cause 是上轮固定泛光只在红灯即时烟测中被覆盖，runtime 仍沿用旧观察窗口：observed color 2 秒过期，且玩家一按拼片就会调用 `suspendHeldFlashlightInteraction()` 把固定光束清掉。修复：`M01GreyboxSession.revealFragment(s)` 增加 `{ persistent: true }` 观察选项，固定手电批量照亮九片时使用持久显色；旧单点扫描仍保留 2 秒短暂窗口；`suspendHeldFlashlightInteraction()` 只释放 legacy held/gesture 手电，不再影响已固定的泛光。smoke helper / script 同步补黄灯、蓝灯、红灯三色换灯覆盖，并断言每种灯都切到对应 `M01ArtSprite_{color}_{shape}` 美术贴片、底层 graphics alpha 保持 0，红灯显色 2.2 秒后仍不消失。验证：先新增红灯并确认旧代码失败；修复后 `npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 93 tests）；`npm run typecheck` 成功；`node --check scripts/m01-preview-smoke.mjs` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入三色换灯均 `usedFallback=false`，黄灯 blue->green / red->orange，蓝灯 yellow->green / red->purple，红灯 blue->purple / yellow->orange / red->red，全部使用 art sprite，`graphicsFillAlphaByFragment` 全为 0，固定光束在拖片后仍 `flashlightBeamLit=true`，completion `completed=true`，console/page error 为空。

2026-05-13 M01 圆形拼片校圆修复：用户指出 preview 里的圆形拼片又不是正圆，放到目标图形上对不齐。Root cause 是桌面 `pinpian` 透明单片里的 circle 源图 alpha bbox 本身约为 `213~215 x 229~231`，`scripts/generate-m01-reference-texture-pieces.mjs` 按最大边等比塞入 112 方形画布后，runtime 仍保持约 `0.93` 的竖椭圆比例；此前回归只卡“来自 direct slice / 不被 Cocos trim”，没有卡“circle 必须保持正圆”。修复：direct slice 生成仅对 `shapeId === "circle"` 使用源 bbox 宽高分别映射到 `112x112`，把圆片几何校正为正圆；三角和六边形继续使用原 square crop，不改变直线边。新增回归 `keeps direct circle standard piece artwork round enough to match target geometry`，要求 hidden / red / yellow / blue / purple / orange / green 七个 circle runtime sprite 的 opaque bbox aspect 在 `0.985..1.015`。验证：先新增红灯并确认旧 circle runtime aspect `0.9286` 失败；修复后 `node scripts/generate-m01-reference-texture-pieces.mjs` 成功，七个 circle runtime bbox 全为 `112x112`；`npm test -- tests/cocos/M01GreyboxArt.test.ts -t "direct circle standard piece|directly from the prepared"` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 91 tests）；`npm run typecheck` 成功；刷新 Cocos runtime-sprites 与 soft reload 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，三色贴图显色和 completion 仍正常；最终 `npm test` 成功（21 files / 204 tests），`git diff --check` 成功。

2026-05-13 M01 三角 / 六边形比例校对修复：用户进一步要求确认三角形和六边形也没有变形。复核 runtime 目标几何后确认底部拼片目标是同一 56 宽标准件体系：圆形 bbox aspect 应为 `1.0`，等边三角和平顶正六边形 bbox aspect 应为 `2 / sqrt(3) = 1.1547`。Root cause 是圆形校正后仍沿用 square crop 的三角 / 六边形贴片实际 bbox 偏高，旧 runtime 约为 triangle `112x99..100`、hexagon `112x100..102`，aspect 只有 `1.10..1.13`，会在预览里显得不够正。修复：`scripts/generate-m01-reference-texture-pieces.mjs` 的 direct slice 路径改为按 shape 选择目标 bbox aspect，circle 使用 `1.0`，triangle / hexagon 使用 `2 / sqrt(3)`；`tests/cocos/M01GreyboxArt.test.ts` 的比例回归从 circle-only 扩展为 all standard pieces，并把“足够大”高度阈值按 shape 调整。重新生成后所有 circle runtime bbox 为 `112x112`，所有 triangle / hexagon runtime bbox 为 `112x97`，aspect `1.1546`，与目标轮廓匹配。验证：先新增红灯确认旧三角 / 六边形比例失败；修复后 `node scripts/generate-m01-reference-texture-pieces.mjs` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts -t "standard piece artwork proportional|directly from the prepared"` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 91 tests）；`npm run typecheck` 成功；刷新 Cocos runtime-sprites 与 soft reload 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，三色贴图显色和 completion 仍正常；最终 `npm test` 成功（21 files / 204 tests），`git diff --check` 成功。

2026-05-13 M01 手电筒三色按钮直连：用户要求手电筒位置不变，手电本体已有固定三色按钮，不再单独弹出选色面板，直接点击红 / 黄 / 蓝三枚按钮切换灯光。当前口径已同步到 `docs/design/game-design-spec.md`：手电筒本体位置固定，正面三枚按钮直接用于切换灯色，不弹出额外选色面板。Root cause 是 art-preview 仍保留 2026-05-05 的 `M01FlashlightButtonPicker` 交互，点击单体手电贴图或按钮热区都会先开 picker，再选灯。修复：删除 picker root / picker button / picker color helper；`M01GreyboxLayout` 将三枚按钮热区从 `18x18` 放大为 `32x32`，仍保持坐标 `red(360,68)`、`yellow(364,85)`、`blue(368,102)`；art-preview 下三个 flashlight token 的 `touch-end` 直接调用 `selectFixedFlashlightButton(token.controllerId)`，复用固定泛光 `activateFixedFlashlightBeam()`，单体手电大贴图只吞掉非按钮点击，不再触发选色。smoke helper / script 改为直接点击 button positions，并移除 `after_flashlight_picker_open` 步骤。验证：先新增红灯并确认旧 picker 路径失败；修复后 `npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts -t "flashlight|fixed buttons|art-preview mode"` 成功；`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 79 tests）；`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（5 files / 118 tests）；`npm run typecheck` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，三色按钮真实输入均 `usedFallback=false`，黄灯 / 蓝灯 / 红灯 click positions 分别为 `{x:364,y:85}` / `{x:368,y:102}` / `{x:360,y:68}`，三色贴图显色与 completion 正常，console/page error 为空；最终 `npm test` 成功（21 files / 204 tests），`git diff --check` 成功。

2026-05-13 M01 红灯光束视觉修复：用户反馈按红灯后“没有亮红光”。复现确认点击红按钮后 runtime 已进入 `flashlight_red`，但画面问题有两层：上一轮直连按钮把光束 anchor 设成按钮 token 本身，红光从按钮位置出发而不是从手电镜头口出发；同时红色 beam fill alpha 只有 46，叠在浅纸背景上过淡，肉眼容易看成没有亮。修复：新增固定镜头光源点 `FIXED_FLASHLIGHT_BEAM_ANCHOR = { x: 352, y: 32 }`，按钮坐标只负责选色，红 / 黄 / 蓝三色光束都从镜头口发出；保持红按钮热区仍在 `red(360,68)` 不漂移；红光 fill/stroke 调整为 `new Color(226,58,52,104)` / `new Color(204,64,54,132)`，黄蓝光也略增强。smoke 增加断言：红按钮 hit zone 保持 `(360,68)`，beam anchor 固定 `(352,32)`，beam target 仍为候选九宫格中心 `(332,-138)`。验证：先新增红灯并确认旧实现失败；修复后 `npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts -t "flashlight|art-preview mode"` 成功；`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（6 files / 131 tests）；`npm run typecheck` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入 `activeFlashlightId = "flashlight_red"`、`flashlightPosition = {x:360,y:68}`、`beamAnchor = {x:352,y:32}`、`beamTarget = {x:332,y:-138}`、九片贴图显色和 completion 正常，console/page error 为空；最新 clean QA 截图 `temp/m01-art-preview-clean-qa.png` 可见红光从镜头照向下方拼片。最终 `npm test` 成功（21 files / 204 tests），`git diff --check` 成功。已 reload 当前 in-app preview tab 到同一 URL。

2026-05-13 M01 手电柔光光效修正：用户指出当前红灯像一块硬色块，希望是柔和、自然、能照到九个拼片的光效。Root cause 是 `M01GreyboxBootstrap.drawFlashlightBeam()` 仍用单个高透明三角/梯形区域填充并描边；上条红灯修复增强了可见度，但也放大了硬边色面。修复：删除光束描边路径，新增 `SOFT_FLASHLIGHT_BEAM_LAYERS`、`FLASHLIGHT_BEAM_GLOW_STOPS` 与 `FLASHLIGHT_LENS_GLOW_LAYERS`；光效现在由镜头口晕光、沿光路密集低透明圆形光斑、以及非常窄且低透明的核心光束叠加构成，主视觉从硬三角色块改为柔和铺开的红/黄/蓝光域；显色贴片机制未改，九片仍使用对应 `M01ArtSprite_{color}_{shape}`。新增 scaffold 回归要求保留柔光层、glow trail、lens glow helper，并断言不再出现 `colorForBeamStroke`。验证：先新增 `FLASHLIGHT_BEAM_GLOW_STOPS` / `drawFlashlightGlowTrail` 断言并确认旧代码失败；修复后 `npm test -- tests/cocosProjectScaffold.test.ts -t "flashlight beam"` 成功；`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（6 files / 131 tests）；`npm run typecheck` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入 `usedFallback=false`、三色显色贴片仍正确、`graphicsFillAlphaByFragment` 全为 0、completion `completed=true`、console/page error 为空；`temp/m01-art-preview-clean-qa.png` 复看确认光效已从硬三角块调整为覆盖九片的柔和光域；最终 `npm test` 成功（21 files / 204 tests），`git diff --check` 成功。

2026-05-13 M01 手电红色按钮命中区对齐：用户反馈红色灯光按钮的点击位置和贴图不匹配。复查 `m01-single-flashlight-tool.png` 像素后确认当前单体手电贴图在 `{x:360,y:72}`、`58x128`、旋转 `168°` 下，三颗色钮视觉中心约为 red `{x:362,y:75}`、yellow `{x:364,y:84}`、blue `{x:368,y:98}`；旧 runtime 仍用 red `{x:360,y:68}` / yellow `{x:364,y:85}` / blue `{x:368,y:102}`，且每个隐形热区为 `32x32`，红黄蓝三块互相重叠，点红色图案时可能落进相邻按钮热区。修复：`M01GreyboxLayout.positionForFlashlightButton()` 改为贴图实测中心；`buildFlashlightNode()` 的固定按钮热区缩为 `14x14`，避免相邻色钮重叠；`scripts/m01-preview-smoke-helpers.mjs` 同步新的真实输入点击坐标。新增 layout 回归锁住三颗按钮中心、`14x14` hit zone，并断言任一按钮中心不会落进其他按钮热区。验证：先改测试并确认旧坐标/旧尺寸失败；修复后 `npm test -- tests/cocos/M01GreyboxLayout.test.ts -t "flashlight"` 成功；`npm test -- tests/m01PreviewSmokeHelpers.test.ts -t "flashlight|includes every"` 成功；`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocosProjectScaffold.test.ts` 成功（4 files / 80 tests）；`npm run typecheck` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入 `flashlightPosition={x:362,y:75}`、`activeFlashlightId="flashlight_red"`、三色显色贴片和 completion 仍正常、console/page error 为空。全量 `npm test` 成功（21 files / 206 tests）；`git diff --check` 成功。期间全量测试暴露蓝色水彩拼片 `averageChannelSpread = 13.98` 卡在旧阈值 `>14` 的边缘，已将该低饱和保护阈值收为 `>13.5`，不改资源。

2026-05-13 M01 移动拼片熄灭固定手电：用户确认仍应保持早期规则“只要移动拼片灯就熄灭”，并要求拼片走出灯光辐射范围后恢复灰白不显色。Root cause 是最近为固定泛光显色引入 `{ persistent: true }` 后，`suspendHeldFlashlightInteraction()` 只清 legacy held/gesture 手电，按下拼片不会再清固定光束和 observed colors。修复：`M01GreyboxSession` 新增 `clearObservedFragmentColors()`；`M01GreyboxBootstrap.suspendHeldFlashlightInteraction()` 现在在任意 fragment press / drag 起点同时清 `activeFlashlightId` / `activeFlashlightColor`、beam anchor/target/lit、观察色 reset scheduler 与 session observed colors，并立即 `syncVisualState()`，让候选拼片回到 hidden 灰白贴图。`docs/design/game-design-spec.md` 同步新口径；`scripts/m01-preview-smoke.mjs` 改为断言点灯后仍可三色显色、拖片后 fixed floodlight 熄灭、`observedColorsByFragment` 清空、art sprites 回到 `M01ArtSprite_hidden_*`。验证：先新增 session / scaffold 红灯并确认旧代码失败；修复后 `npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocosProjectScaffold.test.ts -t "fixed floodlight|fragment press|observed colors"` 成功；`npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01GreyboxLayout.test.ts` 成功（6 files / 134 tests）；`npm run typecheck` 成功；`git diff --check` 成功；`node --check scripts/m01-preview-smoke.mjs` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入 `usedFallback=false`，红灯显色步骤仍为 `M01ArtSprite_purple/orange/red_*`，`after_free_fragment_drag` 和最终状态均 `flashlightBeamLit=false`、`observedColorsByFragment={}`、九片回到 hidden 灰白贴图，completion `completed=true`，console/page error 为空，截图见 `temp/m01-art-preview-clean-qa.png`；最终 `npm test` 成功（21 files / 207 tests）。

2026-05-13 M01 Claude 新手电贴图替换：用户要求找到 Claude 刚生成的新手电筒贴图并替换当前运行时手电。找到最新候选 `docs/design/generated-m01-art-slices/m01-single-flashlight-tool-lens-down-candidate-v1.png` / `assets/art/stage1-m01/m01-single-flashlight-tool-lens-down-candidate.png`，源图为 `2048x2048` RGBA、镜头已朝下。新增可复现脚本 `scripts/sync-m01-lens-down-flashlight.mjs`，按 alpha bbox 将源图等比缩放到当前 `198x437` runtime 画布，并同步覆盖 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-single-flashlight-tool.png`、`assets/art/stage1-m01/m01-single-flashlight-tool.png`、`docs/design/generated-m01-art-slices/m01-single-flashlight-tool-runtime-fixed.png`、`parts/flashlight_single_three_buttons.png`；四处 SHA-256 一致为 `76fa8b35d5bd3ef00d6bb5d879fd1ee8dbeefca820c8ec86f7db00c860111043`。因新图本身已经 lens-down，同步把 static art `rotationDegrees` 从 `168` 改为 `0`，三颗按钮热区改为 red `{x:360,y:116}` / yellow `{x:360,y:103}` / blue `{x:361,y:90}`、`12x12`，固定光束锚点改为镜头中心 `{x:360,y:29}`。Cocos refresh runtime-sprites / stage1-art 后 `.meta` trim 更新为 `trimX=18, trimY=8, width=162, height=422`。验证：`node --check scripts/sync-m01-lens-down-flashlight.mjs scripts/m01-preview-smoke-helpers.mjs scripts/m01-preview-smoke.mjs` 成功；`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（5 files / 106 tests）；`npm run typecheck` 成功；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；额外 refresh `db://assets/resources/art/stage1-m01/runtime-sprites` 与 `db://assets/art/stage1-m01` 均 200；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入 `usedFallback=false`，三色按钮均可点亮，`beamAnchor={x:360,y:29}`，拖片后仍熄灯回灰白，completion `completed=true`，console/page error 为空，最新截图见 `temp/m01-art-preview-clean-qa.png`。最终 `npm test` 成功（21 files / 207 tests）。视觉备注：新稿在当前 `58x128` UI 显示框内比旧稿更窄、更正向垂直。

2026-05-14 M01 提示按钮灯泡图标替换：用户要求使用 Claude 放入的亮灯泡图标替换右上角“提示”文字按钮。找到 Claude worktree 中的最终导出 `.claude/worktrees/agitated-chaum-5165d5/assets/art/icons/icon-hint.png`，同步到主仓库 `assets/resources/art/icons/icon-hint.png`（`1638x1642` RGBA，SHA-256 `9c9d9d2c7f0c15c374b1b9b38dff53c3f95d9d2cc369c4bec7e0d9db3f689fd9`），Cocos refresh 后生成 `icon-hint.png.meta`。`M01GreyboxBootstrap.addHintButton()` 不再绘制 82x34 文本框和“提示”Label，而是保留 `M01HintButton` 点击节点并通过 `M01_HINT_ICON_RESOURCE_PATH = "art/icons/icon-hint/spriteFrame"` 加载 `M01HintButtonIcon` sprite，显示尺寸 `50x50`，原 `requestHint()` 回调和完成态隐藏逻辑保持不变。验证：先新增 scaffold 红灯并确认旧实现失败；修复后 `npm test -- tests/cocosProjectScaffold.test.ts -t "lightbulb icon"` 成功；`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（2 files / 62 tests）；`npm run typecheck` 成功；`git diff --check` 成功；Cocos refresh `db://assets/scripts`、`db://assets/resources/art/icons` 与 soft reload 均 200；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入 `usedFallback=false`，completion `completed=true` 且 `hintButtonVisible=false`，console/page error 为空；截图 `temp/m01-art-preview-clean-qa.png` 可见右上角已替换为亮灯泡图标。

2026-05-14 M01 手电筒切换到 M02 贴图：用户指定将手电筒换成 `assets/art/stage1-tools/M02-flashlight.png`。主工程原路径不存在，已从 Claude worktree `.claude/worktrees/agitated-chaum-5165d5/assets/art/stage1-tools/M02-flashlight.png` 同步到指定路径（`2048x2048` RGBA，SHA-256 `bdd2c412903bb6ac50519b01479b0faab79623069dc6d4ec59b197fafeb0939e`），Cocos refresh 后生成 `M02-flashlight.png.meta`。`scripts/sync-m01-lens-down-flashlight.mjs` 改为以该 M02 源图为唯一同步源，并新增黑底 / 边缘杂点剔除：先清掉连到画布边缘的不透明黑底，再只保留最大可见手电主体，导出透明底 `198x437` runtime PNG 到四处现有手电路径；runtime `.meta` trim 为 `trimX=18, trimY=8, width=162, height=422`。根据新图像素中心同步三色按钮热区为 red `{x:359,y:53}` / yellow `{x:360,y:42}` / blue `{x:358,y:30}`，热区缩为 `10x10` 避免相邻按钮重叠，固定光束锚点改为镜头中心 `{x:360,y:110}`；`scripts/m01-preview-smoke-helpers.mjs` 与相关测试同步更新。验证：先新增 scaffold 红灯并确认主工程缺源图失败；修复后目标测试与 layout / smoke helper 测试通过，最终 `npm test` 成功（21 files / 209 tests），`npm run typecheck` 成功，`git diff --check` 成功，Cocos refresh scripts / config / runtime-sprites / stage1-art / stage1-tools 与 soft reload 均 200；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入 `usedFallback=false`，三色按钮均可点亮，`beamAnchor={x:360,y:110}`，拖片后 `flashlightBeamLit=false` 且九片回到 hidden 灰白贴图，completion `completed=true`，console/page error 为空；截图见 `temp/m01-art-preview-clean-qa.png`。

2026-05-06 当前用户任务：接手隔壁线程报错后的 M01 art-preview 标准件 / 手动目标编辑工作。范围限定为复验当前未提交改动、确认报错是否仍存在、补齐 handoff；不自动提交，不恢复 M30 / 下一关。

2026-05-07 接盘复验结论：当前分支 `codex/m01-drag-greybox` 保留隔壁线程留下的大批 M01 未提交改动，范围集中在标准件 target manifest、手动目标持久化 / 导出、标准件交叠生成、art-preview 资源与 smoke helper。复验未复现报错：`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功（3 files / 82 tests）；`npm run typecheck` 成功；`git diff --check` 成功；`npm test` 成功（21 files / 183 tests）；`npm run smoke:m01-preview-refresh` 成功，MCP `refresh_assets` 与 `soft_reload_scene` 均返回 200；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`ok = true`、`realInput.usedFallback = false`、`manualTargetCompositionMode = true`、console/page error 为空。最新 clean QA 截图仍为 `temp/m01-art-preview-clean-qa.png`，可见大齿轮中心的标准件交叠目标、底部候选标准件、右侧单体三色手电；本轮只补 handoff，不提交，不恢复 M30。

2026-05-07 当前手拼团数值保全：用户要求确认目标生成器能否准确记住当前预览中的手拼团图案。本轮未刷新 / 导航当前 Chrome 页；因当前可见 Chrome 进程未暴露 CDP、AppleScript JS execution 仍被禁用，改为只读解析 Chrome Default Local Storage LevelDB 中 `liuhui-star-guardian:m01:manual-target-draft:v1` 的最新同 key entry。已将 6 个 placements 与由当前 `deriveM01TargetEvidenceFromPlacements()` 派生出的 6 个 evidence 精确落盘到 `docs/design/manual-target-captures/2026-05-07-m01-current-manual-target-export.json`。placement 坐标：`circle_yellow_1(-38,-38.5,r0)`、`circle_red_2(-2,14.5,r0)`、`triangle_blue_1(18,-40.5,r90)`、`triangle_yellow_2(36,-11.5,r180)`、`hexagon_blue_1(-33,3.5,r0)`、`hexagon_red_2(3,-62.5,r90)`；evidence 颜色序列为 `green, orange, orange, purple, green, purple`。验证：`jq empty docs/design/manual-target-captures/2026-05-07-m01-current-manual-target-export.json` 成功；用 Vite SSR 重新导入 `M01TargetPatternGenerator.ts`，从 capture placements 重新派生 evidence，并与 capture evidence `assert.deepEqual` 成功；`git diff --check` 成功。结论：生成器对 placements -> evidence 是确定性的；但真正“长期牢固”应以该 capture 或后续锁进 `m01-memory-gear.json` 为准，不能只依赖浏览器 localStorage。

2026-05-07 当前手拼团已锁入目标配置：已把上述 capture 写入 `assets/resources/configs/stage1/m01-memory-gear.json`，`targetPattern.locked = true`，6 个 target pieces 保留精确 `fragmentId / standardPieceId / position / rotation / layer`，6 个 generated overlap evidence 保留 `generatedOverlap.outline` 数值与颜色。运行时补齐 target-slot 优先级和 locked target 的 evidence 自动提交：当 6 个指定 fragment 分别落在精确目标位时，会按 `expectedFragmentId` 自动 stage 6 组 overlap evidence 并完成 M01；重叠的 evidence 磁吸区不会再抢走目标件位置。验证：`npm run typecheck` 成功；`npm test` 成功（21 files / 186 tests）；`git diff --check` 成功；自定义 Node 断言确认 config targetPattern/evidence 与 capture geometry/evidence 一致；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、`areAllEvidenceStaged = true`、`completionState.completed = true`、`reconstructedEvidenceCount = 6`、`totalEvidenceCount = 6`、`usedFragmentCount = 6`、console/page error 为空；completion 截图见 `temp/m01-preview-completion-smoke.png`，clean QA 截图见 `temp/m01-art-preview-clean-qa.png`。

2026-05-07 review 修复：复查上条锁定后发现 3 个问题并已修正：target snap 现在必须匹配 `expectedFragmentId`，同形状错片不会占用指定 target slot；`snap_fragment_to_target_piece` 会携带并应用 slot `rotation`，自动提交 evidence 前同时校验目标位置与旋转角度；已从所有 solution fragments 上移除 `decoy` 标签并补断言防止回归。smoke helper / smoke script 同步记录 `targetRotation` 和 runtime `targetPieceRotations`，避免只验证中心点。验证：先新增红灯并确认失败；修复后 `npm test -- tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/levels/stage1/M01MemoryGearController.test.ts tests/cocosProjectScaffold.test.ts` 成功（5 files / 95 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 188 tests）；自定义 Node 断言确认 config targetPattern/evidence 仍与 capture geometry/evidence 一致；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，completion `targetPieceRotations = { circle_yellow_1:0, circle_red_2:0, triangle_blue_1:90, triangle_yellow_2:180, hexagon_blue_1:0, hexagon_red_2:90 }`，console/page error 为空。

2026-05-07 平台旧交叠色块清理：用户指出上一版错误的彩色交叠 evidence 色块仍留在大平台上。Root cause 是 `M01GreyboxBootstrap.renderGreybox()` 仍把 `layout.evidence` 实例化为可见 `M01Token_current_manual_target_*` Graphics 节点；这些节点不参与拖拽判定，拖拽 / 完成逻辑只需要 layout evidence 数据。修复：大平台不再添加 visible evidence token，generated target evidence 的专用可见绘制函数也移除；evidence 继续作为 layout-only snap / validation data 使用。smoke 新增节点树断言，若场景里出现 `M01Token_current_manual_target_*` 会失败。验证：先新增红灯并确认失败；修复后 `npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（2 files / 58 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 188 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，console/page error 为空；额外初始预览截图 `temp/m01-art-preview-initial-no-platform-evidence.png` 的 runtime `platformEvidenceNodes = []`，大平台只剩灰色目标件轮廓。

2026-05-07 当前轮廓派生吸附 / 验证数据：用户指出吸附验证数据应根据现在平台目标轮廓生成，而不是旧 evidence。Root cause 是 layout/session/bootstrap 仍默认读取 JSON `config.evidence`，即使 targetPattern 已锁定也可能继续信任过期快照。修复：`M01TargetPatternGenerator` 新增 `deriveM01TargetEvidenceFromTargetPattern()` / `resolveM01TargetEvidenceFromConfig()` / `resolveM01ConfigWithCurrentTargetEvidence()`；当 `targetPattern.locked = true` 且 pieces 带 `fragmentId` 时，runtime evidence 一律从当前 `targetPattern.pieces` 的 `fragmentId / position / rotation` 重新派生，`config.evidence` 只保留为可对照快照。`M01GreyboxLayout`、`M01GreyboxSession`、`M01GreyboxBootstrap` 均接入 resolved config。验证：先新增 stale evidence 红灯，确认旧代码会使用 `stale_previous_overlap`；修复后 `npm test -- tests/cocos/M01TargetPatternGenerator.test.ts -t "current targetPattern"` 成功；`npm test -- tests/cocos/M01TargetPatternGenerator.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxSession.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocosProjectScaffold.test.ts` 成功（7 files / 114 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 189 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，completion 仍为 `reconstructedEvidenceCount = 6 / totalEvidenceCount = 6`，target rotations 仍为 `0,0,90,180,0,90`，console/page error 为空。

2026-05-07 平台目标渲染切到 overlap evidence：接手隔壁线程红灯后，按用户要求把 art-preview 目标渲染从 `renderTargetStandardPieces()` 替换为 `renderTargetOverlapEvidence()`。新渲染不再画 `M01TargetStandardPiece_*` 标准件本体，而是通过 `buildM01GreyboxTargetOverlapEvidencePlan(layout)` 只绘制 `layout.evidence` 的 `magnetPolygon`，节点命名为 `M01TargetOverlapEvidence_current_manual_target_*`，颜色来自 evidence `targetBlendColor`。为保持手拼导出的目标位置不被 work-area compact 影响，`M01GreyboxTokenNode` 记录 `sourcePosition`，overlap plan 使用 source position + 原始 magnet polygon。smoke 同步断言 art-preview 下有 6 个 target overlap evidence 节点。验证：先确认红灯 `npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 失败（缺 `buildM01GreyboxTargetOverlapEvidencePlan` / `renderTargetOverlapEvidence`）；修复后 `npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功（69 tests）；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（93 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 189 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，artPreview / completionArtPreview 均显示 6 个 `M01TargetOverlapEvidence_current_manual_target_*`，`realInput.usedFallback = false`，`completionState.completed = true`，console/page error 为空。

2026-05-07 目标槽吸附增加旋转门槛：用户指出拼片角度与目标图不一致时仍会轻吸附。Root cause 是 `resolveM01GreyboxDrop()` 的 locked target slot 命中只检查 `expectedFragmentId` / shape / 位置范围，运行时虽在完成验证前记录旋转，但吸附判定本身没有读取当前拼片角度。修复：`M01GreyboxBootstrap.handleTokenDrop()` 把 `tokenRotations` 中的当前角度传入 `resolveM01GreyboxDrop()`；`M01GreyboxDrag` 对 target piece slot 增加 1 度旋转容差，位置命中但角度不匹配时直接自由放置，并阻止继续落入弱磁吸路径。验证：先新增红灯 `does not snap an expected fragment to a locked target slot when its rotation is wrong`，确认旧逻辑会把 `fragment_triangle_blue_1` 以 `rotation: 0` 吸到 `target_piece_triangle_blue_1(r90)`；修复后 `npm test -- tests/cocos/M01GreyboxDrag.test.ts` 成功（12 tests）；`npm test -- tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxSession.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocosProjectScaffold.test.ts` 成功（5 files / 99 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 190 tests）；`git diff --check` 成功。预览 smoke 未跑通：`npm run smoke:m01-preview-refresh` 无法连接 `127.0.0.1:3000` Cocos MCP；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 无法连接 `127.0.0.1:7456` 预览服务。

2026-05-07 review follow-up：自查发现上一条仍漏掉真正的 `weak_snap_fragment` 路径，generated evidence 磁吸只看 shape / contour，`fragment_triangle_blue_1(r0)` 拖到 `current_manual_target_green_triangle_triangle_1` 仍会轻吸附。补修：`resolveEvidenceFragmentDrop()` 在筛选 shape-compatible evidence 时，如果该 fragment 是 locked targetPattern 中的 expected target fragment，也必须通过同一 1 度旋转门槛；decoy / 非 target fragment 保持原 shape-only 试拼行为。验证：先新增红灯 `does not weak-snap an expected fragment to generated evidence when its rotation is wrong` 并确认旧逻辑返回 `weak_snap_fragment`；修复后该红灯转绿，`npm test -- tests/cocos/M01GreyboxDrag.test.ts` 成功（13 tests）；相关回归 `npm test -- tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxSession.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocosProjectScaffold.test.ts` 成功（5 files / 100 tests）；`npm run typecheck` 成功；直接 Vite SSR 断言确认同一 evidence 下 `rotation: 0` 返回 `place_fragment_freely`、`rotation: 90` 返回 `weak_snap_fragment`；`npm test` 成功（21 files / 191 tests）；`git diff --check` 成功。

2026-05-07 预览连接恢复 / smoke 跟进：用户反馈 `127.0.0.1` 拒绝连接。排查确认 Chrome 停在旧 `7456` URL，但 `7456` preview 与 `3000` Cocos MCP 均无监听，Cocos Creator 未运行。已通过 `open -a /Applications/CocosCreator-3.8.8.app --args --path /Users/danmac/liuhui-star-guardian` 启动项目，随后 `curl -I http://127.0.0.1:7456/?scene=...&m01ArtPreview=1` 返回 200，`127.0.0.1:3000` MCP 监听恢复，`npm run smoke:m01-preview-refresh` 三步均 200。恢复预览后发现 smoke 自动输入仍按旧规则直接拖片，新增旋转门槛后会失败；已更新 `scripts/m01-preview-smoke.mjs`，在真实输入 completion 路径中按 `targetRotation` 点击 `M01Rotate90Button` 旋转后再拖放目标片，并在输出中记录 target piece 位置 / staged evidence。验证：`npm test -- tests/m01PreviewSmokeScript.test.ts` 成功（7 tests）；`npm run typecheck` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`completionState.completed = true`、`areAllEvidenceStaged = true`、目标旋转为 `0,0,90,180,0,90`、目标位置与 locked target 一致、console/page error 为空；`npm test` 成功（21 files / 191 tests）；`git diff --check` 成功。Cocos 启动曾把 `package.json` version 临时改成 `3.8.8`，已恢复为 `0.1.0`，不保留该工具副作用。

2026-05-11 M01 标准拼片贴片重生成：用户指出上一版拼片与已存档大螺母平台视觉质感不一致，并要求红 / 蓝 / 黄以外补齐紫 / 橘 / 绿显色状态，同时保持拼片边为直线。中途尝试过更贴近大螺母厚重描边 / 边缘沉积的版本，但用户明确纠正：不要复杂重绘，只需要在原标准拼片贴图上加水彩质感；随后进一步明确不应添加额外“拼片自身质感”，默认拼片先按透明玻璃片看。当前验收 contact sheet 已切到 `docs/design/generated-m01-art-slices/m01-standard-pieces-reference-glass-v12-contact-sheet.png`：保留原圆 / 三角 / 六边形的简洁轮廓和直线边，hidden/default 三张按参考图 `ig_023dc6d93fbd68670169f6ae779270819aa6a0e7cc7853e67b.png` 中浅灰图形区域抽取中性纹理，并模拟淡水墨上色的云状晕染，做成浅灰透明玻璃态；红 / 蓝 / 黄 / 紫 / 橘 / 绿六色 × 圆 / 三角 / 六边形 18 张只作为手电观察 / 失败验证显色贴图使用。新增 `scripts/generate-m01-reference-texture-pieces.mjs` 作为可复现生成器：确定性几何 mask 控制形状和尺寸，参考图采样控制 hidden 玻璃质感和彩色 pigment 显色质感。`M01GreyboxArt.getM01GreyboxRuntimeSpriteResourceForToken()` 改为普通 fragment 默认返回 hidden glass sprite，只有传入 `colorTokenOverride` 时才切到对应彩色 runtime sprite；`M01GreyboxBootstrap` 已在观察显色 / 失败显色时传入 override。用户指出贴图接入后视觉尺寸比目标图形变小，root cause 是早期贴片 PNG 的非透明内容只占 112px 画布约 80%-86%，被 Cocos 按标准件尺寸显示后可见图形缩小；已新增回归测试要求标准拼片 artwork bbox 宽高均 > 88%，v12 贴片维持接近满画布的非透明轮廓。用户又指出拼片不需要白色边、最外层边缘不能透明，否则与目标拼不上；已新增 `does not draw pale white rims around colored standard pieces` 和 `keeps standard piece outer contours opaque so they meet target geometry` 回归测试，玻璃感只由内部 alpha / 灰度纹理表达，外轮廓保持不透明。验证：Cocos MCP 刷新 `db://assets/resources/art/stage1-m01/runtime-sprites` 与 `db://assets/scripts` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（77 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 195 tests）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，默认碎片显示为浅灰 glass，`observedColor = purple`、completion `completed = true`、console/page error 为空，截图仍写入 `temp/m01-art-preview-clean-qa.png` / `temp/m01-preview-completion-smoke.png`。

2026-05-11 接手隔壁线程生图任务复验：当前实际接入 runtime 的 canonical source sheet 是 `docs/design/generated-m01-art-slices/m01-reference-style-pieces-contact-sheet.png`（14:25 与 runtime PNG 同批生成，hash 与早前 `m01-standard-pieces-reference-glass-v12-contact-sheet.png` 不同），生成入口为 `scripts/generate-m01-reference-texture-pieces.mjs`；本轮不再新增生图，只复核并补 handoff。当前画面确认：默认候选碎片使用浅灰 hidden glass sprite，手电观察 / 失败验证通过 `colorTokenOverride` 切到红 / 蓝 / 黄 / 紫 / 橘 / 绿彩色 sprite；目标平台仍只显示 6 个 `M01TargetOverlapEvidence_current_manual_target_*` 彩色 overlap evidence。验证：`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（3 files / 77 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 195 tests）；`git diff --check` 成功；`curl -I http://127.0.0.1:7456/?scene=...&m01ArtPreview=1` 返回 200，`curl http://127.0.0.1:3000/health` 返回 `{"status":"ok","tools":157}`；`npm run smoke:m01-preview-refresh` 成功（scripts / stage1-config refresh 与 soft reload 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`ok = true`、`realInput.usedFallback = false`、`observedColor = purple`、completion `completed = true`、6/6 staged evidence 全部为 true、console/page error 为空；最新截图仍为 `temp/m01-art-preview-clean-qa.png` / `temp/m01-preview-completion-smoke.png`。下一步若用户要回到更强纹理的 v12 视觉，需要明确把 v12 设为 canonical 并重刷 runtime sprites；否则按当前 14:25 reference-style 版本继续。

2026-05-11 M01 手电筒贴片替换：按用户要求使用内置 image generation 新生成的几何拼片 / 羊皮纸水彩风格手电筒，源图为 `/Users/danmac/.codex/generated_images/019e1607-f50f-7363-a8a5-134429f0bbfa/ig_021b7dd058c4fb30016a018bb715488199acc0c48c3aba2d48.png`。本轮从纸面图中分离手电筒主体，顺时针转为与现有道具一致的竖版方向，输出 `198x437` 透明 PNG，并同步覆盖 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-single-flashlight-tool.png`、`assets/art/stage1-m01/m01-single-flashlight-tool.png`、`docs/design/generated-m01-art-slices/m01-single-flashlight-tool-runtime-fixed.png`；Cocos `.meta` 未改动。验证：三处 PNG 均为 `198 x 437` RGBA，四角 alpha 为 0，alpha bbox 为 `(16,56)-(181,381)`；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（3 files / 77 tests）；`npm run typecheck` 成功；`git diff --check` 成功；Cocos MCP 已刷新 `db://assets/resources/art/stage1-m01/runtime-sprites` 与 `db://assets/art/stage1-m01` 并 soft reload 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`artPreview.hasSingleFlashlightTool = true`、completion `completed = true`、6/6 staged evidence 全部为 true、console/page error 为空；最新 clean QA 截图为 `temp/m01-art-preview-clean-qa.png`，右侧单体手电筒已显示为新的几何水彩版本。

2026-05-12 M01 手电筒手绘质感增强：用户指出新手电筒与大螺母平台仍有风格差，要求增强手绘感、加粗外轮廓、增加色彩浓淡起伏，并随后补充“适当加一点颜色起伏、线条可以稍微圆滑”。本轮不重生图、不改 Cocos `.meta`，只对现有 `198x437` 透明贴片做本地 paintover：平滑加粗外轮廓与内部结构线，加入低频水彩块面、柔和 ochre / green-grey / lavender 色彩起伏，减少毛刺脏点，保持原 alpha。已同步覆盖 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-single-flashlight-tool.png`、`assets/art/stage1-m01/m01-single-flashlight-tool.png`、`docs/design/generated-m01-art-slices/m01-single-flashlight-tool-runtime-fixed.png`；临时对照图为 `temp/m01-flashlight-handdrawn-before-after-v4-smooth-color.png`。验证：三处 PNG 均保持 `198 x 437` RGBA，四角 alpha 为 0，alpha bbox 仍为 `(16,56)-(181,381)`；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（3 files / 77 tests）；`npm run typecheck` 成功；`git diff --check` 成功；Cocos MCP 刷新 `db://assets/resources/art/stage1-m01/runtime-sprites` 与 `db://assets/art/stage1-m01` 并 soft reload 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`artPreview.hasSingleFlashlightTool = true`、completion `completed = true`、6/6 staged evidence 全部为 true、console/page error 为空；最新 clean QA 截图为 `temp/m01-art-preview-clean-qa.png`，右侧单体手电筒已显示平滑厚墨与水彩起伏增强版本。

2026-05-12 M01 手电筒线条圆润手绘修正：用户复看后认为色彩好了不少，但线条仍太整齐，希望更圆润、更像手绘。本轮保留上一轮颜色起伏，只重做线条层：用平滑低频笔压图打破均匀描边，轻微抬亮低压线段并在高压线段叠加圆润厚墨与少量复描感，让镜头环、外轮廓和内部结构线出现自然粗细段落；仍不改变 alpha / 尺寸 / Cocos `.meta`。已同步覆盖 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-single-flashlight-tool.png`、`assets/art/stage1-m01/m01-single-flashlight-tool.png`、`docs/design/generated-m01-art-slices/m01-single-flashlight-tool-runtime-fixed.png`；临时对照图为 `temp/m01-flashlight-line-rounding-before-after-v6.png`。验证：三处 PNG 均保持 `198 x 437` RGBA，四角 alpha 为 0，alpha bbox 仍为 `(16,56)-(181,381)`；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（3 files / 77 tests）；`npm run typecheck` 成功；`git diff --check` 成功；Cocos MCP 刷新 `db://assets/resources/art/stage1-m01/runtime-sprites` 与 `db://assets/art/stage1-m01` 并 soft reload 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`artPreview.hasSingleFlashlightTool = true`、completion `completed = true`、6/6 staged evidence 全部为 true、console/page error 为空；最新 clean QA 截图为 `temp/m01-art-preview-clean-qa.png`。

2026-05-12 当前线程接手报错复验：接手同项目隔壁报错线程后，按 repo handoff 只复验当前 M01 art-preview / 手电贴片 / 标准拼片工作树，不提交、不恢复 M30 / 下一关。当前未复现报错：`npm run typecheck` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 84 tests）；`npm test` 成功（21 files / 195 tests）；`git diff --check` 成功；`curl http://127.0.0.1:3000/health` 返回 `{"status":"ok","tools":157}`，M01 art-preview scene URL 返回 200；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`ok = true`、`realInput.usedFallback = false`、`observedColor = purple`、completion `completed = true`、6/6 staged evidence 全部为 true、`targetPieceRotations = { circle_yellow_1:0, circle_red_2:0, triangle_blue_1:90, triangle_yellow_2:180, hexagon_blue_1:0, hexagon_red_2:90 }`、console/page error 为空。最新截图仍为 `temp/m01-art-preview-clean-qa.png` / `temp/m01-preview-completion-smoke.png`；若后续再报错，应优先确认 Cocos preview / MCP 是否在线与资源是否 refresh，再进入代码根因排查。

2026-05-12 M01 默认灰白拼片直裁：落实用户决定：默认灰白拼片不再走“材质 / 玻璃 / 纹理设计”生成路径，而是从已批准源图 `docs/design/generated-m01-art-slices/m01-direct-grey-hidden-pieces-source.png` 直接裁出圆 / 三角 / 六边形三块；该源图与桌面 `/Users/danmac/Desktop/灰白拼片.png` 的 SHA-256 一致（`d2e813d650cc99e801452d5e9acde417defadbda2b57823745e9af42b5ef5ff5`）。彩色显色贴片仍保留当前 `m01-reference-style-pieces-contact-sheet.png` / `colorTokenOverride` 机制，只用于手电观察与失败验证显色。实现：`scripts/generate-m01-reference-texture-pieces.mjs` 新增 direct grey hidden source，hidden row 早返回直裁 crop，清掉旧 hidden glass/wash 死代码；重生成 `assets/resources/art/stage1-m01/runtime-sprites/hidden-fragments/m01-fragment-hidden-{circle,triangle,hexagon}.png` 与对应 Cocos `.meta`，hidden meta 现为完整 `112x112` 裁切。新增回归测试 `cuts default grey hidden standard pieces directly from the approved grey source image`，先红后绿，逐像素比较 runtime hidden sprite 与源图直裁期望，避免未来重新绕回材质生成。验证：`node scripts/generate-m01-reference-texture-pieces.mjs` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts -t "cuts default grey hidden standard pieces|standard piece outer contours|large enough|low-saturation"` 成功（5 tests）；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 85 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 196 tests）；`git diff --check` 成功；Cocos MCP refresh `db://assets/resources/art/stage1-m01/runtime-sprites` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`ok = true`、`realInput.usedFallback = false`、`observedColor = purple`、completion `completed = true`、6/6 staged evidence 全部为 true、console/page error 为空。最新 contact sheet 为 `docs/design/generated-m01-art-slices/m01-reference-style-pieces-contact-sheet.png`，clean QA 截图为 `temp/m01-art-preview-clean-qa.png`。

2026-05-12 M01 灰白拼片外圈留白 / 六边形轮廓修复：用户复看最新截图后指出灰白默认拼片边缘切得不干净，轮廓线外仍有纸底留白，六边形与轮廓线尤其不匹配。Root cause：上一轮虽已从批准源图直裁三块 hidden sprite，但直裁后仍套用理想几何 mask；该 mask 与源图手绘墨线不完全重合，导致源图纸底被保留在黑线外侧。修复：`scripts/generate-m01-reference-texture-pieces.mjs` 的 hidden 直裁路径改为基于源图深色轮廓构建 barrier，执行边缘 flood-fill 去背景，并对贴着透明边的浅色中性纸底做多轮 halo trim；同时微调 circle / triangle / hexagon 的 crop box，让三块都以自身手绘轮廓居中。新增回归测试 `clips paper outside the approved grey hidden piece outlines`，用 boundary light-neutral ratio 卡住“外缘仍是浅纸色”的坏图；`cuts default grey hidden standard pieces directly from the approved grey source image` 同步改为比较同一 outline-clip 直裁算法，确保默认灰白仍来自批准源图而非材质生成。验证：`node scripts/generate-m01-reference-texture-pieces.mjs` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts -t "clips paper outside|cuts default grey hidden|standard piece outer contours|large enough|low-saturation"` 成功（6 tests）；Cocos MCP refresh `db://assets/resources/art/stage1-m01/runtime-sprites` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`ok = true`、`realInput.usedFallback = false`、`observedColor = purple`、completion `completed = true`、6/6 staged evidence 全部为 true、console/page error 为空；最终复验 `npm run typecheck` 成功，`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 86 tests），`npm test` 成功（21 files / 197 tests），`git diff --check` 成功。最新 clean QA 截图仍为 `temp/m01-art-preview-clean-qa.png`，局部诊断图为 `temp/m01-hidden-grey-checker-preview.png`。

2026-05-12 M01 候选拼片数量与摆放调整：按用户要求，当前真实 M01 候选拼片池从 13 个收敛为 9 个，只保留 3 个圆、3 个三角、3 个六边形，并移到右侧单体手电筒下方的 3×3 区域。保留 6 个 locked target 必需碎片：`fragment_circle_yellow_1`、`fragment_circle_red_2`、`fragment_triangle_blue_1`、`fragment_triangle_yellow_2`、`fragment_hexagon_blue_1`、`fragment_hexagon_red_2`；保留 3 个同形状 decoy：`fragment_circle_blue_1`、`fragment_triangle_yellow_1`、`fragment_hexagon_yellow_1`。新坐标为三列 `x = 332 / 392 / 452`，三行 `y = -74 / -138 / -202`，位于 `flashlight_red(420,68)` 下方。更新 `M01GreyboxLayout` / controller / session / drag / target generator / smoke helper 相关测试，使它们不再依赖已移除的旧候选 ID。验证：先新增 / 更新红灯，确认旧配置仍为 13 个并失败；修复后 `npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/levels/stage1/M01MemoryGearController.test.ts` 成功（2 files / 35 tests）；`npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（4 files / 65 tests）；`npm test -- tests/cocos/M01TargetPatternGenerator.test.ts` 成功（3 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 197 tests）；`git diff --check` 成功；Cocos `npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`totalFragments = 9`、completion `completed = true`、6/6 staged evidence 全部为 true、console/page error 为空。新的初始画面截图为 `temp/m01-art-preview-initial-qa.png`，交互 smoke 后 clean QA 截图仍为 `temp/m01-art-preview-clean-qa.png`。

2026-05-12 M01 全部图形整体左移修正：用户澄清“全部图形”包括大螺母和手电筒，不只是右侧候选拼片。本轮把整组 gameplay 视觉中心左移 40px：大螺母 / board 读 `entity_memory_gear(-40,0)`，工作台中心与 6 个 locked target piece 均同步左移，右侧单体手电筒 art / hit points / picker root 同步左移，9 个候选拼片保持手电筒下方 3×3，最终坐标为三列 `x = 292 / 352 / 412`、三行 `y = -74 / -138 / -202`，`flashlight_red = (380,68)`。验证：`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（3 files / 40 tests）；`npm test -- tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01TargetPatternGenerator.test.ts` 成功（3 files / 41 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 197 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`totalFragments = 9`、completion `completed = true`、6/6 staged evidence 全部为 true、console/page error 为空。最新初始截图为 `temp/m01-art-preview-initial-qa.png`，交互 clean QA 截图仍为 `temp/m01-art-preview-clean-qa.png`。

2026-05-12 M01 视觉中心二次微调：用户复看后觉得整体视觉上可能仍略偏右，本轮在上一版基础上再整体左移 20px，总左移量为 60px。大螺母 / board 为 `(-60,0)`，工作台中心与 6 个 locked target piece 继续同步，手电筒 hit points 为 `flashlight_red(360,68)` / `yellow(364,85)` / `blue(368,102)`，手电贴图为 `(360,72)`，picker root 为 `(270,82)`，9 个候选拼片三列更新为 `x = 272 / 332 / 392`。验证：先改 layout / art / drag / smoke helper 测试到新坐标并确认红灯；实现后 `npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（4 files / 53 tests）；`npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01TargetPatternGenerator.test.ts tests/levels/stage1/M01MemoryGearController.test.ts` 成功（3 files / 50 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 197 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`totalFragments = 9`、completion `completed = true`、6/6 staged evidence 全部为 true、console/page error 为空。最新初始截图已刷新为 `temp/m01-art-preview-initial-qa.png`。

2026-05-12 M01 六边形预览比例根因修复：用户指出 preview 里的六边形不像正六边形。排查确认不是 Cocos `UITransform` 非等比拉伸，也不是源图裁框本身歪；真正 root cause 是 Cocos 自动 sprite-frame trim 把灰白 hidden hexagon 从 `112x112` 裁成 `111x99`，随后 runtime 又按 `56x56` 方形显示，导致视觉比例被压扁。同类标准件也存在不同程度 auto trim。修复：`scripts/generate-m01-reference-texture-pieces.mjs` 为所有标准件 PNG 写入 alpha=2 的四角 trim guard，视觉透明但阻止 Cocos 自动裁掉方形画布；新增 `keeps standard piece sprite frames untrimmed so Cocos cannot distort their geometry` 回归，要求标准件 sprite-frame `trimX=0 / trimY=0 / width=112 / height=112 / rawWidth=112 / rawHeight=112`。Cocos refresh 后 hidden / colored 标准件 meta 均保持完整 `112x112`。验证：先新增红灯并确认旧 meta 失败；修复后 `npm test -- tests/cocos/M01GreyboxArt.test.ts -t "untrimmed|cuts default grey hidden|large enough|outer contours|clips paper outside"` 成功（6 tests），`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（3 files / 82 tests）。

2026-05-12 M01 手电光束照射修复：用户指出“选了手电筒的光也没有光线照射出来”，并确认希望光线照到下方拼片，不同底色拼片出现对应交融色。Root cause：光束裁剪范围 `FRAGMENT_FLOOR` 仍是旧底部碎片池 `y=-320..-176`，而候选拼片已搬到右侧手电下方 `y=-74/-138/-202`；同时 `moveFlashlightBeamWithPointer()` 在照到拼片触发显色时提前 return，缺少一次直接重画光束。修复：把光束可见范围收束到当前右侧 3×3 拼片与手电区域 `x=200..440, y=-260..120`；移动光束时无论是否触发 `scanFlashlightBeamAtTarget()` 显色都调用 `drawFlashlightBeam()`。新增 scaffold 回归覆盖当前光束范围与“显色时仍重画光束”。验证：先新增红灯并确认失败；修复后 `npm test -- tests/cocosProjectScaffold.test.ts -t "flashlight beam visible|redraws the M01 flashlight beam"` 成功；`npm run typecheck` 成功；Cocos refresh scripts / config / runtime-sprites 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入输出 `flashlight_red(360,68)`、`beamTarget=(272,-74)`、`observedColor=purple`、completion `completed=true`、console/page error 为空；自定义预览截图 `temp/m01-flashlight-beam-reveal-qa.png` 确认红色光束打到右侧蓝底圆片并显示紫色。最终 `npm test` 成功（21 files / 200 tests），`git diff --check` 成功。

2026-05-12 M01 固定手电泛光显色：用户要求“手电筒的光线不要再设置成可以跟着鼠标乱动”，改为选中灯色后一次照亮下方 9 个候选拼片，并让不同底色拼片呈现对应交融色。同步更新 `docs/design/game-design-spec.md` 的 M01 交互契约：当前候选为 3 圆 / 3 三角 / 3 六边形，手电作为固定观察工具，不再跟随鼠标 / 手指移动或手势拉伸射程。实现：`M01GreyboxSession` 增加 `revealFragments()` 批量显色；`M01GreyboxBootstrap` 通过 `activateFixedFlashlightBeam()` 在 picker / flashlight click / drop 选灯后固定 `beamTarget` 到 3×3 候选中心 `(332,-138)`，批量 reveal 全部候选，并从全局 pointer move / beam gesture 主路径移除鼠标跟随控制；smoke helper / smoke script 改为断言 `observedColorsByFragment` 九片全量显色。验证：先新增红灯并确认旧 held-flashlight 路径失败；修复后 `npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 91 tests）；`npm run typecheck` 成功；`node --check scripts/m01-preview-smoke.mjs && node --check scripts/m01-preview-smoke-helpers.mjs` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入 `usedFallback=false`，选红灯快照 `beamTarget=(332,-138)`、九片显色为 blue->purple / yellow->orange / red->red，completion `completed=true`，console/page error 为空；单独视觉截图 `temp/m01-fixed-floodlight-nine-pieces.png` 确认固定红光一次照亮 9 个拼片。最终 `npm test` 成功（21 files / 201 tests），`git diff --check` 成功。

2026-05-12 M01 显色贴图化修正：用户进一步指出“显色也通过贴图来实现，而不是页面呈现交互色”。保留上条固定泛光交互，但修正表现层：art-preview 中 observed / validation 显色不再让 fragment 底层 `Graphics` 用 `colorTokenOverride` 填充程序色；当有可用 art sprite 且无 fallback underlay 时，`shouldUseTextureBackedFragmentReveal()` 会把 fragment graphics presentation 压回 `normal`、color override 传 `undefined`，只让 `syncArtSpriteFrame()` 切换到对应彩色水彩贴图（如 `M01ArtSprite_purple_circle`、`M01ArtSprite_orange_triangle`、`M01ArtSprite_red_hexagon`）。smoke 增补真实 runtime 断言：选红灯后九片 `artSpriteNamesByFragment` 必须全部为对应彩色 sprite，且 `graphicsFillAlphaByFragment` 必须全部为 `0`。验证：先新增红灯并确认缺少贴图显色断言失败；修复后 `npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（2 files / 61 tests）；`npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 91 tests）；`npm run typecheck` 成功；`node --check scripts/m01-preview-smoke.mjs` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，真实输入 `usedFallback=false`、选红灯快照九片彩色 sprite 名称全匹配、graphics fill alpha 全为 `0`、completion `completed=true`、console/page error 为空；视觉截图为 `temp/m01-texture-backed-floodlight-nine-pieces.png`。最终 `npm test` 成功（21 files / 201 tests），`git diff --check` 成功。

2026-05-12 M01 拼片贴图定稿收口：用户确认红黄蓝拼片贴图方向，并要求保留最终图与参考图、丢弃本轮过程稿。已将正式资产收拢到 `docs/design/generated-m01-art-slices/final-puzzle-piece-textures/`：`m01-puzzle-piece-rgb-texture-sheet-selected-v1.png` 为当前选中的红黄蓝拼片贴图，`m01-puzzle-piece-gray-white-master-reference.png` 为灰白未显色母版参考，`m01-puzzle-piece-color-pattern-reference.png` 为彩色花纹 / 色彩参考；已删除本轮试错目录 `docs/design/generated-m01-art-slices/imagegen-piece-tone-candidates/`。本轮只整理设计美术资产，未接入 Cocos runtime sprite / meta。

2026-05-12 M01 显色拼片改用桌面 `pinpian` 单片：用户指出隔壁线程已在桌面 `/Users/danmac/Desktop/pinpian` 放好可直接使用的彩色拼片，不应再从整表临时生成 / 混色。排查确认 `pinpian` 中共有 21 张透明 RGBA 单片：灰白、红、黄、蓝、橙、紫、绿各 3 个形状，另有两张整表参考图。已将 21 张单片复制并语义命名到 `docs/design/generated-m01-art-slices/final-puzzle-piece-textures/direct-piece-slices/`，生成脚本 `scripts/generate-m01-reference-texture-pieces.mjs` 现在直接读取这些透明单片，按 opaque bbox 缩放成 112×112 runtime sprite，并保留 Cocos trim guard；`M01GreyboxArt` 的 hidden / colored fragment `sourceFile` 也改为对应 direct slice，显色贴图不再指向旧 `m01-reference-style-pieces-contact-sheet.png` 生成源。回归测试改为逐像素比较 runtime sprite 与 direct slice 缩放结果，确保手电显色和默认灰白都来自这些单片贴图。验证：`node scripts/generate-m01-reference-texture-pieces.mjs` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（24 tests）；`npm test -- tests/m01PreviewSmokeHelpers.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocos/M01GreyboxSession.test.ts` 成功（37 tests）；`npm run typecheck` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，选红灯后九片 `artSpriteNamesByFragment` 为 direct-slice 生成的 `purple/orange/red` 贴图，`graphicsFillAlphaByFragment` 全为 0，completion `completed=true`，console/page error 为空。最新 clean QA 截图仍为 `temp/m01-art-preview-clean-qa.png`。

## Current Phase

- **Design: FROZEN at v1.9**
- **Prototyping: M01 greybox prototype complete for this phase; hints, error feedback, repair state, completion ToolCard preview, full real-input completion smoke, refresh helper, and first-pass placeholder runtime art are documented as the current baseline**
- **Local toolchain**: Cocos Creator 3.8.8 installed at `/Applications/CocosCreator-3.8.8.app` on 2026-04-24.
- **Cocos project integration**: repo now has Cocos Creator 3.x project metadata (`.creator/`, `settings/v2/`, shared `profiles/v2/packages/scene.json`) and generated `.meta` files for current assets.
- **Local editor automation**: Cocos MCP server plugin installed locally at `extensions/cocos-mcp-server/` and configured to auto-start on `127.0.0.1:3000` when Cocos opens this project.

## In Scope

- 搭建 Cocos Creator 3.8+ 项目脚手架
- 实现 M01 完整可玩版本（占位美术）
- 实现 M30 的 Stage 5 原型，专门测试"命名仪式"的可行性（不再"M30 或 M31"二选一，锁死 M30）
- 设计并实现 ToolCard 数据结构 + Journal/Toolkit UI 的最小可用版本
- 搭建 AI 美术 LoRA / 提示词管线（统一手绘墨线 + 低饱和淡彩）
- 生成 Stage 1 场景概念图，验证统一手绘风格能否覆盖多种谜题装置
- 将已选中的 Stage 1 场景图落成项目内可复用美术资产

## Out Of Scope

- **扩写 M11-M33 的详设**（明确停止——Codex Round 3 诊断此为"精致拖延"）
- 后端 / 云同步 / 个性化系统（P10 以后）
- 微信小游戏发布（与成人向定位需要重新评估）

## Important Decisions (v1.9 收口点)

1. **受众**：成人及青年（16+）。明确**不做儿童产品**——认知工具对儿童过于抽象。
2. **核心体验**：**修复即提炼**（不是学习）—— 唤起 / 命名 / 整理玩家已有的模糊认知直觉。
3. **关卡数**：50 → **33 关**（10-6-7-5-5 金字塔分布），按玩家 A+B 正交性裁定。
4. **美术设计已替换**：统一采用 Arrog 式简约手绘语言——清晰可见的墨线轮廓、低饱和淡彩、功能性简约机械、大量留白；颜色服务线条，不再使用“角色水彩 / 世界墨线 / Klee 饱和填色”的分层方案。
5. **整体风格锚点已入库**：`docs/design/style-references/2026-04-22-unified-handdrawn-style-anchor.png` 作为全项目视觉审稿与提示词对齐基准，重点学习“留白、线条、简化机械、尺度关系”，避免跑回淡水彩氛围图或复杂机械插画。
6. **游戏界面参考图已入库**：2026-04-23 新增两张界面/面板参考图，定位为服务本游戏开发的通用 UI 与面板资产基准，不再按外部工具界面方向解释；文件见 `docs/design/style-references/2026-04-23-game-interface-style-reference.png` 与 `docs/design/style-references/2026-04-23-game-ui-board-style-reference.png`。
7. **莱米角色参考图已入库**：2026-04-24 新增 `docs/design/style-references/2026-04-24-lemmy-rabbit-style-reference.png`，作为莱米 / 小兔子主角的造型、比例、线条和陶土红 / 灰蓝低饱和彩色块基准；资产副本见 `assets/art/style-references/lemmy-rabbit-style-reference.png`。
8. **关键机制**：**认知工具册**（§6.5）—— 每关产出 ToolCard，正面感性（工具名+场景+结晶）/ 反面理性（何时使用+生活例子+常见陷阱）。
9. **技术栈**：Cocos Creator 3.8+ / TypeScript / Dragon Bones 或 Cocos Skeleton2D（Spine Pro 已废弃）。
10. **财务定位**：用户明确"现阶段不考虑成本" —— passion project 优先，商业账延期。
11. **来自 Codex Round 3 的必做验证**：M01 和 Stage 5 原型必须先跑通，否则后续扩写无意义。

## Risks / Blockers

### 来自 Codex Round 3 诊断的三大系统性风险
- **Stage 5 命名仪式可能是空壳盖章** —— 若 M30 原型无法让玩家真实经历那个认知动作，整个游戏核心卖点破产
- **认知工具册可能反噬游戏本体** —— 玩家把卡片当经验值集邮，跳过关卡本身
- **反面文案废话工厂** —— 33 张卡 × 多条内容若由 AI 批量生成，会变成新的"AI 味注释系统"

### 已承认但延期处理的风险
- 商业可行性（Codex Round 1-2 的"卖给谁"和"回不了本"批评）——passion project 路线下暂缓
- M34 拉普拉斯沙盘被砍可能是错的（Codex 两轮坚持应该保留）——待 Stage 5 原型结果后再评估是否加回

### 仍需监控的风险
- v1.9 spec 已近 1050 行，后续任何修改必须警惕"自我繁殖"重新启动
- 继续改 spec 的诱惑远大于去写 Cocos 代码的诱惑——这是作者心理定势，需要用"原型验证优先"原则硬性对抗
- 美术风格曾多次向“淡水彩氛围图”或“复杂机械插画”跑偏——后续出图必须以“线条优先、装饰减少、结构可信”为硬约束
- 批量生图容易出现“每关概念成立，但同批次内部尺度关系和留白强度不一致”——后续需要逐关筛选与二次迭代，而不是整批直接定稿

### 2026-04-24 一致性审视处理项
- 已同步 `CLAUDE.md` / `vision.md` 的受众定位：成人及青年（16+），不再使用“不限年龄”作为入口定位。
- 已将 spec 内残留的 SpineRuntime / `resources/spine/` / Spine Pro 统一到 Dragon Bones 或 Cocos Skeleton2D。
- 已将残留的“Klee 点彩 / 保罗·克利风格 LoRA”替换为“清晰墨线 + 低饱和淡彩 / 统一手绘线条 LoRA 或提示词管线”。
- 已把 P1-a 验收拆成两层：先灰盒/占位美术跑通交互、胜利判定和 ToolCard；随后用四张主动参考图校准首个美术切片。
- 当前已生成 M02-M10 概念图、落盘 M02-M08 资产，但立即原型对象是 M01；M01 除全局风格锚点外尚无明确可复用关卡资产，原型应先确认是否直接用锚点拆资产，还是先灰盒实现。
- Stage 1 首批资产保留为备选概念 / 历史探索，不再作为生产级风格标准；正式图后续围绕四张主动参考图重新生成或精修。

## Next Recommended Step (immediate)

**下一步：继续 M01 art-preview 视觉 polish，不自动跳关**。2026-05-03 最新口径：保留正常关卡布局，但删除地面大平台 / `fragmentFloor`；目标图移到左侧，作为小型圆框缩略图，可点击放大，且源图按 1677:938 原比例显示，不再压成正方形；单个三色手电放到右侧；ToolCard 说明不在通关前显示，只在完成后出现。2026-05-03 本轮改换平台 / 手电贴片修法：不再靠 runtime 非等比宽高或 tint 补救。平台修正版必须从最新 rich-color 水彩候选派生，不能退回过程拆片 `parts/assembly_gear_empty.png`。当前使用 `docs/design/generated-m01-art-slices/m01-overlap-memory-gear-inner-circle-runtime.png` 作为正方透明运行时母版：由 `m01-anchor-cleaned-v2-gear-empty-center-rich-color-candidate.png` 派生，源内圈约 `center=(592,598), radius=328px`，导出为 `1212×1212`，运行时统一显示为 `553×553`，内圈显示半径约 `149.7px`，匹配 staging 约束的 150 半径，并已清掉底部地线 / 右侧小球。手电已改为 `docs/design/generated-m01-art-slices/m01-single-flashlight-tool-runtime-fixed.png`，补强线条并把材质提亮到平台同类灰褐水彩；runtime 静态层保留 `58×128`、旋转 `168` 度，但不再设置 tint。rich-color 母版修正后已刷新 Cocos art surfaces / scripts，并复跑 `npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa`，结果 `ok: true`、`realInput.usedFallback = false`、`artPreview.enabled = true`、`staticArtCount = 2`、`hasFragmentFloor = false`、`bottomLight = steady_on`、ToolCard 标题“分类与归纳”、console/page error 为空；clean QA 截图见 `temp/m01-art-preview-clean-qa.png`。下一 checkpoint 应继续按截图逐项 polish：底部碎片池现在需要避开平台脚座，目标缩略图仍需要真圆形裁切 / 遮罩确认，手电按钮热区与旋转后视觉位置继续复核。M30 及其他关卡仍暂停；只有 operator 明确恢复 M30 / 下一关时才进入新工作流。

2026-05-04 接手隔壁线程后当前 reality：本轮未继续左侧圆形缩略图方案，而是把目标参考图作为 `260×145.31` 的 1677:938 水彩提示板放进齿轮中心；左侧 `referencePattern` 灰盒仍存在但不再承载静态目标贴图。art-preview 中 overlap evidence 节点继续作为隐藏吸附区存在，不再绘制 evidence marker 贴片；错误拼接会在 2 秒验证窗口内以真实碎片颜色闪烁两次，随后自动退回候选碎片池。最新 clean QA 截图为 `temp/m01-art-preview-clean-qa.png`；下一 checkpoint 应重点肉眼评估中心目标提示图是否过度遮挡拼接盘，必要时调整尺寸 / 透明度 / 层级。

**当前 M01 灰盒进度**：
- 2026-05-06 M01 manual target draft persistence：用户追问“刚刚手拼的目标图形数据是否丢失，不应该吧”。复查确认：目标图形生成器此前只提供 `__m01ManualTargetTools.deriveEvidence()` 按需导出，不会自动保存 runtime `tokenPositions/tokenRotations`；本轮多次刷新页面后，未导出的手拼草稿无法从 repo/temp 中恢复。修复后新增 `M01ManualTargetPersistence.ts`，手动构图模式会把平台内拼片 `{ fragmentId, position, rotation }` 自动写入 browser `localStorage`，预览 reload 后 `M01GreyboxBootstrap.restoreManualTargetDraft()` 自动恢复；`__m01ManualTargetTools` 同步新增 `saveDraft()` / `restoreDraft()`。为避免以后需要用 blocked `javascript:` URL 读 runtime，art-preview 还会维护隐藏 DOM 节点 `#m01-manual-target-export`，同步 `data-placements-json` / `data-evidence-json`，方便直接读取生成结果。当前用户再次手拼后检查时，导出节点读到 `placementCount = 0`、`evidenceCount = 0`，继续 root-cause 后发现导出只读 `tokenPositions` 缓存且只在部分事件后同步，可能出现视觉节点位置已经改变但导出仍为空；修复为 `getManualTargetPlacements()` / blend overlay 优先读取实际 Cocos Node `position`，并在 `update()` 中持续同步/持久化 draft 与 hidden export。验证：新增 `tests/cocos/M01ManualTargetPersistence.test.ts`；`npm test -- tests/cocos/M01ManualTargetPersistence.test.ts tests/cocosProjectScaffold.test.ts -t "manual target persistence|persists manual target draft"` 成功；`npm run typecheck` 成功；`npm test` 成功（21 files / 180 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、console/page error 为空。额外 Playwright 闭环自测：故意把旧 `tokenPositions` 缓存写成 `-9999/-9999`，只移动真实 Cocos Node 到平台中心，导出仍得到 `placementCount = 2`、`evidenceCount = 1`（`target_overlap_purple_circle_circle_1`），reload 后仍恢复为 `2 / 1`，console/page error 为空；当前 in-app browser 未刷新前直接读取 `#m01-manual-target-export` 为 `0 / 0`，说明当前页可读取草稿已为空，后续新构图才会被持续保存。
- 2026-05-06 M01 current manual target visual rescue：用户确认当前浏览器仍未移动手拼目标，要求读取并记录。只读检查当前 in-app/browser export：`#m01-manual-target-export` 存在但 `placementCount = 0`、`evidenceCount = 0`、console warning/error 为空；Computer Use 视觉检查确认平台中心仍可见 6 片手拼目标。由于当前页的程序层导出为空、Chrome AppleScript JS execution 被禁用，且为避免地址栏注入造成导航/丢失，没有对当前页执行脚本注入或 reload。已把当前画面按视觉兜底记录为 `docs/design/manual-target-captures/2026-05-06-m01-current-manual-target-visual-capture.json`，包含估算的 6 个标准件 placements、重复 id 的候选说明、可见 overlap 意图和“programmatic export was empty”的警示；该记录不能替代精确 runtime 坐标，但保住了当前目标概念。
- 2026-05-06 M01 manual target stale-evidence correction：用户指出平台上出现的 4 个交叠色块相隔很远，并质疑这不是自己数小时前用 6 个拼片拼出的目标图。复查确认：当前 `m01-memory-gear.json` 仍是 `targetPattern.locked = false`、`targetPattern.pieces = []`，仓库和 `temp/` 中没有保存用户手动六拼片目标的 placements/evidence；屏幕上那 4 个色块来自旧 legacy `evidence_*` 坐标，不是用户新拼的目标。修正：legacy generated-overlap evidence 只在目标锁定 / evidence snap enabled 时合成 `magnetPolygon` 并显示；手动构图模式继续隐藏旧 evidence，避免把历史数据误当目标图。验证：新增/调整 `tests/cocos/M01GreyboxLayout.test.ts` 覆盖“manual mode hides legacy generated overlap targets”与“locked legacy targets synthesize magnet polygons”；`npm run typecheck` 成功；targeted layout/drag tests 成功；`npm test` 成功（20 files / 177 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；in-app browser 刷新后平台回到空白手动构图台，无旧目标色块。
- 2026-05-06 M01 legacy overlap target preview repair：用户打开预览后指出平台上仍未出现交叠颜色目标图形。Root cause：当前 `m01-memory-gear.json` 仍处于 `targetPattern.locked = false` 的手动构图模式，且 4 个旧 `evidence` 只有 `generatedOverlap.offset/sourceShapes`，没有新生成器产出的 `generatedOverlap.outline`；上一轮显示逻辑只放行带 `magnetPolygon` 的 generated evidence，因此旧 evidence 继续隐藏。修复：`M01GreyboxLayout` 对 legacy generated-overlap evidence 自动用 `sourceShapes + offset + targetBlendColor` 合成交叠多边形，并以交叠外框中心归零生成 `magnetPolygon`；当前预览无需先写回新 JSON outline，也能在平台上显示紫 / 绿 / 橙目标轮廓并保留同形状磁吸。验证：新增 `tests/cocos/M01GreyboxLayout.test.ts` 回归；`npm run typecheck` 成功；`npm test` 成功（20 files / 176 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，clean QA 截图 `temp/m01-art-preview-clean-qa.png` 可见平台目标色块。
- 2026-05-06 M01 generated target visibility / shape-only snap check：按用户要求复查“平台只留下目标图案交叠部分，同时同形状拼片即使颜色不符也能靠近磁吸”。结论：锁定目标阶段的磁吸已经按 `generatedOverlap.outline -> magnetPolygon` 工作，`M01GreyboxDrag` 以 evidence tags / source shape 判定弱吸附，不以拼片颜色判定；本轮补齐显示侧缺口，`M01GreyboxBootstrap` 现在只让带 `magnetPolygon` 的 generated evidence 在 art-preview 下绘制为交叠多边形目标，旧 evidence marker 贴图仍不显示，普通无 outline evidence 仍保持隐藏。验证：新增/扩展回归测试覆盖“同形状错颜色可 weak snap、错形状不吸附”以及“generated target evidence visible as overlap polygons”；`npm test -- tests/cocos/M01TargetPatternGenerator.test.ts tests/cocosProjectScaffold.test.ts -t "magnetic hit contour|generated target evidence"` 成功；`npm run typecheck` 成功；`npm test` 成功（20 files / 175 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`manualTargetCompositionMode = true`、`realInput.usedFallback = false`、console/page error 为空。
- 2026-05-06 M01 target overlap manifest generator：用户确认手动拼图时“交叠部分”才是正式目标图案，且这些交叠色块的轮廓应作为后续谜题阶段的磁吸轮廓。实现：新增 `M01TargetPatternGenerator.ts`，输入当前手动摆放的 `{ fragmentId, position, rotation }` 标准件清单，自动派生 M01 `evidence[]`：每个 evidence 以两片标准件的真实交集多边形为 `generatedOverlap.outline`，按隐藏本色生成 `targetBlendColor`，保留 `solution.fragmentIds`、`sourceShapes`、`offset`、`areaRatio`、`tolerance`。`M01GreyboxLayout` 现在把 outline 转成 `magnetPolygon`，`M01GreyboxDrag` 在 locked evidence 模式下用该多边形命中，而不是只用外接矩形；`M01GreyboxBootstrap` 也会用 outline 绘制 generated overlap。为方便多生成目标图案，预览页新增开发导出入口 `globalThis.__m01ManualTargetTools`，包含 `getPlacements()` 与 `deriveEvidence()`；手动摆好后可在浏览器控制台执行 `copy(JSON.stringify(__m01ManualTargetTools.deriveEvidence(), null, 2))` 复制目标 evidence。验证：新增 `tests/cocos/M01TargetPatternGenerator.test.ts`；`npm test -- tests/cocos/M01TargetPatternGenerator.test.ts tests/cocos/M01StandardPieceBlend.test.ts tests/cocosProjectScaffold.test.ts` 成功（3 files / 54 tests）；`npm run typecheck` 成功；`npm test` 成功（20 files / 174 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`manualTargetCompositionMode = true`、`realInput.usedFallback = false`、console/page error 为空；额外 Playwright 临时预览确认 `__m01ManualTargetTools` 存在，空场景下 `placementCount = 0`、`evidenceCount = 0`。
- 2026-05-06 M01 manual target overlap blend repair：用户指出在大螺母平台手动拼目标图案时，标准件交叠区域没有正常显示融合色。Root cause：当前标准件是 Cocos `Graphics` 精确几何直接绘制，交叠表现只依赖每个拼片自身颜色的 alpha 叠加，没有按 M01 颜料规则计算红+蓝=紫、红+黄=橙、蓝+黄=绿的显式交集色层。修复：新增 `M01StandardPieceBlend.ts` 纯几何 helper，用标准圆 / 正三角 / 正六边形多边形和当前旋转角求交集；`M01GreyboxBootstrap` 在 `targetPattern.locked === false` 的手动构图模式下新增 `M01ManualTargetBlendOverlay`，收集位于拼接盘内的候选标准件，实时绘制交叠区域的融合色覆盖层，并在拖动、点击放置、旋转、视觉同步时重绘。验证：新增 `tests/cocos/M01StandardPieceBlend.test.ts`；`npm test -- tests/cocos/M01StandardPieceBlend.test.ts tests/cocosProjectScaffold.test.ts` 成功（2 files / 51 tests）；`npm run typecheck` 成功；`npm test` 成功（19 files / 171 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`manualTargetCompositionMode = true`、`realInput.usedFallback = false`、console/page error 为空，截图见 `temp/m01-preview-manual-composition-smoke.png`。
- 2026-05-06 takeover verification after adjacent-thread error：接手后未复现新的红测。当前工作树仍包含隔壁线程留下的大批 M01 art-preview / 标准件 / 手动目标编辑未提交改动，分支 `codex/m01-drag-greybox` 比远端 ahead 1。验证：`npm run typecheck` 成功；targeted `npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（5 files / 89 tests）；`npm test` 成功（18 files / 167 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，输出 `manualTargetCompositionMode = true`、`realInput.usedFallback = false`、`artPreview.staticArtCount = 1`、`hasOldTargetReferenceCard = false`、`hasSingleFlashlightTool = true`、console/page error 为空，截图刷新为 `temp/m01-art-preview-clean-qa.png` 与 `temp/m01-preview-manual-composition-smoke.png`。
- 2026-05-06 current-thread takeover smoke failure fix：本线程接手后重新跑 `npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa`，复现锁定目标后的真实输入通关 smoke 红灯：5 组 evidence 中 `target_overlap_visual_rescue_orange_circle_triangle_1` 没有 staged，completion snapshot 为 `areAllEvidenceStaged = false`、`bottomLight = off`。Root cause：该 circle+triangle generated-overlap 磁吸多边形非常细，Cocos 真实输入把目标点量化成整数 `(17, 28)` 后，相对多边形中心仅偏约 `0.13px` 但严格 polygon hit-test 判定为外部，导致 `resolveM01GreyboxDrop()` 返回 `place_fragment_freely`。修复：`M01GreyboxDrag` 对 generated overlap contour hit-test 增加 2px 边缘距离容差，保留形状标签匹配与轮廓优先，不扩大到任意 evidence 方框吸附；新增 `tests/cocos/M01GreyboxDrag.test.ts` 回归“browser coordinate quantization”。验证：先确认新增测试红灯；修复后 `npm test -- tests/cocos/M01GreyboxDrag.test.ts -t "coordinate quantization"` 成功；`npm run typecheck` 成功；targeted `npm test -- tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01ManualTargetPersistence.test.ts tests/cocos/M01StandardPieceBlend.test.ts tests/cocos/M01TargetPatternGenerator.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（8 files / 103 tests）；最终整理后复跑 `npm run typecheck` 成功、targeted `npm test -- tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 78 tests）、`npm test` 成功（21 files / 181 tests）、`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`completion.areAllEvidenceStaged = true`、`completionState.completed = true`、`bottomLight = steady_on`、ToolCard 标题“分类与归纳”、console/page error 为空，截图刷新为 `temp/m01-art-preview-clean-qa.png` / `temp/m01-preview-completion-smoke.png`。
- 2026-05-06 M01 workbench target scale alignment：用户指出平台上已留下拼接目标图形，但拖起 / 放下的拼片尺寸又和目标对不齐。Root cause：`EVIDENCE_WORK_AREA_SCALE = 0.85` 原本只该压缩 5 个目标 overlap 的整体分布位置，代码却把每个 `generatedOverlap.outline` / `magnetPolygon` 也乘了 0.85；候选拼片仍是 `M01_STANDARD_PIECE_DISPLAY_SIZE = 56×56`，导致目标图形比实际拼片小一圈。修复：`buildEvidenceWorkNodes()` 继续用 0.85 缩放 evidence positions，但调用 `buildEvidenceNodeAt(..., sizeScale = 1)` 保持每个工作台 overlap 轮廓为标准件原尺寸；左侧 reference preview 仍单独用 `REFERENCE_PATTERN_SCALE = 0.4` 做缩略。新增 `tests/cocos/M01GreyboxLayout.test.ts` 回归“workbench overlap outlines at standard-piece scale while compacting positions”，先确认红灯（18px 轮廓被缩为 15.3px），修复后通过。验证：`npm test -- tests/cocos/M01GreyboxLayout.test.ts -t "standard-piece scale|snaps the two fragments"` 成功；`npm run typecheck` 成功；targeted `npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 79 tests）；`npm test` 成功（21 files / 182 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`completion.areAllEvidenceStaged = true`、`bottomLight = steady_on`、ToolCard 标题“分类与归纳”、console/page error 为空，截图刷新为 `temp/m01-art-preview-clean-qa.png` / `temp/m01-preview-completion-smoke.png`。
- 2026-05-06 M01 hand-composed target preservation clarification：用户补充“它从我们的手拼切过去，应该完美保留手拼时的尺寸和比例关系”。据此明确规则：`generatedOverlap.outline` 是手拼导出的 source of truth；锁定后的工作台目标只允许把整组 evidence 位置重新排到平台中心，不允许缩放、重算或用当前标准件几何替代手拼 outline。已撤回一次“用 current standard-piece geometry 重新合成”的临时假设，保留 explicit outline 优先；`tests/cocos/M01GreyboxLayout.test.ts` 的 scale 回归进一步断言 `evidence.magnetPolygon` 必须逐点等于 `evidenceConfig.generatedOverlap.outline`。验证：`npm test -- tests/cocos/M01GreyboxLayout.test.ts -t "standard-piece scale"` 成功；`npm run typecheck` 成功；targeted `npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 79 tests）；`npm test` 成功（21 files / 182 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`completion.areAllEvidenceStaged = true`、`bottomLight = steady_on`、ToolCard 标题“分类与归纳”、console/page error 为空。
- 2026-05-07 M01 locked target manifest restore：用户用标准件卡对照后指出目标图仍拼不上且目标内容像是缺失。Root cause：当前锁定目标是 2026-05-06 的 visual rescue 兜底记录，`programmaticExportAtCapture.placementCount = 0`，随后正式 `m01-memory-gear.json` 只保留了 5 个估算 `visual_rescue` overlap evidence，却把 `targetPattern.pieces` 清成 `[]`；六个手拼标准件的目标 manifest 没进运行时，平台自然只能显示局部估算 overlap，缺少完整手拼目标基准。修复：把 visual rescue 记录中的 6 个标准件 placement 恢复到 `targetPattern.pieces`，保留 position / rotation / layer，让 `M01GreyboxLayout.targetPieceSlots` 与 `buildM01GreyboxTargetStandardPiecePlan()` 在锁定模式下重新渲染完整手拼标准件目标，generated overlap evidence 继续叠在同一几何基准上。先把 layout / art / drag / scaffold 旧“targetPieceSlots 为空”断言改成“必须保留 6 个手拼标准件”并确认红灯，再补 JSON 后 targeted 测试通过。验证：`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocosProjectScaffold.test.ts` 成功（4 files / 90 tests）；`npm run typecheck` 成功；`npm test` 成功（21 files / 182 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 MCP step 均 200）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`completion.areAllEvidenceStaged = true`、`bottomLight = steady_on`、console/page error 为空；额外初始截图 `temp/m01-art-preview-initial-target-restored.png` 确认 runtime 有 6 个 `M01TargetStandardPiece_*` 与 5 个 evidence。
- 2026-05-07 M01 visual-rescue false-lock rollback：用户复查指出“这根本就不是我当时拼的，也变形了”。深挖确认上一条“restore”判断错误：`docs/design/manual-target-captures/2026-05-06-m01-current-manual-target-visual-capture.json` 明确 `programmaticExportAtCapture.placementCount = 0` / `evidenceCount = 0`，且状态为 visual rescue only；代码层 `getManualTargetPlacements()` 只扫描可拖 `layout.fragments` 的真实 Cocos Node，锁定后的非交互 `targetPieceSlots` / `M01TargetStandardPiece_*` 即使肉眼可见也不会进入导出。因此当时看到“平台有目标”并不等价于有可锁定手拼数据。真正 root cause 是：没有精确导出时仍把视觉估算当成锁定数据源，并且 unlocked 模式还会把 explicit visual-rescue `generatedOverlap.outline` 转成 `magnetPolygon` 显示，造成假目标持续出现在平台。修复：撤回估算的 6 个 `targetPattern.pieces`，把真实配置切回 `targetPattern.locked = false` / `pieces = []`，note 明确禁止从 visual rescue estimates 锁定；`buildGeneratedOverlapMagnetPolygon()` 现在先检查 evidence snap 是否启用，manual composition 模式下即使 evidence 有 explicit outline 也不生成/显示目标轮廓。新增/调整回归覆盖“visual rescue explicit outlines 在手拼模式隐藏”、真实配置未锁定、locked fixture 仍可测试 evidence magnetism；targeted `npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts tests/m01PreviewSmokeHelpers.test.ts` 成功（6 files / 99 tests）。最终验证：`npm run typecheck` 成功；`npm test` 成功（21 files / 183 tests，第一次并行跑曾有单个 PNG 测试 5s 超时，单独与全量重跑均成功）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`manualTargetCompositionMode = true`、`isEvidenceStaged = false`、console/page error 为空；额外初始截图 `temp/m01-art-preview-initial-manual-open.png` 证明平台空白，runtime `targetPieceCount = 0`、export `placementCount = 0` / `evidenceCount = 0`。
- 2026-05-06 M01 target-setting evidence magnet disable：用户指出目标设置阶段平台上仍有旧 evidence 吸附。Root cause：`targetPattern.locked = false` 且 `pieces = []` 已表示当前处于人工目标设置阶段，但 `M01GreyboxDrag.resolveEvidenceFragmentDrop()` 只看 `layout.evidence.length > 0`，仍会对平台上的 hidden evidence zones 触发 `weak_snap_fragment`。修复：`M01GreyboxLayout` 新增 `evidenceSnapEnabled`，当 `targetPattern.locked === false` 时关闭 evidence 吸附；drag resolver 在关闭状态下只尝试已锁定/已声明的 target-piece slot，否则自由放置，不再进入 evidence weak snap。保留锁定目标后的 evidence magnetism 回归测试，避免后续谜题阶段能力丢失。验证：`npm run typecheck` 成功；`npm test` 成功（18 files / 165 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；真实浏览器拖拽 `fragment_triangle_red_1` 到旧 `evidence_purple_upper_left` 区域后 `evidenceSnapEnabled = false`、`isEvidenceStaged = false`、状态为“已把碎片 fragment_triangle_red_1 放在工作区。”，截图见 `temp/m01-no-evidence-snap-target-stage.png`。
- 2026-05-05 M01 target-piece geometry calibration：用户指出可拖瓷片与中心目标拼接图的拼片位置 / 尺寸对不上。Root cause：中心目标图使用 Cocos `SpriteFrame` 裁切后的透明边界渲染，旧代码仍按手写槽位坐标和未校准尺寸处理，导致 target-piece snap zones 与目标图视觉锚点不在同一坐标系；一度按未裁透明画布放大到 `560×313` 后肉眼 QA 发现目标图过大，已收回为按裁切内容校准的 `184×102.92`。修复：新增 `M01_TARGET_REFERENCE_DISPLAY_SIZE` / `M01_TARGET_REFERENCE_PIECE_SLOT_SIZE`，将 6 个隐形 target-piece snap zones 对齐到目标贴图裁切内容的拼片中心，并保持 `64×64` 命中区覆盖 `56×56` 可见瓷片；`M01GreyboxDrag` 改为 evidence reconstruction 命中优先，避免新槽位抢走 overlap evidence 的真实拖拽；同步修复 smoke helper 旧手电坐标断言和 smoke 脚本重复 `clickLocalPoint` 声明。验证：`npm run typecheck` 成功；`npm test` 成功（18 files / 161 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、`completion.areAllEvidenceStaged = true`、`bottomLight = steady_on`、ToolCard 标题“分类与归纳”、console/page error 为空；clean QA 截图刷新为 `temp/m01-art-preview-clean-qa.png`。
- 2026-05-04 M01 evidence marker visibility fix：用户复看 preview 指出旧 evidence 仍显示在平台上。Root cause：前一轮只在 `buildM01GreyboxTokenArtPlan()` 过滤了 `token.kind === "evidence"`，但真实 runtime 的 `M01GreyboxBootstrap.addShapeNode()` 会对每个 token 直接调用 `addTokenArtSprite()`；因此 art-preview 仍给 `layout.evidence` 节点挂载了旧 evidence marker sprite。补充发现 target guide 也曾被放在 `layout.gear.position`，会把 clue 图压进平台中心；已改回 `layout.referencePattern.position` 左侧提示位，尺寸 `168×94`，并增加测试禁止 target guide 放到 repair platform。修复：`addTokenArtSprite()` 对 evidence token 直接返回 `null`，保留隐藏 snap zone / hit target，但不再挂载 evidence marker 贴图；`tests/cocosProjectScaffold.test.ts` 新增 runtime 层防回归。验证：`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功（60 tests）；`npm run typecheck` 成功；刷新 Cocos scripts + soft reload 后 `npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`artSpriteCount = 14`、`staticArtCount = 2`、console/page error 为空；截图 `temp/m01-art-preview-clean-qa.png` 中平台已无旧 evidence marker，只保留左侧目标提示和玩家实际放置的灰白瓷片。
- 2026-05-04 M01 takeover refresh-test timeout fix：用户要求接手隔壁线程报错并完成其刚刚工作。接手后复现全量测试失败：`tests/m01PreviewRefreshScript.test.ts` 两个 `spawnSync` 测试在 Vitest 默认 5s 内超时；业务与 art-preview smoke 均可运行。Root cause：测试通过子 Node 启动 refresh helper，其中离线 MCP 路径会执行真实 `fetch`，在全量并发测试负载下超过单测默认超时。修复：`scripts/m01-preview-refresh.mjs` 的 MCP `fetch` 增加 `AbortSignal.timeout(2000)`；测试里的 `spawnSync` 增加 10s 子进程 timeout，并把两个用例的 Vitest timeout 提到 15s。补充验证隔壁新增的 target piece snap-zone 工作也已接线：`M01GreyboxDrag` 先按同形状隐形标准件槽吸附，`M01GreyboxBootstrap` 处理 `snap_fragment_to_target_piece` 并保留 evidence staging 验证路径。验证：`npm test -- tests/m01PreviewRefreshScript.test.ts` 成功（2 tests）；`node --check scripts/m01-preview-refresh.mjs` 成功；`npm run typecheck` 成功；`npm test` 成功（18 files / 158 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；刷新 Cocos scripts/runtime-sprites 后 `npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、`artSpriteCount = 18`、`staticArtCount = 2`、console/page error 为空。
- 2026-05-04 M01 porcelain fragment style pass：用户指出当前拼片贴图颜色太厚实，希望改成灰白色瓷片质感，同时保留手绘水彩风格；随后补充“他们的色调应该一致”。处理：保留现有 112×112 runtime sprite 文件名、透明 alpha、Cocos `.meta` 与玩法映射，只重绘 `runtime-sprites/fragments/` 9 张彩色候选拼片和 `runtime-sprites/hidden-fragments/` 3 张隐藏拼片；新材质统一为同一套灰白瓷片底、低对比水彩晕染、轻微裂纹、手绘 graphite 外轮廓，不再按红 / 蓝 / 黄保留冷暖 undertone。同步刷新 `parts/hidden_fragment_*.png` 审稿导出，新增审稿 contact sheet `docs/design/generated-m01-art-slices/m01-fragment-porcelain-watercolor-contact-20260504.png`；`tests/cocos/M01GreyboxArt.test.ts` 新增像素级回归，要求 isolated fragment 平均亮度保持在灰白瓷片范围、平均通道差低于厚色块阈值，并按形状组检查不同隐藏色拼片之间的明度 / 色差范围一致。验证：`npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（16 tests）；`npm run typecheck` 成功；`npm test` 成功（18 files / 154 tests）；`git diff --check` 成功；刷新 Cocos runtime-sprites 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`artSpriteCount = 18`、`staticArtCount = 2`、`realInput.usedFallback = false`、console/page error 为空，clean QA 截图见 `temp/m01-art-preview-clean-qa.png`。
- 2026-05-04 M01 Lemmy-style pink flashlight：用户希望手电从当前卡通版进一步贴近莱米参考图的手绘质感。处理：用内置 image generation 生成单个三键手电，参考莱米图的铅笔晃线、低饱和水彩、短胖玩具比例；第一版红色气质成立但红色偏跳，第二版灰赭更贴合大螺母但用户判断“不如红色好看”，最终从红色版本地调色为灰粉 / 豆沙粉，保留原始笔触和三键识别。运行时覆盖 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-single-flashlight-tool.png`，同步 `docs/design/generated-m01-art-slices/m01-single-flashlight-tool-runtime-fixed.png` 与 `parts/flashlight_single_three_buttons.png/.psd`；保留红色源图 `source/m01-handdrawn-flashlight-lemmy-style-v1-source.png` 和粉色审稿导出 `parts/flashlight_single_three_buttons-lemmy-pink-v3.png`。验证：`npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（15 tests）；`npm run typecheck` 成功；`git diff --check` 成功；Cocos MCP 刷新 art surfaces 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`hasSingleFlashlightTool = true`、`realInput.usedFallback = false`、console/page error 为空，clean QA 截图见 `temp/m01-art-preview-clean-qa.png`。
- 2026-05-04 M01 takeover completion：接手隔壁线程未提交状态后，复现到两条红测：scaffold 仍断言旧 `view.validationColor ?? view.observedColor` 文本，目标图 `260×145` 与源图 1677:938 比例差刚好超过测试容差。修复：目标提示层改为 `260×145.31`，测试断言更新到当前 `validationFlashVisible` 闪烁实现；随后刷新 Cocos MCP scripts/config 并复跑浏览器 smoke。验证：`npm run typecheck` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01GreyboxSession.test.ts tests/cocosProjectScaffold.test.ts` 成功（83 tests）；`npm test` 成功（18 files / 153 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、`artSpriteCount = 18`、`staticArtCount = 2`、`hasFragmentFloor = false`、`validationColorDuringFlash = red`、`hasValidationColorAfterFlash = false`、ToolCard 标题“分类与归纳”、console/page error 为空。
- 2026-05-04 M01 art-preview link fix：用户指出上一条给出的 preview 链接打开后没有美术贴图。Root cause：`smoke:m01-preview:input -- --enable-art-preview` 会在 Playwright 里临时设置 `bootstrap.enableArtPreview = true` 并重渲染，但普通 `?scene=...` 链接不会打开该开关，导致用户实际看到灰盒。修复：`M01GreyboxBootstrap` 新增 URL 参数入口 `m01ArtPreview=1`，场景启动时执行 `this.enableArtPreview = this.enableArtPreview || shouldEnableM01ArtPreviewFromUrl()`；正确人工预览链接为 `http://127.0.0.1:7456/?scene=a2135734-fc11-4a0e-926d-40bc2301a752&m01ArtPreview=1`。刷新 Cocos scripts 后真实链接验证成功：`enableArtPreview = true`、`artSpriteCount = 18`、`staticArtCount = 2`、`hasTargetReferenceCard = true`、`hasSingleFlashlightTool = true`、`hasFragmentFloor = false`、console/page error 为空；截图见 `temp/m01-art-preview-link-qa.png`。验证：先新增 scaffold 回归测试并确认红灯，随后实现；`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（56 tests），`npm run typecheck` 成功，`npm test` 成功（18 files / 150 tests），`git diff --check` 成功。
- 2026-05-04 M01 hidden evidence snap-zone takeover：接手隔壁线程未提交改动后，确认本轮意图是让 overlap evidence 节点在 art-preview 中继续作为吸附区存在，但不再绘制 evidence marker 贴片，避免工作区出现过多可见证据装饰抢戏。`buildM01GreyboxTokenArtPlan()` 现在过滤 `token.kind === "evidence"`，fragment / gear art 仍照常进入 token art plan；`M01GreyboxBootstrap` 仍保留 `for (const evidence of layout.evidence)` 与 `addShapeNode()` 路径，保证隐藏 snap zone 交互不丢。接手时发现 `smoke:m01-preview:input` 的真实输入判定会在整段拖拽结束后读取 `observedColor`，与 2 秒观察显色窗口冲突，已改为检查手电照射当下的 debug snapshot。验证：`npm run typecheck` 成功，`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功（55 tests），`npm test` 成功（18 files / 149 tests），`git diff --check` 成功，`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、`artSpriteCount = 18`、`staticArtCount = 2`、`hasFragmentFloor = false`、ToolCard 标题“分类与归纳”、console/page error 为空；clean QA 截图刷新到 `temp/m01-art-preview-clean-qa.png`。
- 2026-05-04 M01 cartoon flashlight pass：用户要求把手电筒形象塑造得更卡通。修复：保留“一个实体手电 + 红黄蓝三个按钮”的交互口径，但将 `m01-single-flashlight-tool-runtime-fixed.png`、runtime surface `m01-single-flashlight-tool.png`、PSD parts `flashlight_single_three_buttons.*` 改为更胖短的童话道具造型：圆头大镜片、软边身体、大颗按键、手绘晃线和低饱和水彩材质；`m01-single-flashlight-with-light-buttons-candidate.svg` 新增 `cartoon-toy-silhouette`，prompt 明确锁定 `cartoon toy-like proportions`，防止后续回到写实金属管方向。刷新 Cocos preview 后，唯一当前预览见 `temp/m01-art-preview-cartoon-flashlight-current-20260504-110434.png`；验证：`npm run typecheck` 成功，`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功，`npm test` 成功（18 files / 149 tests），`node scripts/m01-preview-smoke.mjs --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、console/page error 为空。
- 2026-05-04 M01 complete rich-color gear platform correction：用户指出 full-outline 平台贴片外轮廓仍有残破，且上一轮 4/22 直切版不是定稿的色彩浓郁版本。修复：运行时 `m01-overlap-memory-gear.png` 改用完整螺母造型母版重新透明化，并提升到更接近 rich-color 的灰褐水彩强度；`M01GreyboxArt` 的 `gearStar.sourceFile` 锁到 `docs/design/generated-m01-art-slices/m01-overlap-memory-gear-full-outline-rich-color-runtime.png`，避免回到旧 direct-crop / broken-alpha 文件；`M01GreyboxBootstrap` 在 art-preview 普通态隐藏 `board` 灰盒 underlay，移除平台内圆额外叠线。刷新 Cocos surfaces / scripts 后，唯一当前预览见 `temp/m01-art-preview-complete-gear-current-20260504-100844.png`；验证：`npm run typecheck` 成功，`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功，`npm test` 成功（18 files / 149 tests），`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、`artSpriteCount = 18`、`staticArtCount = 2`、console/page error 为空。
- 2026-05-04 M01 full-outline gear platform repair：用户指出大螺母 / 齿轮平台贴片切片时没有保留完整外轮廓，导致运行时造型很难看。修复：从 2026-04-22 锚点图重新裁切完整 gear outer silhouette，保留外齿与底座，去除方形纸底，并把旧九宫格中心改为空水彩拼接盘；新母版保存为 `docs/design/generated-m01-art-slices/m01-overlap-memory-gear-full-outline-runtime.png`，runtime surface 覆盖 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-overlap-memory-gear.png`，`M01GreyboxArt` 的 `gearStar.sourceFile` 已切到 full-outline 母版，避免后续误回旧 inner-circle crop。刷新 Cocos surfaces 后，`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，唯一预览截图见 `temp/m01-art-preview-full-outline-current-20260504-092816.png`；验证：`npm run typecheck` 成功，`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功，`npm test` 成功（18 files / 149 tests），`git diff --check` 成功。
- 2026-05-04 M01 4/22 watercolor sticker fast-mode implementation：用户明确指出“过程中一直要求加上 2026-04-22 锚点图的水彩贴片，但没有实现”。Fast-mode 处理：直接以 `/Users/danmac/Desktop/截屏2026-04-22 09.43.48.png`（与 `docs/design/style-references/2026-04-22-unified-handdrawn-style-anchor.png` 同尺寸同源）为纹理来源，覆盖生成 `runtime-sprites/fragments/` 9 张红/蓝/黄圆三角六边形、`runtime-sprites/hidden-fragments/` 3 张灰白隐藏碎片、`runtime-sprites/evidence-markers/` 4 张局部交叠 evidence marker；新贴片保留参考图的低饱和水彩沉积、破墨线、纸面斑驳和边缘掉白。审稿 contact sheet 输出到 `temp/m01-watercolor-sticker-contact.png`，最新 clean QA 截图输出到 `temp/m01-art-preview-clean-qa.png`。验证：刷新 Cocos runtime-sprites 资源成功；`npm run typecheck` 成功；`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功；`npm test` 成功（18 files / 149 tests）；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`artSpriteCount = 18`、`staticArtCount = 2`、`realInput.usedFallback = false`、console/page error 为空。下一步若继续 polish，应优先调底部碎片池位置/尺度，而不是再回到工程占位贴片。
- 2026-05-04 M01 art-preview sticker visibility repair：用户复看指出截图里“几乎没有贴片显现”。Root cause 是 art-preview 只渲染 target card / single flashlight 两个 static layer，工作区 `layout.evidence` 的 evidence marker sprite 没有实例化；同时 fragment / evidence 普通态仍强制绘制灰盒 underlay，导致已加载的贴片被线框和淡填充压回工程占位感。修复：`M01GreyboxBootstrap` 现在渲染 `layout.evidence` 工作区节点，art-preview 下 fragment / evidence 普通态优先显示 sprite，只在交互态或 fallback 时叠 underlay；隐藏碎片贴片显示尺寸调到 `56×56`，普通态使用更实的纸色 tint，手电观察 / 失败验证的颜色显现仍保留 underlay。验证：`npm run typecheck` 成功；`npm test` 成功（18 files / 149 tests）；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`artSpriteCount = 18`、`staticArtCount = 2`、`realInput.usedFallback = false`、`consoleMessages/pageErrors = []`，clean QA 截图见 `temp/m01-art-preview-clean-qa.png`。下一步仍是视觉 polish：底部碎片池和平台脚座间距、目标圆框裁切、ToolCard 完成态遮挡和整体构图。
- 2026-04-29 spec 已更新：M01 从“过滤器筛选 + 九宫格归类”改为“光谱探测 + 交叠证据拼接”。以下旧灰盒进度代表已验证技术能力，不再代表最终 M01 玩法形态。
- 2026-04-30 已完成新版 M01 plan 的 Task 1：在 `M01MemoryGearController` 中新增童话颜料混色 domain helper（`blendM01PigmentColors` / `revealM01FragmentColor`），覆盖红黄蓝同色与两两混合规则。
- 2026-04-30 已完成新版 M01 plan 的 Task 2：真实 `m01-memory-gear.json` 已从旧“过滤器 + 九宫格槽位”切换为 13 个候选灰白碎片、3 个手电色、4 个两片交叠证据、`solution_defined` 使用碎片派生和 `overlap_evidence_reconstructed` 胜利目标；已提交的旧灰盒 layout/session 测试改用 `m01LegacySortConfig` 继续保护旧 runtime 壳子，不再要求真实 M01 JSON 具备旧滤镜/槽位结构。
- 2026-04-30 已完成新版 M01 plan 的 Task 3：`M01MemoryGearController` 现在支持手电显色、证据 pair staging、失败后同 evidence replacement、底光整体验证、错误候选 2 秒闪光状态、正确候选常亮并解锁 ToolCard；旧 sort controller 路径仍保留给 legacy 灰盒壳子使用。
- 2026-04-30 已完成新版 M01 plan 的 Task 4：`M01GreyboxLayout` 现在为真实 M01 配置生成三色手电、灰白候选碎片、局部交叠证据节点和拼接盘；真实配置不再暴露旧九宫格 slots，legacy sort fixture 仍可返回旧 filters/slots 以保护尚未迁移的灰盒壳子测试。
- 2026-04-30 已完成新版 M01 plan 的 Task 5：`M01GreyboxDrag` 现在支持选择手电、按局部形状证据弱磁吸碎片、形状不匹配或远离证据时自由放置；旧 filter/slot drop action 暂时保留给 legacy 灰盒壳子，后续 Task 6/7 迁移 session/bootstrap。
- 2026-04-30 已完成新版 M01 plan 的 Task 6：`M01GreyboxSession` 现在支持选择手电、显色观察、点击拾起/自由放置、弱磁吸提示、证据 pair 暂存/替换、底光整体验证和成功后 ToolCard 解锁；旧 filter/slot session API 保留给 legacy 灰盒壳子。
- 2026-04-30 Task 6 review 修复：新版 session API 对 legacy config 改为返回拒绝而不是访问缺失数组崩溃；真实 overlap-evidence 配置的提示流改为手电 -> 候选碎片 -> 形状兼容证据。
- 2026-04-30 已完成新版 M01 plan 的 Task 7：`M01GreyboxBootstrap` 现在会渲染新版拼接盘、证据点、候选碎片和三色手电；拖拽手电会进入 `selectFlashlight()`，落在碎片上会尝试 `revealFragment()`；碎片可自由放置，形状匹配证据时弱磁吸，第二个碎片吸上同一证据后自动 `submitEvidencePair()`，全部证据 staged 后自动 `validateCandidateStructure()`，不新增单独校验按钮。
- 2026-04-30 Task 7 review 修复：证据点现在使用紫 / 绿 / 橘融合色，而不是灰色 fallback；`arc_lens` / `notch_lens` / `crescent_overlap` / `branch_lens` 有独立简笔局部形状；新版 hint 可高亮手电和证据点；碎片支持点击拾起并点击拼接盘任意位置放下，仍保留拖拽路径。
- 2026-04-30 Task 10 preview 修复：重启 Cocos preview 后发现 `/` 默认加载 `current_scene` 会进入空场景，改用 `?scene=a2135734-fc11-4a0e-926d-40bc2301a752` 可直接加载 M01；交互 smoke 发现“手持第二片点击已吸附证据点”会误拾取第一片，现已让 held fragment 点击优先放置，并修正弱磁吸后的 `tokenPositions` 缓存。
- 2026-05-01 表现层 polish：`M01GreyboxBootstrap` 已新增手电光束、拼接盘底光（off / flash_then_off / steady_on）、底光状态下 evidence 局部点亮，以及无文字的机械城式简笔提示纸条；当前只补灰盒表现，不改变证据 pair staging 或胜利判定规则。
- 2026-05-01 表现层 polish review 修复：底光失败闪烁现在会按 `validationLightSeconds` 设置一次视觉复位 timeout，超时后重新读取 controller completion state 并重绘；组件销毁时清理 timeout，避免错误候选后画面一直停在亮起状态。
- 2026-05-01 M01 机制口径修正：根据用户澄清，候选碎片外形收敛为圆 / 三角 / 六边形三类固定形状，同形状保留不同隐藏底色以制造推理必要性；真实配置改为底部碎片池，目标图显示系统预生成的两两交叠证据（含局部轮廓、目标融合色、相对位置、生成面积/偏移），玩家要反推每个证据对应的具体形状与底色碎片。弱吸附仍受固定形状组合与目标交叠区域约束，最终验证要求每个 evidence 的 staged pair 精确匹配该 evidence 的 `solution.fragmentIds`，避免只凑齐全局碎片集合也过关。
- 2026-05-01 M01 手感口径修正：碎片选中后会跟随未离屏的鼠标 / 手指移动，再次点击屏幕任意位置放下，保留屏幕任意位置自由分类；三色手电改为可游动光束，移动光束时会持续扫描底部碎片并触发短暂显色；全局触摸 move/end/cancel 已接入同一套 held-fragment 跟随逻辑，cancel 走取消路径而不是误放置；鼠标滚轮可调节光束 reach。光束只用于扫描底部碎片，不照拼接盘。验证：`npm test -- tests/levels/stage1/M01MemoryGearController.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxSession.test.ts tests/cocosProjectScaffold.test.ts` 成功（5 个测试文件 / 81 个测试）；`npm run typecheck` 成功；`npm test` 成功（15 个测试文件 / 121 个测试）。
- 2026-05-02 browser smoke harness 修复：为 ralphex / 后续 Codex 子进程补上 repo-local 浏览器驱动入口：新增 `playwright-core` devDependency、`scripts/m01-preview-smoke.mjs` 和 `npm run smoke:m01-preview`。脚本自动寻找 `M01_SMOKE_CHROME_PATH`、Playwright 缓存 Chromium 或 `/Applications/Google Chrome.app`，进入 M01 preview 场景，校验运行时节点与当前 `m01-memory-gear.json` 一致，再触发错误 complete candidate 验证失败底光显色窗口。实测普通沙盒会被 macOS Mach port 权限挡住浏览器启动，需要提升权限运行浏览器；首次运行发现 Cocos preview stale bundle 缺少固定形状节点，已用 MCP 刷新 `db://assets/scripts` 与 `db://assets/resources/configs/stage1` 并 soft reload scene 修复。复跑 `npm run smoke:m01-preview` 成功，截图见 `temp/m01-preview-failed-validation-smoke.png`，结果为错误候选 `wrong_blend_color`、失败底光 `flash_then_off`、2 秒后 `validationColor` 熄灭且 console/page error = 0。
- 2026-05-02 M01 real-input preview smoke：`scripts/m01-preview-smoke.mjs` 新增 `scripts/m01-preview-smoke-helpers.mjs` 规划真实 M01 输入坐标，并先尝试通过 preview canvas 的浏览器输入事件覆盖一条非 API 交互链路：选红手电 -> 观察 `fragment_circle_blue_1` 得到 `observedColor = purple` -> 自由放置 `fragment_circle_yellow_1` -> 将 `fragment_circle_red_1` 与 `fragment_triangle_blue_1` staged 到 `evidence_purple_upper_left`。当前 headless Cocos preview 对 canvas 注入的 browser input 事件不响应，默认 `npm run smoke:m01-preview` 会把该 blocker 明确记入 JSON 输出，并回退到 bootstrap-level 等价 drop path 继续提供可重复验证；严格 gate `npm run smoke:m01-preview:input` 已新增，带 `--require-browser-input`，若 `realInput.usedFallback = true` 会直接失败，防止真实输入坏掉时误报绿灯。最新默认 smoke 复跑成功：`realInput.usedFallback = true`，`realInput.blocker` 已记录，`activeFlashlightId = flashlight_red`，`observedColor = purple`，`fragment_circle_yellow_1` 自由放置到 `(0, -146)`，`evidence_purple_upper_left` 成功 staged，随后错误候选仍返回 `wrong_blend_color`、底光 `flash_then_off`、`validationColorDuringFlash = red`、2 秒后 `hasValidationColorAfterFlash = false`，`consoleMessages/pageErrors = 0`，截图仍输出到 `temp/m01-preview-failed-validation-smoke.png`。验证：`node --check scripts/m01-preview-smoke.mjs`、`node --check scripts/m01-preview-smoke-helpers.mjs`、`npm test -- tests/m01PreviewSmokeScript.test.ts`、`npm run typecheck`、`npm test`、`git diff --check` 成功；严格 input smoke 在当前 headless Cocos 输入 blocker 修复前预期会失败。
- 2026-05-02 M01 strict held-flashlight lifecycle repair：本轮 strict preview fallback 的真实 root cause 落在 `M01GreyboxBootstrap` 的输入状态机，而不是 domain 规则。具体有三层：1) 手电 shine 结束后 `heldFlashlightId` 仍保持激活，导致鼠标在按下碎片前先移动到碎片起点时，会把手电拖到光标下方并把后续 fragment drag 误判成 flashlight 交互；2) fragment press 本身没有统一中止 held flashlight follow / beam gesture，在 root pointer press 与 token drag 两条入口上都可能继承旧手电状态；3) headless real-input completion 路径把 node-level touch start 和 global mouse move/up 混用，drag pointer id 需要在离开 token node 后继续沿用 active drag session，否则空白区 free placement 会脆弱。修复：新增 `suspendHeldFlashlightInteraction()` 与 `releaseHeldFlashlightAfterBeamGesture()`，在 fragment press、beam gesture end、flashlight drag-reveal completion 三处释放 held flashlight；新增 `pointerIdForActiveDragEvent()` 让全局 mouse move/up 复用当前 drag session 的 pointer id；保留 `activeFlashlightId` 与最后一次 `beamTarget` 供 smoke 断言，但在 fragment 交互前清掉 follow / lit / anchor。结果：`npm run smoke:m01-preview:input` 现在真实通过，`realInput.usedFallback = false`，`fragment_circle_yellow_1` 会自由放置到 `(0, -146)`，第一组 staged fragments 仍保持 partial-overlap pose，completion `evidenceCount = 4`、`areAllEvidenceStaged = true`、`bottomLight = steady_on`、ToolCard 标题“分类与归纳”、`consoleMessages/pageErrors = []`。本轮验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（36 tests），`npm run typecheck` 成功，`npm test` 成功（18 files / 139 tests），`git diff --check` 成功，`npm run smoke:m01-preview-refresh` 三个 MCP 步骤均 200，`npm run smoke:m01-preview:input` 返回 `ok: true`。
- 2026-05-02 M01 strict input smoke hardening：重新复盘 strict preview fallback 后，最终 root cause 定位为两层：1) `ObservedResetScheduler` 默认保存浏览器 `setTimeout` 后以对象方法形式调用，在 Cocos preview 里抛 `Illegal invocation`，导致手电显色后的 drag cleanup 中断并把 `activeDragNode` 卡在 flashlight；2) fragment 视觉尺寸与输入命中面同为 `48x48`，对 headless real-input smoke 和玩家触控都偏脆弱。修复：`ObservedResetScheduler` 默认用 wrapper 调用 `setTimeout/clearTimeout`，`M01GreyboxBootstrap.endTokenDrag()` 在 inactive dragState 下也清 active drag，fragment 输入 hit area 扩到 `FRAGMENT_INPUT_HIT_SIZE = 64` 但不改变视觉尺寸；smoke 脚本改用 Playwright mouse-driven real input，并保留逐步 debug snapshot 和默认 fallback。刷新 Cocos MCP 后，`npm run smoke:m01-preview:input` 成功，输出 `realInput.usedFallback = false`、`observedColor = purple`、`fragment_circle_yellow_1` 自由放置到 `(0, -146)`、`evidence_purple_upper_left` staged、`consoleMessages/pageErrors = []`。
- 2026-05-02 M01 full real-input completion smoke：`scripts/m01-preview-smoke-helpers.mjs` 现在从真实 config 派生全部 `completionEvidence`，`scripts/m01-preview-smoke.mjs` 在保留失败验证 smoke 后会重载干净 M01 preview，用真实 Playwright mouse input 依次完成全部 4 个 evidence pair，并断言 `areAllEvidenceStaged = true`、`completionState.bottomLight = steady_on`、`completionState.completed = true`、ToolCard 标题为“分类与归纳”、`consoleMessages/pageErrors = []`。最新 `npm run smoke:m01-preview:input` 成功，输出 `completion.evidenceCount = 4`、`usedFragmentCount = 8`、截图 `temp/m01-preview-completion-smoke.png`。
- 2026-05-02 M01 completion presentation polish：复看 `temp/m01-preview-completion-smoke.png` 后发现完成态仍保留活动红色手电光束，压过拼接盘和 ToolCard；`M01GreyboxBootstrap` 现在在 ToolCard 渲染前清空 `activeFlashlightId` / `activeFlashlightColor` / `flashlightBeamTarget`、重绘光束，并隐藏 `M01HintButton`。严格真实输入 smoke 增补完成态断言：活动光束目标清空、提示按钮不可见、ToolCard 标题仍为“分类与归纳”。最新验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（2 个测试文件 / 30 个测试）；`npm run typecheck` 成功；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功（三个 Cocos MCP 步骤均 200）；`npm run smoke:m01-preview:input` 成功，输出 `realInput.usedFallback = false`、`completion.evidenceCount = 4`、`completion.completionState.bottomLight = steady_on`、`completion.hintButtonVisible = false`、`consoleMessages/pageErrors = []`，新完成截图仍为 `temp/m01-preview-completion-smoke.png`。
- 2026-05-02 M01 partial-overlap snap fix：根据用户复看 preview 的反馈，修正“多个碎片拼成一个大图形，碎片之间只两两局部交叠”的执行口径。根因是 runtime 把每个 evidence 作为单一吸附点，导致同一 evidence 的两片碎片都会 snap 到同一中心，视觉上像大面积堆叠。`M01GreyboxLayout` 现在从 `generatedOverlap.offset` 派生每个 solution fragment 的独立 snap pose，并设置最小中心距离；`M01GreyboxBootstrap.snapNodeToEvidence()` 使用该 pose 而不是 evidence 中心点。严格真实输入 smoke 也新增断言，要求 staged 两片距离至少 32px，防止回退成一堆。最新 `npm run smoke:m01-preview:input` 成功，第一组 staged positions 分别为 `(-150.86, 102.26)` 与 `(-121.14, 85.74)`，`realInput.usedFallback = false`、`completion.completionState.bottomLight = steady_on`、`consoleMessages/pageErrors = []`。
- 2026-05-02 M01 manual QA + art polish plan：按用户要求对当前 M01 进行人工试玩 QA 风格复核，并保存后续美术计划到 `docs/plans/2026-05-02-m01-manual-qa-art-polish-plan.md`。刷新 Cocos preview 后，`npm run smoke:m01-preview:input` 通过，截图证据为 `temp/m01-preview-completion-smoke.png` 与 `temp/m01-preview-failed-validation-smoke.png`。QA 结论：核心玩法可作为 greybox baseline，但 human readability 只有 6/10，art readiness 只有 3/10。主要待修：目标证据必须从拼接盘中移出，作为旁置目标图显示；中央拼盘不能画目标图，只能显示玩家实际放置的碎片、底光和不可见的吸附逻辑；可见手电光束仍扫过拼接盘区域；截图带 Cocos preview/debug overlay；底部缺少“放在地上”的碎片工作面；旧 v3 art prompt 仍残留九槽 sorter 假设，不能直接复用。计划建议先做 clean QA capture 与 side-target/assembly-board 分离，再生成/导入新 art。
- 2026-05-02 M01 manual QA art polish Task 1-3：按 `docs/plans/2026-05-02-m01-manual-qa-art-polish-plan.md` 完成前三项 readability 修复。`scripts/m01-preview-smoke.mjs` 新增 `--capture-clean-qa`，输出 canvas-only 干净截图 `temp/m01-preview-clean-qa.png`；`M01GreyboxLayout` 新增 `referenceEvidence`，保留 `layout.evidence` 作为拼盘内隐藏 snap zone，把可见目标证据移到拼盘右侧；`M01GreyboxBootstrap` 不再渲染中央目标证据点，并用 `FRAGMENT_FLOOR` / `clipFlashlightBeamToFragmentFloor()` 将可见手电光束限制在底部碎片区。验证：`npm test -- tests/m01PreviewSmokeScript.test.ts` 成功（4 tests）；`npm test -- tests/cocos/M01GreyboxLayout.test.ts` 成功（7 tests）；`npm test -- tests/cocosProjectScaffold.test.ts` 成功（28 tests）；`npm run typecheck` 成功；`git diff --check` 成功；`npm run smoke:m01-preview-refresh` 成功；`npm run smoke:m01-preview:input -- --capture-clean-qa` 成功，输出 `realInput.usedFallback = false`、`completion.completionState.bottomLight = steady_on`、`consoleMessages/pageErrors = []`。
- 2026-05-02 M01 manual QA art polish Task 4：新增 `docs/design/generated-m01-art-slices/m01-overlap-runtime-art-polish-qa-and-prompt.md`，替换旧九槽 sorter prompt 假设，明确当前 M01 需要的 empty memory gear / assembly plate、旁置 target overlap evidence diagram、13 个圆 / 三角 / 六边形灰白隐藏碎片、3 支手持三色手电、底部碎片工作面、底光状态 overlay、ToolCard preview frame。验收清单已覆盖：不泄露最终答案轮廓、默认碎片不露隐藏本色、target evidence 只显示两片局部交叠、不画三层交叠、支持透明或 chroma-key 提取、并在 `960x640` gameplay scale 可读。验证：`git diff --check` 成功。
- 2026-05-02 M01 失败验证纠错回归覆盖：阅读现有 tests 后，优先补的是“完整错误候选验证失败后，玩家立即替换错 pair 并再次提交”的闭环，因为它同时穿过 `wrong_blend_color` 闪烁、`validationColor` 可见窗口、同 evidence pair replacement、以及成功重验四个脆弱状态切换，而此前 coverage 只分别覆盖了错误闪烁或局部纠错。`tests/cocos/M01GreyboxSession.test.ts` 现新增回归：先触发错误 complete candidate 的 `flash_then_off`，再用正确 pair 覆盖 `evidence_purple_upper_left`，断言底光立即回到 `off`、旧 `validationColor` 立刻清掉、随后重新 `validateCandidateStructure()` 成功进入 `steady_on`。
- 2026-05-02 M01 overlap art checkpoint：`M01GreyboxArt` 现已为真实 overlap-evidence M01 暴露 3 张 shape-only hidden fragment runtime sprite（`hidden_circle` / `hidden_triangle` / `hidden_hexagon`）和 4 张 evidence marker runtime sprite（按 `targetBlendColor + generatedOverlap.sourceShapes` 映射）；`M01GreyboxLayout` 会把 `generatedOverlap.sourceShapes` 保留为 `source-shape:*` tags，`buildM01GreyboxTokenArtPlan()` 则会把 evidence token 一并纳入非交互 art sprite 计划。新增资源位于 `assets/resources/art/stage1-m01/runtime-sprites/hidden-fragments/` 与 `.../evidence-markers/`，并附带 `.meta`。当前范围仍只做 first-pass runtime art，不触碰 legacy filter/tray sorter 的新模式隔离边界。
- 2026-05-02 M01 overlap art preview inventory Task 5：已补齐当前 overlap-mode first-pass art inventory 的缺口。`M01GreyboxArt` 现在额外声明 3 张手持手电 runtime sprite（`flashlight_red` / `flashlight_yellow` / `flashlight_blue`）、1 张 overlap memory gear surface、1 张底部 fragment floor surface、以及 1 张 ToolCard preview frame；真实 overlap layout 的 token art 计划现已把 flashlights 一并纳入，overlap 静态 art 计划会在 preview toggle 下加载底部 floor surface，`M01GreyboxBootstrap` 则会在完成态 preview 中消费 ToolCard frame 资源。新增占位资源位于 `assets/resources/art/stage1-m01/runtime-sprites/flashlights/` 与 `.../surfaces/`，目前是从已接受透明候选复制出来的最小 placeholder，不改变默认 `enableArtPreview=false` 的灰盒路径，也不触碰交互命中或谜题规则。验证：`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功（2 个测试文件 / 44 个测试），`npm run typecheck` 成功，`npm test` 成功（18 个测试文件 / 137 个测试），`git diff --check` 成功，`npm run smoke:m01-preview-refresh` 成功。该 Task 5 执行记录已归档到 `docs/plans/completed/2026-05-02-m01-art-preview-task5-ralphex.md`；后续 strict input fallback 风险已在后续输入修复 checkpoint 中解除。
- 2026-05-02 M01 art-preview visual QA checkpoint：`scripts/m01-preview-smoke.mjs` 新增 `--enable-art-preview`，可在不改变 `M01GreyboxBootstrap.enableArtPreview=false` 默认值的前提下，于 smoke runtime 临时打开 art-preview 并重渲染；`--capture-clean-qa` 在该模式下输出 `temp/m01-art-preview-clean-qa.png`。刷新 Cocos preview 后，`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、`artPreview.enabled = true`、`artPreview.artSpriteCount = 21`、`artPreview.staticArtCount = 1`、`artPreview.hasFragmentFloor = true`、completion `bottomLight = steady_on`、ToolCard 标题“分类与归纳”、console/page error = 0。视觉结论：art-preview 管线与真实输入路径已同时可用，但当前 first-pass placeholder 仍偏工程占位，底部工作面、手电道具、ToolCard frame 与 target evidence marker 需要下一轮视觉资产 polish。
- 2026-05-02 M01 target pattern clarification：用户复看后指出上一轮仍把最终目标做成了零落的两两拼接，正确口径应是“拼成一个大的完整图案”。Root cause 是 `buildReferenceEvidenceNode()` 为了把目标图移到右侧，按 index 把 evidence 重新排成 2x2，丢掉了原始 `evidence.position` 的整体结构，也没有为右侧目标图提供完整轮廓底图。修复：`M01GreyboxLayout` 现在按原始 evidence bounds 计算中心点，把所有 target evidence 整体缩放/平移到右侧参考区，保留各局部 overlap 之间的相对距离和方向；新增 `referencePattern` / `drawReferencePattern()`，在右侧目标图下面绘制完整图案轮廓；config hint 和 art prompt 也改为“完整目标图案内的局部交叠线索”。验证：新增 layout 测试先红后绿，`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/core/PuzzleConfig.test.ts tests/m01PreviewSmokeHelpers.test.ts tests/cocosProjectScaffold.test.ts` 成功（4 个测试文件 / 43 个测试），`jq empty assets/resources/configs/stage1/m01-memory-gear.json` 成功；刷新 Cocos 后 `npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、completion `bottomLight = steady_on`、console/page error = 0，截图 `temp/m01-art-preview-clean-qa.png` 中右侧目标图已显示完整轮廓底图。
- 2026-05-02 M01 console cockpit style reference：用户提供 `console.png` 作为下一轮美术资产画风参考。该图已入库到 `docs/design/style-references/2026-05-02-console-cockpit-style-reference.png`，并同步副本到 `assets/art/style-references/console-cockpit-style-reference.png`；`docs/design/style-references/README.md` 标记其为 M01 runtime-art generation / paintover pass 的局部主动参考，不替换全项目四张全局参考。`docs/design/generated-m01-art-slices/m01-overlap-runtime-art-polish-qa-and-prompt.md` 已更新为深色旧铜控制台方向：深蓝灰夜窗、石墨/旧铜金属、暖赭边光、曲面控制台框、螺丝/面板缝、暗色工作台与清晰可读的 separated runtime sprites。
- 2026-05-02 M01 rejected console sheet v1：一次按 console cockpit 参考生成的 `m01-console-cockpit-runtime-sheet-v1.png` 被用户否决，因为目标图案画成了不规则裂片 / 拼图块，而不是由圆、三角、六边形这些玩家实际碎片组成的大图案。该候选未提交、未切片、未导入 runtime 资源；prompt 已加硬性拒收条件：target pattern 必须由 circle / triangle / hexagon 组成，禁止 torn-map / puzzle-piece / rock-shard / irregular cracked silhouettes。
- 2026-05-02 M01 target art iteration correction：用户继续指出第二版目标图案仍过复杂，且两片碎片交叠面积太小、颜色读不清；更重要的是颜色应只出现在真实交叠区域内，而不是像一束彩色灯光照在碎片表面。下一轮美术生成必须把目标图压简为少量大圆 / 三角 / 六边形构成的完整图案，保证每个 overlap 色块面积足够大，并把 muted orange / green / purple 严格裁切在两几何体交集内；任何 glow / halo / spotlight / colored wash 都视为拒收。
- 2026-05-02 M01 canonical shape correction：用户确认方向更接近，但指出目标图里的三角与实际三角碎片大小/形状不一致。下一轮 target art 必须把圆 / 三角 / 六边形当作同一套可复用 fragment 模板来摆放：整体可随参考图缩放，但内部三类几何片的比例、描边、材质和规范形状必须一致；三角必须是规则直边三角，不能变形、拉伸或手绘成不规则符号。
- 2026-05-02 M01 canonical triangle / boolean overlap correction：用户确认整体思路正确，但指出生成图中两个三角大小仍不一致，且下方交叠图形的交集形状不对。下一轮目标图优先复用同一个等边三角模板参与多个 overlap，或确保所有三角都是同尺寸同模板；每个彩色 overlap 必须按对应两形状的真实几何交集裁切，尤其 hexagon + circle 需要同时保留圆弧边界和六边形直边边界，不能画成装饰色块 / 徽章 / 任意 blob。
- 2026-05-02 M01 runtime-standard-piece correction：复查 `M01GreyboxBootstrap.drawTokenShape()` 与 layout test 后确认当前标准碎片不是生成器自定比例，而是所有 fragment 都在同一个 `48x48` token box 中绘制；圆、三角、六边形共享该标准件外框。后续目标图生成/paintover 必须以 runtime standard piece 为准，三角不能被画成独立缩放的等边符号或大小不一的装饰形状。
- 2026-05-02 M01 target geometry guide：新增 `docs/design/generated-m01-art-slices/m01-target-standard-piece-geometry-guide.svg` 作为后续 target reference art 的硬几何模板。该文件用同一套标准件实例画出 2 个圆、2 个同尺寸三角、2 个六边形，并用 SVG clip 表达四个真实 boolean overlap；后续美术生成只允许改材质、线条和浅纸底手绘面板处理，不允许改三角大小、标准件比例或交集形状。
- 2026-05-02 M01 target standard-piece rule accepted / corrected：用户确认“同尺寸标准件 + SVG clip 真实交集”规则可作为后续美术/贴图基线，并进一步澄清目标图可以是两个三角形。正确规则更新为：2 个圆同尺寸，2 个三角同尺寸，2 个六边形同尺寸；彩色部分用 SVG clip 表达真实交集；后续只允许改 2026-04-22 浅纸底手绘齿轮质感、线条和材质，不能改标准件比例和交集形状。
- 2026-05-02 M01 art style re-anchor：用户明确后续美术资料仍按照 2026-04-22 浅纸底手绘齿轮图生成，而不是 2026-05-02 深色 cockpit 参考。当前 M01 prompt 已切回 `docs/design/style-references/2026-04-22-unified-handdrawn-style-anchor.png`：温暖纸底、清晰抖动墨线、低饱和水彩、灰褐齿轮机械、小红 / 蓝灰 / 赭黄碎片点染；几何 guide 继续作为硬形状约束。
- 2026-05-02 M01 handdrawn sheet candidate rejected：按 2026-04-22 手绘锚点生成的一张美术资料候选风格接近，但目标图几何仍不合格：target diagram 内三角与下方标准三角大小/比例不一致，六边形也不像同一套标准件，且部分彩色 overlap 像细长色片 / 装饰块而不是真实两标准件交集。结论：后续不能让图像生成器自由绘制 target geometry；target reference diagram 必须先由确定性 SVG / runtime 标准件几何生成，再只做线条、水彩材质和纸面/面板质感 paintover。
- 2026-05-02 M01 deterministic target reference implementation：按“AI 只做风格、不决定几何”的方向开始改 runtime。`M01GreyboxBootstrap` 现在在存在 `referencePattern` 时不再渲染旧的 4 个 `referenceEvidence` marker，而是由 `drawStandardReferencePattern()` 直接画出 2 个同尺寸圆、2 个同尺寸三角、2 个同尺寸六边形；所有目标件使用 `REFERENCE_STANDARD_PIECE_SIZE = 48`，与底部标准碎片同尺寸体系；彩色证据区域通过确定性的 convex polygon clipping 从两个标准件相交计算出来。`M01GreyboxLayout` 给 reference pattern 增加 `standard_piece_geometry` tag，测试已覆盖该契约。
- 2026-05-02 takeover checkpoint：接手同项目隔壁线程时发现工作树留下一个已写好的红灯测试，要求 M01 runtime 标准碎片几何使用 `Math.sqrt(3)` 并移除旧 `quarterWidth` 六边形近似。随后复查发现该半步只覆盖 runtime，两个 SVG 仍保留旧的非正六边形点列，且三角 SVG 仍是旧高三角；已补充测试卡住 `m01-target-standard-piece-geometry-guide.svg` 与 `m01-target-standard-piece-art-candidate.svg` 的标准件 triangle / hexagon 点列，并将 runtime、geometry guide、art candidate 统一到同一套等边三角和平顶正六边形公式（三角高 `sqrt(3)/2 * 48 = 41.569`，六边形左右半径 48、上下高度 41.569）。`m01-target-standard-piece-art-candidate.svg` 已作为 scoped file 加入 git index，避免测试依赖未跟踪本地文件。保持玩法、solution pairs、snap zones 和 M30 状态不变。验证：`npm test -- tests/cocosProjectScaffold.test.ts` 成功（1 个测试文件 / 32 个测试），`xmllint --noout docs/design/generated-m01-art-slices/m01-target-standard-piece-geometry-guide.svg docs/design/generated-m01-art-slices/m01-target-standard-piece-art-candidate.svg` 成功，`npm run typecheck` 成功，`npm test` 成功（18 个测试文件 / 141 个测试），`git diff --check` 成功。
- 2026-05-02 M01 target art polish checkpoint：按“只改质感、不改几何”的规则推进 `m01-target-standard-piece-art-candidate.svg`。复查发现旧候选把 `feDisplacementMap` 挂在 standard pieces / overlap colors 上，会把直线三角与正六边形视觉上扭曲；已新增测试要求 art candidate 不能对确定性几何层使用 displacement/filter，并必须具备 `paper-tooth`、`piece-wash-overlays`、`ink-linework-overlays` 这些非破坏性质感层。候选 SVG 现改为浅纸底 pattern、低饱和渐变填充、半透明 wash overlay、独立墨线 overlay 和 dry-brush 细节；标准件点列与 clip 交集保持不变。验证：`npm test -- tests/cocosProjectScaffold.test.ts` 成功（1 个测试文件 / 33 个测试），`xmllint --noout docs/design/generated-m01-art-slices/m01-target-standard-piece-art-candidate.svg docs/design/generated-m01-art-slices/m01-target-standard-piece-geometry-guide.svg` 成功，`npm run typecheck` 成功，`git diff --check` 成功；Quick Look 已导出预览 `temp/m01-target-standard-piece-art-candidate.svg.png` 并人工复看边界保持直线/正圆。
- 2026-05-02 M01 target overlap readability correction：用户复看指出“预览里看着不等边”与“好几个碎片没出现交叠”。复核后确认标准件点列本身等边，但 art candidate 的摆放产生了未染色偶然交叠：右侧三角与下方六边形 / 下方圆之间存在可见相交，却没有对应 evidence color，导致交叠规则读起来不一致。已新增 SVG 几何测试解析 `standard-pieces` 的 6 个实例并计算两两交叠面积，要求只有 4 个指定 evidence pair（0:1、2:3、3:4、4:5）大面积相交，其他组合保持低于误触阈值；随后调整 art candidate 与 geometry guide 的右侧链式构图，所有明显交叠都对应彩色 evidence，非 evidence 组合不再形成未染色大交叠。Quick Look 已刷新 `temp/m01-target-standard-piece-art-candidate.svg.png`。
- 2026-05-02 M01 target readable-overlap threshold correction：用户指出“要求大面积相交”不是设计目标。已将 scaffold 测试语义改为 readable evidence overlap：4 个指定 evidence pair 必须落在可读但不过度的面积区间（当前 `450..1200`），非 evidence pair 仍低于误触阈值 30。随后将右侧 triangle / hexagon 链整体微调，绿色 triangle+hex overlap 从过大的约 1666 降到约 942，保留可读线索但不鼓励越叠越大。Quick Look 再次刷新 `temp/m01-target-standard-piece-art-candidate.svg.png`。
- 2026-05-02 M01 target composition complexity correction：用户指出最新预览与上一版差异不明显，且仍有两片完全没交叠，整体组合方式太简单。已将 target art / geometry guide 从 4-pair 构图改为 6 个标准件的连续波形链：circle -> triangle -> hexagon -> circle -> triangle -> hexagon，指定 readable adjacent evidence pair 改为 5 对（0:1、1:2、2:3、3:4、4:5），每一片都至少参与一个 true SVG clip 交叠；非相邻组合仍必须低于误触阈值，避免未上色偶然交叠。Quick Look 已刷新 `temp/m01-target-standard-piece-art-candidate.svg.png`。
- 2026-05-02 M01 target preview cache / topology correction：用户指出聊天中一直看到第一版预览。排查确认 SVG 曾更新，但复用 `temp/m01-target-standard-piece-art-candidate.svg.png` 容易让预览路径缓存混淆；更重要的是前一轮“找回大图案”时把拓扑绕回了早期左右两团旧图。当前改为新的紧凑 knot/emblem 大图案：circle -> triangle -> hexagon -> circle -> triangle -> hexagon 仍保持 2 圆 / 2 三角 / 2 六边形标准件，但关系改为 6 个受控 true clip evidence pair（0:1、0:5、1:2、2:3、2:4、4:5），整体宽度受测约束为 <= 230，避免松散链。为防缓存，人工预览使用唯一临时源文件与输出路径 `temp/m01-target-preview-knot-v1/m01-target-knot-v1-source.svg.png`。
- 2026-05-03 M01 flashlight art direction correction：用户确认进入美术贴图阶段，并指出手电筒应是一个实体道具，而不是红 / 黄 / 蓝三支独立手电。当前 art prompt 已改为“one handheld flashlight tool with three selectable light buttons”；新增单体手电筒 SVG 候选 `docs/design/generated-m01-art-slices/m01-single-flashlight-with-light-buttons-candidate.svg`，包含一个手绘手电本体、镜头、红/黄/蓝三个集成按钮和轻微 beam-start accent。针对“灯光是否需要美术贴片”的确认，新增 `docs/design/generated-m01-art-slices/m01-flashlight-runtime-decals-candidate.svg`：只包含小的 lens glow、三个按钮高亮环和短 beam-mouth accent；完整扫描光束继续由 runtime 动态绘制，避免方向 / 射程 / 颜色 / floor clipping 被贴图写死。预览使用唯一输出路径 `temp/m01-flashlight-tool-v1/m01-single-flashlight-v1-source.svg.png` 与 `temp/m01-flashlight-decals-v1/m01-flashlight-decals-v1-source.svg.png`，避免复用旧缓存图。当前只更新美术资产口径与候选贴图，runtime 仍保留三 flashlight token 的旧交互实现，后续应单独收敛为一个工具 + 三个 mode button 的运行时交互。
- 2026-05-03 M01 runtime sticker sheet candidate：按用户“给所有资产生成贴片”要求，新增总览审稿 SVG `docs/design/generated-m01-art-slices/m01-runtime-sticker-sheet-candidate.svg`。该候选把 assembly plate、target reference card、13 个 hidden fragments、单体三按钮 flashlight、flashlight 小运行态 decals、fragment floor、bottom-light overlays、ToolCard frame 分组放到同一张卡里；完整 flashlight beam 仍不做贴片，继续由 runtime 动态绘制。用户指出 v2 过于工程图、没有吃到 2026-04-22 参考图画风；当前 v3 已改成更接近参考图的大齿轮拼接盘、褐灰水彩块面、破墨线叠线、纸面纹理和低饱和按钮/碎片，预览使用唯一缓存隔离路径 `temp/m01-runtime-sticker-sheet-v3-style-anchor/m01-runtime-sticker-sheet-v3-style-anchor-source.svg.png`。这仍是 visual-review candidate，尚未切成 Cocos runtime PNG / spriteFrame，也未改变 `enableArtPreview=false` 的安全默认路径。
- 2026-05-03 M01 generated watercolor PSD correction：用户进一步指出直接切参考图会不贴合原线条，正确做法是以 2026-04-22 水彩手绘图为风格参考重新生图，并让每个零件成为独立 PSD。当前新增 `docs/design/generated-m01-art-slices/m01-generated-watercolor-psd-assets/`：保留 source sheet `source/m01-watercolor-generated-source-sheet-v1.png/.psd`，并从该 sheet 切出 26 个单件透明 PNG / PSD，覆盖 empty gear assembly plate、target reference card、13 个 hidden fragments、single three-button flashlight、5 个 flashlight decals、fragment floor、3 个 bottom-light overlays、ToolCard frame。预览 contact sheet 为 `m01-generated-watercolor-parts-contact-sheet.png`。这组是当前正式 visual-review 资产候选；旧的直切参考图 PSD 只作为失败探索，不应继续作为美术管线口径。
- 2026-05-03 M01 target-card locked knot correction：用户指出生成 PSD source sheet 里的目标卡结构错误，目标图必须保持 `/Users/danmac/Desktop/M01 new knot target preview.png` 中已经确认过的紧凑 knot pattern，而不是 AI 重新生成的松散拼法。当前已用该 locked preview 裁切覆盖 `m01-generated-watercolor-psd-assets/parts/target_reference_card.png/.psd`，并保存结构源 `source/m01-locked-knot-target-reference-source.png/.psd`。后续 target card 可以做水彩 paintover，但不允许自由重排标准件、改变 knot 拓扑或回到 loose target。
- 2026-05-03 M01 target-card watercolor paintover：用户指出 locked target card 仍只是干净线稿/色块，未贴上同样水彩风格。当前已在不改变 knot geometry 的前提下，对 `parts/target_reference_card.png/.psd` 加入浅纸底、低对比水彩沉积、淡污渍、色块边缘 pigment 和轻微破墨线；paintover 源另存为 `source/m01-locked-knot-target-watercolor-paintover-v2.png/.psd`。目标卡后续仍必须以 locked knot 为结构源，水彩只能作为贴片/paintover 层。
- 2026-05-03 M01 contact sheet stale-cache correction：用户截图显示旧 contact sheet 仍在预览中出现，原因是继续复用 `m01-generated-watercolor-parts-contact-sheet.png` 容易被聊天/预览缓存误读。当前新增版本化预览 `m01-generated-watercolor-parts-contact-sheet-v2-locked-target-watercolor.png/.psd`，顶部带 `v2 locked target watercolor contact sheet` 标记，target card 明确显示 locked knot + watercolor paintover。后续给用户看资产卡时必须使用该版本化路径或新的唯一文件名，不再复用旧 contact sheet 路径。
- 2026-05-03 M01 clue-only watercolor target accepted：用户确认 clue-only 目标卡质感正确。当前已将 `source/m01-locked-knot-target-clue-only-watercolor-imagegen-v1.png/.psd` 作为 target card 当前源，并覆盖 `parts/target_reference_card.png/.psd`；新版资产卡为 `m01-generated-watercolor-parts-contact-sheet-v4-clue-only-target.png/.psd`。规则锁定：目标图不再显示完整圆 / 三角 / 六边形轮廓和浅色 bodies，只保留彩色交叠 clue 及其局部墨线边界；水彩质感沿用用户确认的 imagegen 版本，不回退到本地滤镜版或完整答案线稿版。
- 2026-05-03 M01 target-card runtime art-preview wiring：已把用户确认的 clue-only target card 接入 Cocos art-preview 运行时资源：新增 `assets/resources/art/stage1-m01/runtime-sprites/surfaces/m01-target-reference-card.png/.meta`，其来源锁定为 `docs/design/generated-m01-art-slices/m01-generated-watercolor-psd-assets/parts/target_reference_card.png`。`buildM01GreyboxStaticArtPlan(real M01)` 现在会在 reference pattern 位置加载 `targetReferenceCard` 静态图层，并继续保留 bottom fragment floor；art-preview 下的灰盒 `reference_pattern` 完整答案线稿默认隐藏，只有 target card 资源加载失败或 debug underlay 开启时才回退显示。验证：先新增 `tests/cocos/M01GreyboxArt.test.ts` 红灯，随后 `npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（15 tests），`npm run typecheck` 成功。后续 strict smoke 发现截图仍是普通灰盒，root cause 是 smoke 在 completion 前重新 `page.goto(sceneUrl)` 后没有重新启用 art-preview；已修复 `scripts/m01-preview-smoke.mjs`，新增 `hasTargetReferenceCard` / `targetReferenceSpriteFrame` / `targetReferenceFallbackActive` / `completionArtPreview` 断言，确认 target card 在初始与 completion 两段都加载 `m01-target-reference-card` 且未回退到灰盒线稿。最终验证：`npm test -- tests/m01PreviewSmokeScript.test.ts tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（58 tests），`node --check scripts/m01-preview-smoke.mjs` 成功，刷新 Cocos scripts + soft reload 后 `npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`，`artPreview.hasTargetReferenceCard = true`，`completionArtPreview.hasTargetReferenceCard = true`，console/page error 为空；唯一预览截图见 `temp/m01-art-preview-clean-qa-target-card-runtime-v2.png`。
- 2026-05-03 M01 minimal art-preview correction：用户指出上一张 art-preview 截图有大量无用信息，本轮将目标从“把全部贴片摆上去”改为“先删干净，再逐步加必要信息”。已删除本轮临时接入的 runtime `m01-target-reference-card.png/.meta`，真实 M01 `buildM01GreyboxStaticArtPlan(layout)` 改为不输出任何 decorative static surface；ToolCard preview 不再加载 `M01ToolCardPreviewArtFrame`，gear runtime art 不再进入 token art plan，`drawStandardReferencePattern()` 只画彩色 clue overlap，不再画完整答案轮廓 / 背板 / 全件 outline。art-preview runtime 下同步跳过中央底光盘、齿轮占位和拼接盘；clean QA 截图会临时隐藏 ToolCard、状态/反馈/提示按钮、手电控件和 flashlight beam，只保留当前必要的碎片关系与右侧 clue-only target。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（58 tests），`node --check scripts/m01-preview-smoke.mjs` 成功，`npm run typecheck` 成功，`npm test` 成功（18 files / 146 tests），`git diff --check` 成功；刷新 Cocos scripts + soft reload 后 `npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`artPreview.artSpriteCount = 16`、`staticArtCount = 0`、`hasDecorativeStaticArt = false`、`completionArtPreview.staticArtCount = 0`、console/page error 为空；当前 minimal QA 截图另存为 `temp/m01-art-preview-minimal-v2.png`。
- 2026-05-03 M01 floor-platform-only correction：上一条 minimal art-preview correction 误解用户意图，已回退。正确需求是保留正常 art-preview 结构，只删除地面上的大平台 / `fragmentFloor`。当前已恢复 target reference card 静态层、gear art token、ToolCard preview frame、中央底光盘 / 拼接盘、手电控件和完整 reference-pattern fallback；`buildM01GreyboxStaticArtPlan(real M01)` 只输出 `targetReferenceCard`，不再输出 `fragmentFloor`。`scripts/m01-preview-smoke.mjs` 现在断言 `hasTargetReferenceCard = true` 且 `hasFragmentFloor = false`。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（58 tests），`node --check scripts/m01-preview-smoke.mjs` 成功，`npm run typecheck` 成功，`npm test` 成功（18 files / 146 tests），`git diff --check` 成功；刷新 Cocos scripts/resources + soft reload 后 `npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`artPreview.artSpriteCount = 17`、`staticArtCount = 1`、`hasTargetReferenceCard = true`、`hasFragmentFloor = false`、console/page error 为空；当前对照截图为 `temp/m01-art-preview-no-floor-platform-v1.png`。
- 2026-05-02 M01 interaction regression checkpoint：新增 `tests/cocos/M01GreyboxSession.test.ts` 覆盖失败整体验证后的纠错重提交流程。验证：`npm test -- tests/cocos/M01GreyboxSession.test.ts` 成功（1 个测试文件 / 24 个测试），`npm run typecheck` 成功，`npm test` 成功（16 个测试文件 / 123 个测试），`git diff --check` 成功。
- 2026-05-02 M01 preview refresh workflow checkpoint：新增 `scripts/m01-preview-refresh.mjs` 与 `npm run smoke:m01-preview-refresh`，把 stale preview diagnosis、MCP `refresh_assets` / `soft_reload_scene` 顺序，以及“只有 MCP 刷新失败或本地 MCP server 不可达时才考虑重启 Cocos Creator”的 fallback 明确固化到 repo-local helper。新增 `tests/m01PreviewRefreshScript.test.ts` 先红后绿，覆盖 dry-run JSON 契约和离线 MCP 错误提示；验证：`npm test -- tests/m01PreviewRefreshScript.test.ts` 成功（1 个测试文件 / 2 个测试），`node --check scripts/m01-preview-refresh.mjs` 成功，`npm run typecheck` 成功，`npm test` 成功（17 个测试文件 / 126 个测试），`git diff --check` 成功。当前会话里 `127.0.0.1:3000` 与 `127.0.0.1:7456` 都离线，因此 live refresh 只验证到了“local MCP server unavailable”错误提示，post-refresh `npm run smoke:m01-preview` 复跑在本轮记为 skipped，因为 preview service 不可达。
- 2026-04-30 已完成新版 M01 plan 的 Task 8：旧九宫格 sorter art 已隔离为 legacy / calibration 资产；真实 overlap evidence layout 不再挂载旧红蓝黄碎片 sprite、旧滤镜 sprite 或九宫格托盘静态 layer，仅保留无害的齿轮 art token 候选。
- 已建立 Cocos 3.8.8 项目元数据与 TypeScript/Vitest 验证脚手架。
- 已实现 `PuzzleConfig` / `GoalEvaluator` / `ProgressStore` / `ToolCard` 核心契约。
- 已实现 `DragHandler` / `SnapZone` / `FilterSystem` 交互基础件。
- 已落地 `assets/resources/configs/stage1/m01-memory-gear.json`，并用真实 JSON 跑通 M01 控制器、完成判定、进度持久化与 ToolCard 一次性解锁。
- 已补充 `M01GreyboxBootstrap`，通过 Cocos `resources.load` 载入 M01 配置，避免 Creator 脚本编译不支持 JSON import attributes 的问题。
- 已补充 `M01GreyboxLayout` / `M01GreyboxSession`，运行时生成 M01 灰盒节点，并提供首版点击式验证路径：过滤器 -> 碎片 -> 槽位。
- 已补充 `assets/scenes/M01Greybox.scene`，并修复点击放置后的视觉状态同步：激活过滤器会刷新高亮/变暗，选中碎片会加粗，归位碎片会隐藏并禁用交互。
- 已补充最小 `ToolCardView` presenter 与完成态卡片预览：通关后场景内显示“认知工具卡已解锁 / 分类与归纳 / 智慧结晶 / 核心行动 / 何时使用”。
- 已修正 M01 初始碎片点位，避免碎片压住匹配槽位导致点击式灰盒无法完成 18/18。
- 已将 M01 灰盒运行期 status 本地化为中文，避免完成反馈混入英文调试文本。
- 已补充 WP4 首段反馈层：提示按钮按上下文提示过滤器 / 可选碎片 / 目标槽位；错误放置会保留选中并标出错误槽位；完成后齿轮进入修复态并展示 ToolCard。
- 已新增 `M01GreyboxText` 与 ToolCard preview text overrides，当前新增/触碰的可见文字均可通过 overrides 替换，后续接多语言不需要改玩法逻辑。
- 已新增 `M01GreyboxDrag`，把 greybox token drop 解析为三类动作：过滤器落在齿轮上启用过滤器、碎片落在槽位上尝试归位、无效落点返回原位。
- 已把 `M01GreyboxBootstrap` 接到 `DragHandler` / `SnapZone`：filter / fragment 节点现在通过 Cocos `touch-*` drag session 走真实拖拽路径；全局 mouse move/up 用于防止指针离开节点后拖拽丢失，并在组件销毁时解绑。
- Cocos 浏览器预览中发现真实鼠标会同时触发 `touch-*` 与 `mouse-*`；已移除节点级 `mouse-down` 启动绑定，避免 pointer mismatch 导致 drop 不执行。真实输入路径以 Cocos `touch-*` 为主，全局 mouse move/up 只保留兜底。
- 完成态 ToolCard 预览曾遮挡底部完成反馈；已改为完成时清空底部 feedback，仅保留顶部完成状态和 ToolCard。
- 手感检查发现槽位中心上下间距 40px、槽位吸附高度 52px，吸附区存在重叠；已把 M01 drop resolver 改为在多个槽位命中时优先选择与碎片颜色/形状标签匹配更多的槽位，再按距离选择，避免边界释放误吸到相邻槽。
- 已新增 M01 WP6 美术切片校准计划：`docs/plans/2026-04-27-m01-art-slice-calibration.md`；当前已明确第一张统一手绘锚点图为主要游戏风格参考，UI/莱米参考只做次级约束；生成允许低对比纸纹 / 水彩沉积，但拒收高对比白色噪点 / 碎白斑 / 做旧斑驳。
- 已建立 M01 美术切片资产目录：`assets/art/stage1-m01/`。首个候选 contact sheet 与旧 cropped QA candidates 已移至 `docs/design/generated-m01-art-slices/`；这些文件只供视觉 QA / paintover / prompt iteration。
- 已新增 `docs/design/generated-m01-art-slices/m01-runtime-sprite-sheet-candidate-v2.png`，专门补 v1 的空齿轮 / 空九槽盘 / 独立碎片 / 独立滤镜短板。
- 已从 v2 裁出五张纸背 runtime 候选：`m01-gear-star-slice.png`、`m01-nine-slot-tray-slice.png`、`m01-memory-fragments-slice.png`、`m01-color-filters-slice.png`、`m01-toolcard-thumbnail-slice.png`；Cocos 已为每张 PNG 生成真实 `.png.meta`。
- 已新增 `M01GreyboxArt` 清单和 `tests/cocos/M01GreyboxArt.test.ts`，把五张候选的角色、来源、editor `db://...` URL、非 `resources.load` 状态、最低像素尺寸和 Cocos image `.png.meta` 结构纳入自动验证。
- 已复制同一批候选到 `assets/resources/art/stage1-m01/`，并为 gameplay-scale preview 暴露 `resources.load(.../spriteFrame)` 路径；`M01GreyboxBootstrap.enableArtPreview` 默认关闭，开启时渲染非交互 art calibration layers。
- 已从纸背 resource copies 生成五张透明 runtime 候选到 `assets/resources/art/stage1-m01/runtime-transparent/`，并为其生成 Cocos `.png.meta`；`M01GreyboxArt` 现在区分 paper-backed preview resources 与 transparent runtime candidates。
- `M01GreyboxBootstrap.enableArtPreview` 已从纸背 preview plan 切到 `buildM01GreyboxRuntimeTransparentPlan()`，默认仍关闭，开启时使用透明候选做非交互 art-enabled 复验。
- 已从透明 composite 候选自动裁出独立 fragment/filter sprite-frame 候选：9 个碎片位于 `assets/resources/art/stage1-m01/runtime-sprites/fragments/`，3 个滤镜位于 `assets/resources/art/stage1-m01/runtime-sprites/filters/`；Cocos 已为这些 PNG 生成 `.png.meta`。
- `M01GreyboxArt` 已新增 token-level runtime sprite 清单与 `getM01GreyboxRuntimeSpriteResourceForToken()` / `buildM01GreyboxTokenArtPlan()`；`M01GreyboxBootstrap.enableArtPreview` 开启时，会在 fragment/filter token 节点下挂载 `M01ArtSprite_*` 子节点加载独立 spriteFrame，子节点非交互，原 token 节点继续负责拖拽和 hit target。
- Review 修复：token-level art sprite 现在会保存到 `greyboxNodes` 并随 fragment/filter presentation 同步颜色 / alpha；dimmed 碎片会降透明，selected / hinted / placed 等状态不会再被完整不透明 sprite 遮住。
- Gear/tray 继续向表现层替换推进：`getM01GreyboxRuntimeSpriteResourceForToken()` 现在会为 gear token 返回透明齿轮 spriteFrame；`buildM01GreyboxStaticArtPlan()` 只返回九槽托盘静态 layer，不再把 fragment/filter composite sheet 或 ToolCard thumbnail 作为 gameplay overlay 加载。
- Review 修复：gear token art resource 新增 `displaySize: { width: 300, height: 281 }`，`M01GreyboxBootstrap` 挂载 token art sprite 时优先使用 `resource.displaySize ?? token.size`，保留父 token 的 `300x300` 命中区但避免齿轮美术被拉伸。
- Preview hygiene：`M01GreyboxBootstrap.enableArtPreview` 显式声明为 `@property({ type: Boolean })`，避免 Cocos 预览控制台对布尔属性报 `undefined type` warning；默认仍保持 `false`。
- Art preview polish：`enableArtPreview` 开启时，灰盒 `Graphics` 通过 `applyTokenGraphicsState()` 切到轻量 underlay；普通 token 线宽封顶到 1，slot 线宽封顶到 2，普通填充 alpha 封顶 36，错误 / 提示 / 选中状态保留更高可见度。父 token 的 `UITransform` 和拖拽 hit target 不变。
- Art preview debug hygiene：新增 `showArtPreviewDebugUnderlay`，默认关闭；`enableArtPreview` 开启时，普通 slot/gear 的灰盒 underlay 不再绘制，避免九槽盘/齿轮美术出现双重描边。fragment/filter 仍保留轻量 underlay；提示/错误等反馈态仍会显示 slot/gear underlay。
- Art preview failure fallback：新增 `artPreviewFallbackUnderlayIds`，token art 或静态 tray art 加载失败时会把对应 gear / slot 灰盒 underlay 恢复出来，避免 art preview 资源缺失时只剩不可见 hit target。
- Art v3 direction：已完成最新 art-preview 视觉 QA；当前 v2 候选可用于玩法校准但不应升为最终 runtime 替换。v3 应改为 flat chroma-key 背景的透明候选生成，重点重画空齿轮中心、九槽托盘和三枚滤镜，让资产可直接进入 alpha extraction / Cocos spriteFrame 管线。

**不再做的事**：
- ❌ 扩写 M11-M33 的详设
- ❌ 给 spec 新增章节
- ❌ 继续优化措辞 / 加 meta 层

## Verification Evidence

- 2026-05-06 M01 selected-piece 90-degree rotation control：用户提出在空白纸面手动拼新目标时，需要选中拼片后用平台旁边按钮旋转 90 度。设计判断：这是当前“手动构图模式”的基础编辑能力，应先作为 runtime 编辑辅助实现，不接入胜利判定和目标 manifest 写回。修复：`M01GreyboxBootstrap` 新增 `M01Rotate90Button`，放在平台右侧；点击/拾起一个 fragment 后按 `旋转90°`，当前 `heldFragmentId` 对应节点原地顺时针加 90 度，并记录到 `tokenRotations`，按钮点击会抑制 root click，避免旋转时把拼片误放到按钮位置；未选中拼片时提示“先选中一个拼片”。目标 manifest 未锁定时 smoke 现在识别 `manualTargetCompositionMode`，仍验证真实浏览器手电 / 自由拖放 / 截图 / console error，但跳过证据 staged 和通关检查。验证：先新增 scaffold / smoke 红灯并确认失败；实现后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（47 tests），`npm test -- tests/m01PreviewSmokeScript.test.ts` 成功（7 tests），`npm run typecheck` 成功，`npm test` 成功（18 files / 167 tests），`git diff --check` 成功，`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，输出 `manualTargetCompositionMode = true`、`usedFallback = false`、console/page error 为空；额外 Playwright 实测点击 `fragment_triangle_blue_1` 后点击旋转按钮，`tokenRotations.get(...) = 90` 且 node `eulerAngles.z = 90`，截图 `temp/m01-rotate-button-smoke.png`。
- 2026-05-06 M01 empty target composition platform：用户要求把平台上的目标拼图清空，准备手动拖动拼片凑一个全新的目标。修复：保留 `standardPieces` 中圆 / 三角 / 六边形标准件定义，便于继续手动拼装；将 `assets/resources/configs/stage1/m01-memory-gear.json` 的 `targetPattern.locked` 改为 `false`，并把 `targetPattern.pieces` 清成空数组。由此 `M01GreyboxLayout.targetPieceSlots` 为空，`buildM01GreyboxTargetStandardPiecePlan()` 不再生成中心目标件，旧目标位置也不会再触发 `snap_fragment_to_target_piece`，玩家拖动拼片时可在空白平台上重新组合。验证：先新增 layout / drag / art / scaffold 红灯并确认失败（旧 6 个 target pieces 仍存在且会吸附），实现后 `npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功（80 tests）；`npm run typecheck` 成功；`npm test` 成功（18 files / 164 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，console/page error 为空；额外初始截图 `temp/m01-art-preview-empty-target-initial.png` 确认平台中心为空白拼接台。
- 2026-05-06 M01 blue palette correction：用户指出底部蓝色拼片和手电按钮都很像绿色。Root cause：运行时代码里的标准件蓝色为 `RGB 88/119/132`，手电 picker 蓝色为 `92/134/164`，手电本体 PNG 蓝按钮约落在 `73/100/118` 到 `132/152/160`，绿通道与蓝通道过近，叠在偏米黄平台上会被视觉混成青绿灰。修复：标准件色板改为 `72/104/190`，picker 蓝按钮改为 `80/110/206`，蓝色光束同步偏蓝；手电本体 runtime PNG 和 SVG 源里的蓝按钮重着色为明确低饱和蓝，同时保留墨线与水彩纹理。验证：先新增 scaffold 红灯并确认失败（旧蓝色仍存在），实现后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（46 tests）；`npm run typecheck` 成功；`npm test` 成功（18 files / 164 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，输出 `hasSingleFlashlightTool = true`、`realInput.usedFallback = false`、console/page error 为空；截图 `temp/m01-art-preview-clean-qa.png` 可见蓝色拼片和手电按钮已明显回到蓝色。
- 2026-05-06 M01 exact-geometry standard pieces：根据用户反馈“先别弄手绘风格，正圆 / 正三角 / 正六边形，六边形变形了”，把 art-preview 标准件从手绘 PNG sprite 本体切回 Cocos `Graphics` 精确几何绘制。Root cause：此前 layout 尺寸和 manifest 已标准化，但正常候选碎片和目标标准件仍加载 hidden-fragment bitmap，因此手绘瓷片边缘会把标准件形状带偏，尤其六边形视觉上不再是正六边形。修复：`M01GreyboxBootstrap` 新增 `isM01StandardPieceToken()` / `drawStandardPieceShape()`，候选 fragment 在 art preview 中不再挂载 `M01ArtSprite_*`，目标 `M01TargetStandardPiece_*` 也不再 `resources.load(piece.resourcesLoadPath)`，统一用 `drawExactEquilateralTriangle()` / `drawExactRegularHexagon()` / 正圆绘制；同时给标准件几何单独的灰白填色规则，避免被 art-preview underlay 透明度压得太淡。Review 修复：`buildM01GreyboxTargetStandardPiecePlan()` 也已从 hidden-fragment sprite resource lookup 解耦，target 标准件 plan 现在只依赖 layout slot 的 shape / size / position / rotation / layer；补充测试覆盖没有任何匹配 sprite resource 的 geometry-only target slot 仍会保留。验证：先新增 scaffold 红灯并确认失败（缺少 exact geometry 标准件路径，目标件仍加载 sprite），实现后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（46 tests）；review 修复后 `npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功（64 tests）；`npm run typecheck` 成功；`npm test` 成功（18 files / 164 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，输出 `artPreview.artSpriteCount = 1`（只剩非标准件 art sprite）、`completion.areAllEvidenceStaged = true`、`realInput.usedFallback = false`、console/page error 为空；截图 `temp/m01-art-preview-clean-qa.png` 可见底部候选件和中心目标件均为正几何灰白标准件。
- 2026-05-06 M01 clear old platform target art：用户确认进入标准件校准阶段后，需要把大螺母平台上的旧目标图案清空。修复：`buildM01GreyboxStaticArtPlan(real M01)` 不再输出 `targetReferenceCard` 静态层，只保留 `singleFlashlightTool`，中心平台只显示正几何标准件和玩家拖放状态；历史 `target_reference_card` 资源仍保留在 catalog / zoom 相关入口，避免丢失资产来源，但不再铺到底图上。同步 smoke 字段从 `hasTargetReferenceCard` 改为 `hasOldTargetReferenceCard` 并断言为 `false`。验证：先改测试并确认红灯（static plan 仍包含 `targetReferenceCard`），实现后 `npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（70 tests）；`npm run typecheck` 成功；`npm test` 成功（18 files / 164 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，输出 `staticArtCount = 1`、`hasOldTargetReferenceCard = false`、`hasSingleFlashlightTool = true`、console/page error 为空；截图 `temp/m01-art-preview-clean-qa.png` 可见平台旧彩色目标图案已清空。
- 2026-05-06 M01 own-color standard pieces：用户要求“这些拼片带上它自身的颜色”。修复：`buildFragmentNode()` 不再把所有 candidate fragment 的 `colorToken` 强制设为 `hidden`，而是使用 `fragment.hiddenColor ?? fragment.color ?? "hidden"`，所以底部候选标准件直接按红 / 黄 / 蓝显示；`colorForStandardPieceGeometry()` 的 dimmed 状态也保留原色相，仅降低透明度，不再回到灰白。目标 manifest 里的未放置标准件仍保持中性色，因为它们没有具体 fragment color 归属；玩家拖上平台的拼片会显示自身颜色。验证：先新增 layout / scaffold 红灯并确认失败（所有 fragment 仍为 `hidden` 且 dimmed 返回灰白），实现后 `npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocosProjectScaffold.test.ts` 成功（73 tests）；`npm run typecheck` 成功；`npm test` 成功（18 files / 164 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，console/page error 为空；截图 `temp/m01-art-preview-clean-qa.png` 可见底部拼片已按自身红 / 黄 / 蓝上色。
- 2026-05-06 M01 standard-piece target manifest：根据最新设计，把“标准件 -> 手动拼目标 -> 锁定目标”的链路落成数据源。新增计划 `docs/plans/2026-05-06-m01-standard-piece-target-manifest-plan.md`；`assets/resources/configs/stage1/m01-memory-gear.json` 新增 `standardPieces` 与 locked `targetPattern`，当前以最近验收的 6 个目标件位置作为初始手动 manifest。`M01GreyboxLayout` 不再硬编码 `target_piece_*` 坐标，而是从 manifest 解析 `targetPieceSlots`，并保留 `standardPieceId` / `rotation` / `layer`；`M01GreyboxArt` 和 `M01GreyboxBootstrap` 同步按 manifest 层级/旋转渲染目标标准件。验证：先新增红灯测试并确认失败（配置缺少 manifest、布局仍硬编码、art plan 缺标准件 ID/层级/旋转），实现后 `npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功（71 tests）；`npm run typecheck` 成功；`npm test` 成功（18 files / 162 tests）；`git diff --check` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，输出 `realInput.usedFallback = false`、`completion.areAllEvidenceStaged = true`、`artPreview.hasTargetReferenceCard = true`、console/page error 为空。预览地址：`http://127.0.0.1:7456/?scene=a2135734-fc11-4a0e-926d-40bc2301a752&m01ArtPreview=1`。
- 2026-05-05 M01 standard-piece target alignment：用户再次指出“早就说过，需要给这些拼片和目标值都做成标准件”。Root cause：上一轮只把目标合成图和隐形 target-piece snap zones 调到近似位置，仍然让目标合成图、可拖瓷片、吸附槽分别由不同尺寸体系驱动，尤其目标 PNG 经 Cocos `SpriteFrame` 自动裁切后实际内容比例与旧显示尺寸不一致。修复：新增并使用 `M01_STANDARD_PIECE_DISPLAY_SIZE = 56×56`，候选 fragment token、隐藏瓷片 sprite display size、target-piece snap zone 全部共用该标准件尺寸；`buildM01GreyboxTargetStandardPiecePlan()` 在目标图上额外渲染 6 个 `M01TargetStandardPiece_*`，直接复用底部可拖瓷片同源 hidden-fragment sprite，旧彩色目标合成图仅保留为半透明颜色提示层。验证：先新增红灯测试并确认失败（fragment 仍为 48、slot 仍为 64、target standard piece plan 不存在），实现后 `npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（25 tests）；`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（83 tests）；`npm run typecheck` 成功；`npm run smoke:m01-preview-refresh && npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，`realInput.usedFallback = false`、`completion.areAllEvidenceStaged = true`、console/page error 为空；`npm test` 成功（18 files / 161 tests）；`git diff --check` 成功。in-app browser 刷新后可见目标区标准件 overlay 已使用与底部瓷片同源的灰白瓷片 sprite。
- 2026-05-05 M01 art-preview flashlight picker：按用户纠正后的交互口径重做手电选择。art-preview 中点击单个三键手电贴图或其按钮热区，不再直接选择某个小按钮，而是在手电旁弹出 `M01FlashlightButtonPicker`，显示红 / 黄 / 蓝三枚 `40x40` 放大水彩按钮；选择任一颜色后 picker 销毁，并通过 `session.selectFlashlight()` 点亮对应颜色光束。同步更新真实输入 smoke：先点击手电区域打开 picker，再点击红色放大按钮，之后光束跟随指针照射目标碎片。修复 completion smoke 在 picker 化后仍按旧拖拽手电路径操作的问题，完成路径现在与人工路径一致。验证：`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功，输出 `realInput.usedFallback = false`、`realInput.activeFlashlightId = "flashlight_red"`、`completion.areAllEvidenceStaged = true`、`completionState.bottomLight = "steady_on"`、console/page error 为空；`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/m01PreviewSmokeScript.test.ts` 成功（4 files / 66 tests）；`npm run typecheck` 成功；`git diff --check` 成功。人工预览地址仍为 `http://127.0.0.1:7456/?scene=a2135734-fc11-4a0e-926d-40bc2301a752&m01ArtPreview=1`。
- 2026-05-05 M01 art-preview target guide repair：修复用户反馈的两个视觉问题。`target_reference_card` 的 runtime source 已从旧 `m01-locked-knot-target-clue-only-watercolor-v4.png` 切到干净的 `source/m01-locked-knot-target-colored-overlaps-only-v1.png`，并确认 runtime PNG 与该 clean source 字节一致；目标 guide 继续放在 `layout.gear.position`，尺寸保持 `260x145.31`，即平台中心。用户复看后指出右侧紫色 clue 仍有小缺口，已用已验收 `parts/target_reference_card.png` 的同位置紫/绿 clue 局部重建 `m01-locked-knot-target-colored-overlaps-only-v1.png`、`target_reference_card-colored-overlaps-only.png` 与 runtime PNG，只替换该局部透明 clue，不带入纸底卡片。Cocos preview 服务一度因 `/preview-app/index.js` 返回 500 卡住，已先保存 scene 后重启 Cocos Creator，入口恢复 200。验证：`npm test -- tests/cocos/M01GreyboxArt.test.ts tests/cocosProjectScaffold.test.ts` 成功（60 tests），`npm run typecheck` 成功，Cocos refresh + runtime-sprites refresh 成功，`npm run smoke:m01-preview:input -- --enable-art-preview --capture-clean-qa` 成功；smoke 输出 `artPreview.hasTargetReferenceCard = true`、`staticArtCount = 2`、`hasFragmentFloor = false`、console/page error 为空，截图见 `temp/m01-art-preview-clean-qa.png`。可预览地址：`http://127.0.0.1:7456/?scene=a2135734-fc11-4a0e-926d-40bc2301a752&m01ArtPreview=1`。
- 2026-05-02 M01 失败底光 preview smoke checkpoint：`curl -I http://127.0.0.1:7456/` 返回 `HTTP/1.1 200 OK`，确认预览服务在线；`npm test -- tests/cocos/M01GreyboxSession.test.ts` 成功（1 个测试文件 / 23 个测试），覆盖错误 complete candidate 触发底光闪烁、已 staged 碎片短暂暴露真实本色、约 2 秒后返回灰白的 session/domain 行为；`npm run typecheck` 成功；`npm test` 成功（15 个测试文件 / 121 个测试）。本轮未产出新截图，console/page error 也未采集，因为当前会话没有 `mcp__node_repl__js` 浏览器入口，repo 未安装 `playwright`，本机也没有 `chrome/chromium` 可供 shell 自动化驱动。
- 2026-05-02 M01 overlap art checkpoint：先新增 `tests/cocos/M01GreyboxArt.test.ts` 红灯，要求真实 M01 overlap-evidence layout 为 hidden fragment shapes 和 evidence markers 返回 runtime sprite，而不是只剩 gear token；实现后 `npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（1 个测试文件 / 14 个测试），`npm run typecheck` 成功，`npm test` 成功（15 个测试文件 / 122 个测试），`git diff --check` 成功，`npm run smoke:m01-preview` 成功。preview smoke 结果：scene URL 仍为 `http://127.0.0.1:7456/?scene=a2135734-fc11-4a0e-926d-40bc2301a752`，错误候选返回 `wrong_blend_color`，底光状态 `flash_then_off`，`validationColorDuringFlash = red`，2 秒后 `hasValidationColorAfterFlash = false`，console/page error = 0，截图输出 `temp/m01-preview-failed-validation-smoke.png`。
- 2026-04-24 已安装 Cocos Creator 3.8.8 到 `/Applications/CocosCreator-3.8.8.app`；`Info.plist` 显示 `CFBundleShortVersionString = 3.8.8`，二进制为 universal `x86_64 + arm64`，`codesign --verify --deep --strict` 通过。
- 2026-04-24 已用 `/Applications/CocosCreator-3.8.8.app/Contents/MacOS/CocosCreator --project /Users/danmac/liuhui-star-guardian` 打开项目入口；日志显示 engine 加载成功，并注册 `web-desktop` / `web-mobile` / `ios` / `android` / `wechatgame` 等平台成功；所有当前 `assets/` 下的 PNG/JPG/JSON/TS 资源均已生成对应 `.meta` 文件。随后修正 `M01GreyboxBootstrap.ts` 的 JSON 载入方式，避免 Cocos/Babel 不支持 `import attributes` 的脚本编译错误；复测未再出现该语法错误。
- 2026-04-24 已安装本机 Cocos MCP 插件（DaxianLee/cocos-mcp-server v1.4.0）到 `extensions/cocos-mcp-server/`，并写入本机 `.mcp.json` 的 `cocos-creator` HTTP 连接；启动 Cocos 后 `http://127.0.0.1:3000/health` 返回 `{"status":"ok","tools":157}`。
- 2026-04-24 M01 灰盒 TypeScript 原型验证通过：`npm run typecheck` 成功；`npm test` 成功（9 个测试文件 / 30 个测试）。
- 2026-04-24 M01 灰盒可视化入口补强：新增运行时布局与点击会话测试，`npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01GreyboxLayout.test.ts` 成功（2 个测试文件 / 6 个测试）。
- 2026-04-24 review 修复验证：`assets/scenes/M01Greybox.scene` 可通过 Cocos MCP 打开，`M01GreyboxRoot` 上识别到 `M01GreyboxBootstrap` 组件且无 `MissingScript`；`npm run typecheck` 成功；`npm test` 成功（11 个测试文件 / 38 个测试）。
- 2026-04-24 下一步执行验证：新增 M01 完成态 ToolCard 预览、中文完成状态和布局防遮挡测试；`npm run typecheck` 成功；`npm test` 成功（12 个测试文件 / 43 个测试）；Cocos 预览从过滤器 -> 18 个碎片 -> 槽位完整跑通，完成画面显示中文 ToolCard 预览，浏览器与 Cocos 控制台均无 error。
- 2026-04-24 WP4 反馈层验证：新增 M01 hint / wrong placement feedback / repair state / visible text replacement 测试；`npm run typecheck` 成功；`npm test` 成功（12 个测试文件 / 48 个测试）；Cocos 预览刷新资源后，Playwright + 本机 Chrome 点击提示按钮无 console/page error，截图见 `temp/m01-feedback-hint-click.png`。
- 2026-04-24 WP4 review 修复验证：Level 1/2 hint 现在会让过滤器 / 可选碎片进入 `hinted` 视觉态；hint 文案改为走 `M01GreyboxText` overrides，不再被 `config.hints[].text` 绕过；`npm run typecheck` 成功；`npm test` 成功（12 个测试文件 / 49 个测试）；Cocos 预览刷新资源后，Playwright + 本机 Chrome 点击提示按钮无 console/page error，截图见 `temp/m01-feedback-hint-review-fix.png`。
- 2026-04-27 接入上一线程未提交的 M01 拖拽灰盒进展并收口：去除 runtime drag 调试日志、补全全局 input 解绑生命周期；`npm run typecheck` 成功；`npm test` 成功（13 个测试文件 / 53 个测试）。尚未做 Cocos 预览拖拽实测。
- 2026-04-27 Cocos 预览拖拽实测：`http://127.0.0.1:7456/` 下真实 canvas 加载成功；首次实测发现 `touch-*` / `mouse-*` 双事件导致 drop 未执行，修复后复测通过。验证包括：红色过滤器拖到齿轮后 status 变为“已启用 red 过滤器”；红圆拖到正确槽位后 status 变为“已归位 1 个碎片”且碎片隐藏；红三角拖到错误红圆槽位显示 wrong_slot 错误反馈；随后完整拖拽 18/18 碎片通关，最终 status 为“M01 已修复，认知工具卡已解锁。”，ToolCard 标题为“分类与归纳”，active fragment count = 0，浏览器 console/page error = 0。截图见 `temp/m01-drag-cocos-preview-complete-no-overlap.png`。
- 2026-04-27 M01 拖拽手感检查：新增相邻槽位重叠区测试，先观察到红三角在红三角/红圆边界释放会误判到 `slot_red_circle`；修复后 `npm test -- tests/cocos/M01GreyboxDrag.test.ts` 成功（4 个测试），`npm run typecheck` 成功，`npm test` 成功（13 个测试文件 / 55 个测试）。Cocos 预览复测红三角边界释放：status = “已归位 1 个碎片。”，feedback = “归位正确。”，redTriangleActive = false，redCircleActive = true，console/page error = 0；完整 18/18 拖拽通关仍通过，ToolCard 标题为“分类与归纳”，active fragment count = 0。截图见 `temp/m01-drag-overlap-boundary-fix.png` 与 `temp/m01-drag-handfeel-complete.png`。
- 2026-04-27 M01 WP6 美术切片准备：新增 `docs/plans/2026-04-27-m01-art-slice-calibration.md`，明确 contact sheet prompt、ToolCard thumbnail prompt、拒收规则、目标文件和视觉 QA；新增 `assets/art/stage1-m01/README.md` 与对应 Cocos `.meta`；随后按用户判断将 `2026-04-22-unified-handdrawn-style-anchor.png` 提升为主要游戏风格参考，并把材质规则收敛为“允许低对比纸纹 / 水彩沉积，拒收高对比白色噪点 / 碎白斑 / 做旧斑驳”。已用内置 image generation 生成首个候选 `docs/design/generated-m01-art-slices/m01-contact-sheet-v1.png`（1536x1024，SHA-256 `31425a0995df1ceb840d818c318e16579cc98777fac6760f2a4e54e484fa320a`）；视觉初筛认为构图、风格、功能件分离度较好，但仍需人工确认轻微白色颗粒是否可接受。尚未接入 bitmap art。
- 2026-04-27 M01 art candidate hygiene 修复：未验收的 contact sheet 和 cropped QA candidates 已从 Cocos `assets/` 树移到 `docs/design/generated-m01-art-slices/`，避免缺失 `.png.meta` 和误认为 runtime-ready asset；当时 `assets/art/stage1-m01/` 只保留 README 和目录 meta。后续已接受 v2 的五张纸背 runtime 候选并导入该目录，见下一条。
- 2026-04-27 M01 runtime sprite sheet v2：用内置 image generation 生成 `docs/design/generated-m01-art-slices/m01-runtime-sprite-sheet-candidate-v2.png`（1536x1024，SHA-256 `59022f0c6c18763324f59e3f2676588498dc30c6ec2c96f6fd36c9aba6508777`）。v2 视觉预检：空齿轮、空九槽盘、9 个独立碎片、3 个独立滤镜均存在，风格贴近主锚点；仍需人工视觉 QA 和后续裁切 / 隔离，禁止直接 runtime 接入。
- 2026-04-27 M01 runtime candidate import：从 `m01-runtime-sprite-sheet-candidate-v2.png` 裁出五张纸背候选 PNG 到 `assets/art/stage1-m01/`；通过本机 Cocos MCP 刷新 `db://assets/art/stage1-m01`，确认 Cocos 为五张 PNG 生成真实 image `.png.meta`，`project_get_assets` 返回 count = 5 且五项类型均为 `cc.ImageAsset`。新增 `M01GreyboxArt` 清单测试，红灯为缺少清单 / 缺少 `.png.meta`，绿灯为 `npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（1 个测试文件 / 3 个测试）。随后 `npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 58 个测试）。尚未完成 art-enabled Cocos 玩法接入。
- 2026-04-27 review follow-up：修正 `M01GreyboxArt` 的 runtime 语义，明确当前五张 PNG 是 editor/import catalog，不是 `resources.load` 路径；清单新增 `assetDatabaseUrl` 与 `resourcesLoadPath: null`。`tests/cocos/M01GreyboxArt.test.ts` 现在会解析 `.png.meta` 并检查 `importer = image`、`imported = true`、texture 和 sprite-frame subMeta。同步修复 active 中“当前不含 PNG”的过期句子。
- 2026-04-27 M01 art preview resource path：复制五张纸背候选 PNG 到 `assets/resources/art/stage1-m01/`，通过 Cocos MCP 刷新该目录，`project_get_assets` 返回 count = 5 且五项类型均为 `cc.ImageAsset`。`M01GreyboxArt` 新增 `M01_GREYBOX_ART_PREVIEW_RESOURCES` 和 `buildM01GreyboxArtPreviewPlan()`，为每张候选提供 `art/stage1-m01/<name>/spriteFrame` 动态加载路径。`M01GreyboxBootstrap` 新增默认关闭的 `enableArtPreview`，开启时用 `SpriteFrame` 加载非交互校准层。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（2 个测试文件 / 15 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 61 个测试），`npm_config_registry=https://registry.npmjs.org npm audit --audit-level=moderate` 返回 0 vulnerabilities。
- 2026-04-28 art preview review follow-up：`M01GreyboxBootstrap` 现在在赋值 `spriteFrame` 前设置 `sprite.sizeMode = Sprite.SizeMode.CUSTOM`，避免 Cocos Sprite 按原图尺寸覆盖 preview plan 的 gameplay-scale `UITransform` 尺寸；`tests/cocos/M01GreyboxArt.test.ts` 新增 source PNG 与 `assets/resources` preview copy 的字节一致性测试，防止双份候选漂移。验证：`npm test -- tests/cocosProjectScaffold.test.ts` 成功（10 个测试），`npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（6 个测试），`npm run typecheck` 成功。
- 2026-04-28 接盘复验：`npm run typecheck` 成功；`npm test` 成功（14 个测试文件 / 62 个测试）；`npm_config_registry=https://registry.npmjs.org npm audit --audit-level=moderate` 返回 0 vulnerabilities。Cocos 预览与 MCP 均在线：`http://127.0.0.1:7456/` 返回 200，`http://127.0.0.1:3000/health` 返回 `{"status":"ok","tools":157}`。通过 Cocos MCP 临时打开 `M01GreyboxBootstrap.enableArtPreview` 后，Playwright 复验 5 个 `M01ArtPreview_*` 节点全部 active，红色过滤器仍可拖到齿轮并把状态推进到“已启用 red 过滤器。请选择同色碎片。”，console/page error = 0，截图见 `temp/m01-art-preview-enabled-check.png`。视觉结论：当前五张纸背候选存在矩形纸背与裁切遮挡，适合校准 / paintover，不适合直接作为最终 gameplay sprite 替换。
- 2026-04-28 透明 runtime 候选首轮接入：从 `assets/resources/art/stage1-m01/` 的五张纸背 preview copy 生成五张 RGBA 透明候选到 `assets/resources/art/stage1-m01/runtime-transparent/`，并通过 Cocos MCP 刷新目录；`project_get_assets` 返回 count = 5 且五项类型均为 `cc.ImageAsset`。新增透明候选测试：每个 PNG 必须 RGBA、四角 alpha = 0、保留足够 opaque 像素、存在 `.png.meta`，并暴露 `art/stage1-m01/runtime-transparent/<name>/spriteFrame`。`M01GreyboxBootstrap.enableArtPreview` 已改用透明候选 plan。验证：先确认新增测试红灯（缺少清单 / plan），再实现后 `npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（8 个测试），`npm test -- tests/cocosProjectScaffold.test.ts` 成功（10 个测试）。Cocos 预览临时打开 art preview 后，5 个 `M01ArtPreview_*` 节点全部 active，红色过滤器仍可拖到齿轮并把状态推进到“已启用 red 过滤器。请选择同色碎片。”，console/page error = 0，截图见 `temp/m01-runtime-transparent-preview-check.png`。验证后已把 `enableArtPreview` 关回 false。
- 2026-04-28 透明 art-enabled 完整通关复测：临时打开 `M01GreyboxBootstrap.enableArtPreview` 后，用 Playwright 按配置自动拖拽 3 个过滤器与 18 个碎片，最终状态为“M01 已修复，认知工具卡已解锁。”，ToolCard 标题为“分类与归纳”，active fragment count = 0，5 个 `M01ArtPreview_*` 节点均 active，console/page error = 0，截图见 `temp/m01-runtime-transparent-complete.png`。复测后已把 `enableArtPreview` 关回 false。视觉观察：透明候选已经消除纸背矩形，但仍作为 overlay 叠在灰盒之上；下一步应拆 sprite-frame / atlas 并改为表现层替换。
- 2026-04-28 token-level sprite 拆分接入：先新增 M01 art 测试红灯，要求独立 9 个 fragment + 3 个 filter runtime sprites、RGBA 透明边角、Cocos `.png.meta`、以及 layout token 到 spriteFrame 的映射；随后从透明 composite 候选自动裁出 12 张 PNG 到 `assets/resources/art/stage1-m01/runtime-sprites/`，通过 Cocos MCP 刷新 `db://assets/resources/art/stage1-m01/runtime-sprites`，`project_get_assets` 返回 count = 38（包含 12 个 `cc.ImageAsset` 及其 texture / spriteFrame subassets）。`M01GreyboxBootstrap.enableArtPreview` 开启时为 fragment/filter token 增加非交互 `M01ArtSprite_*` 子节点；灰盒 hit targets 和拖拽节点仍保留。验证：红灯为缺少清单 / 函数，绿灯为 `npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（10 个测试）；`npm test -- tests/cocosProjectScaffold.test.ts` 成功（10 个测试）；`npm run typecheck` 成功；`npm test` 成功（14 个测试文件 / 66 个测试）。
- 2026-04-28 token art review 修复：新增 scaffold 测试红灯，要求 `M01GreyboxBootstrap` 保存 `artSprite` 引用并在 `syncVisualState()` 中同步 token-level art sprite presentation；修复后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（11 个测试）。完整验证：`npm run typecheck` 成功；`npm test` 成功（14 个测试文件 / 67 个测试）；`npm_config_registry=https://registry.npmjs.org npm audit --audit-level=moderate` 返回 0 vulnerabilities；密钥扫描与危险 JS pattern 扫描无命中。
- 2026-04-28 gear/tray node-level art plan：新增测试红灯，要求 gear 进入 token art plan、tray 进入静态 gameplay art plan，并且 static plan 不再包含 `memoryFragments` / `colorFilters` composite overlay。实现后 `npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（11 个测试），`npm test -- tests/cocosProjectScaffold.test.ts` 成功（11 个测试）。完整验证：`npm run typecheck` 成功；`npm test` 成功（14 个测试文件 / 68 个测试）；`npm_config_registry=https://registry.npmjs.org npm audit --audit-level=moderate` 返回 0 vulnerabilities；密钥扫描与危险 JS pattern 扫描无命中。尚未完成 art-preview Cocos 完整通关复验。
- 2026-04-28 gear art display size review fix：先新增测试红灯，要求 gear runtime sprite resource 声明 `displaySize: { width: 300, height: 281 }`，并要求 Bootstrap token art sprite 使用 `resource.displaySize ?? token.size` 设置 `UITransform`；实现后 `npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（11 个测试），`npm test -- tests/cocosProjectScaffold.test.ts` 成功（11 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 68 个测试），`npm audit --audit-level=moderate` 返回 0 vulnerabilities，密钥扫描与危险 JS pattern 扫描无命中。
- 2026-04-28 token-level art-preview Cocos 复验：通过 Cocos MCP 刷新脚本与 soft reload scene 后，在浏览器运行时临时打开 `enableArtPreview` 并重渲染灰盒；验证 22 个 `M01ArtSprite_*` 与 1 个 `M01StaticArt_nineSlotTray` 均加载 spriteFrame，gear sprite size = `300x281`，tray size = `156x166`。红色过滤器启用后，红色 fragment sprite alpha = 220，蓝/黄 fragment sprite alpha = 56，证明 dim/highlight 已同步到 art 子层；完整真实鼠标拖拽 3 个过滤器 + 18 个碎片后，active fragment count = 0，placed fragment 父节点 inactive 且 sprite alpha = 0，gear sprite repaired alpha = 255，console/page error/warning = 0。截图见 `temp/m01-art-token-preview-red-filter-dim.png` 与 `temp/m01-art-token-preview-complete.png`。视觉观察：当前候选已可完成 gameplay-scale preview，但 art preview 下灰盒线框/槽位叠加仍偏重，需下一轮视觉 polish。
- 2026-04-28 art-preview greybox underlay polish：新增 scaffold 红灯，要求 `M01GreyboxBootstrap` 通过 `applyTokenGraphicsState()` 在 art preview 模式下降低灰盒 `Graphics` 强度；实现后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（12 个测试），`npm run typecheck` 成功，`npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（11 个测试）。Cocos 预览中临时打开 `enableArtPreview` 后，初始 `entity_memory_gear` / fragment / filter lineWidth = 1、fillAlpha = 36、strokeAlpha = 48，slot lineWidth = 2、fillAlpha = 36、strokeAlpha = 82；完整拖拽后 active fragment count = 0，console/page error/warning = 0。截图见 `temp/m01-art-preview-subdued-initial.png` 与 `temp/m01-art-preview-subdued-complete.png`。
- 2026-04-28 art-preview debug underlay toggle：视觉 QA 发现 `temp/m01-art-preview-subdued-complete.png` 中 slot/gear 仍有双重灰盒描边；新增 `showArtPreviewDebugUnderlay` 默认关闭，并让普通 slot/gear 在 art preview 下 lineWidth = 0、fill/stroke alpha = 0。验证：新增 scaffold 红灯后实现，`npm test -- tests/cocosProjectScaffold.test.ts` 成功（13 个测试），`npm run typecheck` 成功。Cocos 预览临时打开 `enableArtPreview` 后，初始 `entity_memory_gear` 与 `slot_red_circle` lineWidth = 0、fillAlpha = 0、strokeAlpha = 0，fragment/filter 仍为 lineWidth = 1、fillAlpha = 36、strokeAlpha = 48；完整拖拽后 status = “M01 已修复，认知工具卡已解锁。”，active fragment count = 0，ToolCard 存在，console/page error/warning = 0。截图见 `temp/m01-art-preview-debug-underlay-hidden-initial.png` 与 `temp/m01-art-preview-debug-underlay-hidden-complete.png`。
- 2026-04-28 art-preview load failure fallback：新增 scaffold 红灯，要求 token/static art load failure 登记 fallback underlay；实现后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（14 个测试），`npm run typecheck` 成功。Cocos 预览中临时打开 `enableArtPreview` 并手动触发 fallback：正常状态下 `entity_memory_gear` / `slot_red_circle` lineWidth = 0、fillAlpha = 0、strokeAlpha = 0；触发 `markArtPreviewUnderlayFallback("entity_memory_gear")` 与 `markStaticArtPreviewUnderlayFallback("nineSlotTray")` 后，gear 恢复 lineWidth = 1、fillAlpha = 36、strokeAlpha = 48，slot 恢复 lineWidth = 2、fillAlpha = 36、strokeAlpha = 82，console/page error/warning = 0。
- 2026-04-28 M01 art v3 visual QA / prompt：复看 `temp/m01-art-preview-debug-underlay-hidden-complete.png`、主风格锚点和 `m01-runtime-sprite-sheet-candidate-v2.png` 后，确认 v2 在 gameplay-scale 已可读，但滤镜仍像纸背标签，gear/tray 仍有 composite/crop 痕迹，不适合升为最终 runtime art。已新增 `docs/design/generated-m01-art-slices/m01-runtime-v3-visual-qa-and-prompt.md`，要求下一轮生成 flat `#00ff00` chroma-key 背景、可透明提取的正式 runtime sprite sheet，并明确 gear / tray / filters / fragments 的验收规则；review 后已修正 tray prompt 的行列语义：列为 red/blue/yellow，行为 circle/triangle/hexagon，与 runtime hit targets 一致。
- 2026-04-28 review recheck：复核 gear art `displaySize`、art load failure fallback underlay、v3 tray 行列语义三条 review finding，当前工作树已覆盖对应修复。验证：`npm run typecheck` 成功；`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxArt.test.ts` 成功（25 个测试）；`npm test` 成功（14 个测试文件 / 71 个测试）；`npm audit --audit-level=moderate` 返回 0 vulnerabilities；密钥扫描与危险 JS pattern 扫描无命中。下一步仍是按 v3 prompt 生成 / 导入新的透明 runtime 候选。
- 2026-04-28 v3 generation rejected attempt：一次纯文本 imagegen 生成的 v3 sheet 风格明显偏离主参考图，原因是未把主风格锚点作为视觉输入，且 runtime sheet / flat `#00ff00` / functional cartridge 等约束压过了 Arrog 式手绘线条与留白比例。该图未导入 repo 资产树，不作为候选继续推进。下一次应以主风格锚点和 v2 结构图作为显式视觉参考，优先保留粗糙墨线、纸面留白感、低对比水彩沉积和不规则边缘，再解决透明提取。
- 2026-04-28 主参考图清理：新增 `docs/design/style-references/2026-04-22-unified-handdrawn-style-anchor-cleaned-v1.png` 和更深补色版 `docs/design/style-references/2026-04-22-unified-handdrawn-style-anchor-cleaned-v2.png`，用局部同色系补色方式处理高亮白色噪点 / 碎白斑，不覆盖原图。v2 取周围更暗的水彩邻域色填回白点，目标是保留原图水彩深浅不均、墨线和纸感，同时进一步减少看起来脏的盐粒白点；对比图见 `temp/m01-style-anchor-cleaned-v2-comparison.png`。
- 2026-04-28 贴图源复核：当前 Cocos runtime gear 仍来自 `m01-runtime-sprite-sheet-candidate-v2` 裁图，即使清理白噪点也不会变成主参考图造型。已新增直接从 cleaned-v2 主参考图裁出的对照候选 `docs/design/generated-m01-art-slices/m01-anchor-cleaned-v2-gear-composite-candidate.png`，对比图见 `temp/m01-current-v2-vs-anchor-cleaned-gear.png`。该候选中心是已完成九宫格，不应直接替换初始 gameplay 空 gear；下一步应基于 cleaned-v2 参考图重做“空心 gear / 空 tray”的贴图，而不是继续修 v2 生成资产。
- 2026-04-29 M01 empty gear candidate：用户最终选择以 16:37 版本作为原型母版，不再继续调色 / 修线 / 重绘。当前 preferred review-only candidate 为 `docs/design/generated-m01-art-slices/m01-anchor-cleaned-v2-gear-empty-center-rich-color-candidate.png`；它保留更丰富的机械主体灰褐水彩浓淡。后续工作应只围绕该图做工程化裁切、透明化和 gameplay-scale 导入预检。`m01-anchor-cleaned-v2-gear-empty-center-rich-color-outline-tidy-candidate.png` 仅保留为后续对照，不作为当前原型推进。
- 2026-04-30 新版 M01 Task 1 验证：先新增混色测试并确认红灯（`blendM01PigmentColors is not a function` / `revealM01FragmentColor is not a function`），随后实现最小 helper；`npm test -- tests/levels/stage1/M01MemoryGearController.test.ts` 成功（1 个测试文件 / 7 个测试），`npm run typecheck` 成功。
- 2026-04-30 新版 M01 Task 2 验证：真实 M01 JSON 已切到 `overlap_evidence_reconstructed`，新增测试覆盖候选池 12-16、证据数 4-6、solution 派生使用碎片、shape-compatible decoys、不泄露目标答案；已提交的旧九宫格 layout/session runtime 测试改用 legacy fixture。可在 clean checkout 复现的验证：`jq empty assets/resources/configs/stage1/m01-memory-gear.json` 成功；`npm test -- tests/levels/stage1/M01MemoryGearController.test.ts tests/core/PuzzleConfig.test.ts` 成功（14 个测试）；`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxSession.test.ts` 成功（14 个测试）；`npm run typecheck` 成功。
- 2026-04-30 新版 M01 Task 3 验证：先新增 controller tests 并确认红灯（缺少 `stageEvidencePair` / `validateCandidateStructure` / evidence completion 字段），随后实现候选结构验证。验证：`npm test -- tests/levels/stage1/M01MemoryGearController.test.ts` 成功（17 个测试）；`npm test -- tests/core/PuzzleConfig.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxSession.test.ts` 成功（17 个测试）；`npm run typecheck` 成功。
- 2026-04-30 新版 M01 Task 3 review 修复：`rejectCandidateStructure()` 的底光闪烁现在按 `validationLightSeconds` 到期返回 `off`，`getCompletionState()` 不再无限保留 `flash_then_off`。验证：`npm test -- tests/levels/stage1/M01MemoryGearController.test.ts` 成功（18 个测试），`npm run typecheck` 成功。
- 2026-04-30 新版 M01 Task 4 验证：先把 layout 测试改为真实 M01 overlap evidence 配置并确认红灯（`layout.flashlights` / `layout.evidence` 不存在），随后实现新版 layout 节点。验证：`npm test -- tests/cocos/M01GreyboxLayout.test.ts` 成功（3 个测试）；`npm test -- tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxSession.test.ts tests/levels/stage1/M01MemoryGearController.test.ts tests/core/PuzzleConfig.test.ts` 成功（33 个测试）；`npm run typecheck` 成功。
- 2026-04-30 新版 M01 Task 5 验证：先把 drag resolver 测试改为真实 M01 overlap evidence 配置并确认红灯（手电返回 `wrong_token_kind`，碎片仍访问旧 `layout.slots`），随后实现新版 drop action。验证：`npm test -- tests/cocos/M01GreyboxDrag.test.ts` 成功（4 个测试）；`npm test -- tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/cocos/M01GreyboxSession.test.ts tests/levels/stage1/M01MemoryGearController.test.ts` 成功（34 个测试）；`npm run typecheck` 成功。
- 2026-04-30 新版 M01 Task 6 验证：先新增 session tests 并确认红灯（`selectFlashlight` / `revealFragment` / `submitEvidencePair` / `pickFragment` 等方法不存在），随后实现新版 session API。验证：`npm test -- tests/cocos/M01GreyboxSession.test.ts` 成功（15 个测试）；`npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/levels/stage1/M01MemoryGearController.test.ts` 成功（40 个测试）；`npm run typecheck` 成功。
- 2026-04-30 Task 6 review 修复验证：新增 session tests 并确认红灯（legacy config 下 `selectFlashlight` 抛 TypeError；真实配置 hint targetIds 为空），随后修复数组兼容和新版 hint 分支。验证：`npm test -- tests/cocos/M01GreyboxSession.test.ts` 成功（17 个测试）；`npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01GreyboxDrag.test.ts tests/cocos/M01GreyboxLayout.test.ts tests/levels/stage1/M01MemoryGearController.test.ts` 成功（42 个测试）；`npm run typecheck` 成功。
- 2026-04-30 新版 M01 Task 7 验证：先新增 bootstrap scaffold 红灯，要求 `layout.board` / `layout.flashlights` / `layout.evidence` 渲染路径和 `select_flashlight` / `weak_snap_fragment` / `place_fragment_freely` action 接线；实现后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（16 个测试），`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01GreyboxDrag.test.ts` 成功（37 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 92 个测试）。
- 2026-04-30 Task 7 review 修复验证：先新增 scaffold/session 红灯，覆盖融合色 palette、局部证据 shape 绘制、新版 hint target 高亮、点击拾取/点击放置，以及弱磁吸后释放 held fragment；实现后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（19 个测试），`npm test -- tests/cocos/M01GreyboxSession.test.ts` 成功（18 个测试），`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxSession.test.ts tests/cocos/M01GreyboxDrag.test.ts` 成功（41 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 96 个测试），`npm audit --audit-level=moderate --registry=https://registry.npmjs.org` 返回 0 vulnerabilities。
- 2026-04-30 新版 M01 Task 8 验证：先新增 art tests 并确认红灯（真实 overlap evidence layout 仍加载 legacy nine-slot tray）；实现后 `npm test -- tests/cocos/M01GreyboxArt.test.ts` 成功（13 个测试），`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxArt.test.ts tests/cocos/M01GreyboxLayout.test.ts` 成功（35 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 98 个测试），`npm audit --audit-level=moderate --registry=https://registry.npmjs.org` 返回 0 vulnerabilities。
- 2026-04-30 新版 M01 Task 9 验证：先新增 ToolCard / preview / scaffold 文案测试并确认红灯（真实 M01 ToolCard 仍是旧“多维线索分类关系”文案，灰盒默认文案仍含旧“颜色过滤器 / 收纳槽”话术）；实现后 `npm test -- tests/core/ToolCard.test.ts tests/ui/ToolCardView.test.ts tests/cocosProjectScaffold.test.ts` 成功（3 个测试文件 / 25 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 100 个测试），`npm audit --audit-level=moderate --registry=https://registry.npmjs.org` 返回 0 vulnerabilities，密钥扫描与危险 JS pattern 扫描无命中。
- 2026-04-30 新版 M01 Task 10 自动验证：`npm run typecheck` 成功；`npm test` 成功（14 个测试文件 / 100 个测试）；`npm audit --audit-level=moderate --registry=https://registry.npmjs.org` 返回 0 vulnerabilities。Cocos preview smoke 尝试：`http://127.0.0.1:7456/` 外壳返回 200，并生成截图 `temp/m01-task10-preview-initial.png`；但浏览器实际加载 runtime 时 `/scripting/polyfills/bundle.js` 与 `/preview-app/index.js` 返回 500，页面停在 “Get http://127.0.0.1:7456/preview-app/index.js failed!”。因此尚未完成手电选择、碎片显色、弱磁吸、错误底光 2 秒熄灭、正确通关 ToolCard 解锁的浏览器交互 smoke。剩余 blocker：需刷新 / 重启 Cocos Creator preview 服务后重试；美术资产仍需后续补正式灰白碎片、手电光束和证据 marker。
- 2026-04-30 新版 M01 Task 10 preview 复验：已按用户授权重启 Cocos Creator / preview 服务。`http://127.0.0.1:7456/` 外壳 200 且 runtime 资源不再 500；默认 `/` 会因 `current_scene` 为空而显示黑场，使用 `http://127.0.0.1:7456/?scene=a2135734-fc11-4a0e-926d-40bc2301a752` 可进入 `M01Greybox`。Playwright + 本机 Chrome 完成 smoke：选择红光手电，按 `fragment_a/b/c/d/e/f/g/h` 点击拾起并点击对应 evidence 放置，四个证据均 staged，最终 status = “底光保持亮起，结构成立。”，ToolCard 标题 = “分类与归纳”，console/page error = 0，截图见 `temp/m01-task10-click-place-complete-fixed-restart.png`。该轮还修复了点击放置第二片时误拾取已吸附第一片的问题；验证：`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 100 个测试）。
- 2026-05-01 M01 表现层 polish 验证：先新增 scaffold 红灯，要求 `M01GreyboxBootstrap` 渲染手电光束、拼接盘底光、底光简笔提示和验证态 evidence 点亮；实现后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（21 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 101 个测试）。
- 2026-05-01 M01 底光视觉复位修复验证：先扩展 scaffold 测试并确认红灯（缺少 `validationLightResetTimeout` / `scheduleValidationLightReset` / `clearValidationLightReset`），随后实现失败验证后的超时重绘与销毁清理；`npm test -- tests/cocosProjectScaffold.test.ts` 成功（21 个测试）。
- 2026-05-01 M01 灰白碎片轮廓 polish 验证：先新增 scaffold 红灯，要求 candidate fragments 有 shape-specific 绘制函数和 `hidden` palette；实现后 `npm test -- tests/cocosProjectScaffold.test.ts` 成功（22 个测试）。
- 2026-05-01 M01 手电观察显色 polish 验证：先新增 session / scaffold 红灯，要求手电照到候选碎片后 `getFragmentView()` 暴露 `observedColor` 并让 Bootstrap 使用融合色重绘；拾起或弱磁吸进入拼接流程时立即清除 observed color，保证拼接盘仍保持灰白、防止逐片试色。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxSession.test.ts` 成功（42 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 104 个测试）。
- 2026-05-01 M01 手电显色 review 修复：观察显色不再永久留在观察区；`observedColor` 现在以 `M01_OBSERVED_REVEAL_MS = 2000` 作为短暂窗口，过期后 session 自动清除，Bootstrap 也会安排超时重绘，避免画面停在旧融合色。验证：`npm test -- tests/cocos/M01GreyboxSession.test.ts tests/cocosProjectScaffold.test.ts` 成功（43 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 105 个测试）。
- 2026-05-01 M01 连续手电照射 polish：Bootstrap 的观察显色重绘不再只有一个可被覆盖的 timeout；每次成功照射都会登记独立短定时器，保证连续扫过多块碎片时，每块碎片按自己的 2 秒窗口熄灭。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxSession.test.ts` 成功（43 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 105 个测试）。
- 2026-05-01 M01 失败底光显色 polish：新增 session 红灯，要求错误整体验证时只在 2 秒底光闪烁窗口内暴露已 staged 碎片的真实本色，窗口结束后恢复灰白；同时新增 scaffold 红灯，要求 Bootstrap 在重绘 fragment 时优先消费 `validationColor`。实现后 `submitEvidencePair()` 会登记 staged pair，`getFragmentView()` 在 `bottomLight === "flash_then_off"` 时仅对已 staged 碎片返回 `validationColor`，Bootstrap 则以 `view.validationColor ?? view.observedColor` 作为颜色覆写。验证：`npm test -- tests/cocos/M01GreyboxSession.test.ts` 成功（21 个测试），`npm test -- tests/cocosProjectScaffold.test.ts` 成功（24 个测试），`npm run typecheck` 成功，`npm test` 成功（14 个测试文件 / 107 个测试），`git diff --check` 成功；预览可用性检查：`curl -I http://127.0.0.1:7456/` 返回 `HTTP/1.1 200 OK`，但本轮未做新的交互式浏览器 smoke。
- 2026-05-01 AI workflow 更新：新增 `docs/ai-autonomous-checkpoint-loop.md`，把当前“执行 checkpoint → 验证 → 自审 → 修复 → 记录 → 提交 → 下一步”的 bounded Ralph loop 固化为 repo-local 工作协议；`docs/ai-workflow.md` 新增入口说明，后续用户说“继续下一步 / 自己 review 一下”时应按该循环自主推进，直到遇到产品决策、工具阻塞或验证不明确。
- 2026-05-01 ralphex Codex wrapper 试运行：已通过 Homebrew 安装 `ralphex v1.0.1`；默认 Claude Code 路径因 CLI 未登录失败。随后接入官方 `codex-as-claude.sh` wrapper，并在 `/private/tmp/ralphex-smoke` 玩具 repo 中用 `CODEX_SANDBOX=workspace-write CODEX_MODEL=gpt-5.4 ralphex --tasks-only --max-iterations=1` 成功执行单任务 plan，产生 `feat: create smoke marker` 与 `move completed plan` 两个提交。项目内新增 `.ralphex/config` 指向本地 wrapper，暂时关闭 external review / finalize，作为后续小范围真实 plan 试跑入口。
- 2026-05-01 ralphex wrapper 安全修复：`codex-as-claude.sh` 的默认 `CODEX_SANDBOX` 已从官方脚本的 `danger-full-access` 改为 `workspace-write`，避免直接运行项目内 `ralphex` 时默认给 Codex 全量非沙盒权限。
- 2026-05-01 review 修复回合：确认并修复了五类真实问题。1) `.ralphex/bin/codex-as-claude.sh` 不再默认传 `--dangerously-bypass-approvals-and-sandbox`，并改为在 FIFO 流式转发下保留真实 `codex` 退出码，不再把失败伪装成空成功。2) M01 staged evidence 在碎片被重新拾起/移走时现在会通过 controller/session 明确 unstage，避免 UI 与 domain state 漂移后仍可误过校验。3) overlap-evidence goal 现已加入 `PuzzleConfig` 参数校验与负例测试，坏 JSON 会在加载/测试期被拦下。4) 增补回归测试：真实 M01 config 的成功解锁持久化、`incomplete_candidate` 分支、碎片移走后失去 staged 状态，以及观察显色独立 2 秒过期调度。5) 文档与 handoff 已同步到当前 reality：新增根 `README.md`、更新 `CLAUDE.md` 自动化约定、刷新 `assets/scenes/README.md` 的 M01 运行说明，并把两个相关计划文件标记为已完成/补足 commit hash 与残留脏文件。
- 2026-05-01 review 修复验证：`npm run typecheck` 成功；`npm test` 成功（15 个测试文件 / 113 个测试）；`git diff --check` 成功；wrapper smoke 1 确认默认参数包含 `exec --json -s workspace-write` 且不再携带危险绕过 flag；wrapper smoke 2 使用 stub `codex` 返回 `exit 7`，wrapper 现已同样返回 7 而不是伪造成功。
- 2026-05-06 M01 点击拾起 / 再点放下修复：复现到“拿起后再次点击同一个拼片”会重新走 `pickFragment`，导致状态停在“已拾起碎片”；已将 Bootstrap 的 token click 分支改为只要当前 `heldFragmentId` 存在，就在点击位置执行 `placeHeldFragmentAtPosition()`，覆盖同一拼片与其他 token。验证：`npm test -- tests/cocosProjectScaffold.test.ts tests/cocos/M01GreyboxSession.test.ts`、`npm run typecheck`、`npm test`、`git diff --check` 均成功；Cocos MCP refresh 成功；Playwright 真实预览中点击 `fragment_circle_yellow_1` 两次后 `heldFragmentId` 清空，状态变为“已把碎片 fragment_circle_yellow_1 放在工作区。”，截图见 `temp/m01-click-same-fragment-place-fixed.png`。
- Spec 收口到 v1.9（2026-04-20），Codex Round 3 审阅完成，诊断记入 §七 路线图 + §十 风险表
- 2026-04-22 已将美术主轴改为 Arrog 式统一手绘墨线 + 低饱和淡彩，并落盘到 `docs/design/game-design-spec.md` §4
- 2026-04-22 已将整体风格参考图入库到 `docs/design/style-references/2026-04-22-unified-handdrawn-style-anchor.png`，并补充提炼规则到 `docs/design/style-references/README.md`
- 2026-04-22 桌面截图 `截屏2026-04-22 09.43.48.png` 与已入库的 `docs/design/style-references/2026-04-22-unified-handdrawn-style-anchor.png` 哈希一致；已补充资产副本到 `assets/art/style-references/unified-handdrawn-style-anchor.png`
- 2026-04-23 已将新增界面参考图重命名并重新表述为游戏界面/面板参考，去除外部工具界面方向词，更新 `docs/design/game-design-spec.md`、`docs/design/style-references/README.md` 与资产路径
- 2026-04-24 已将新增莱米兔子图纳入主动风格参考：`docs/design/style-references/2026-04-24-lemmy-rabbit-style-reference.png`；同步资产副本 `assets/art/style-references/lemmy-rabbit-style-reference.png`
- 2026-04-24 已新增 M01 第一关原型项目计划：`docs/plans/2026-04-24-m01-prototype-plan.md`，明确先灰盒可玩、再按四张主动参考图做首个美术切片
- 2026-04-22 已生成 Stage 1 `M02-M10` 首轮场景概念图，并存入 `docs/design/generated-stage1-scenes/`
- 2026-04-22 已选定并落盘 Stage 1 首批 7 张场景资产到 `assets/art/stage1-scenes/`：`M02-M08`，其中 `M07` 采用 `v1`，`M08` 采用 `v1`
- 会话记录见 `.context/codex-session-id`（019da998-3185-7db3-9ae8-4a74a1ab7330）
- 五阶段调性保真度审计见 `docs/design/stage-tonality-verification.md`
- 50→33 去重过程见 `docs/design/tool-dedup-audit.md` + `docs/design/50-puzzle-comparison.md`
