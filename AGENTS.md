# AGENTS.md — liuhui-star-guardian

This file is the short entry point for AI collaborators.

- Read `CLAUDE.md` for repo rules and coding constraints.
- Read `docs/ai-workflow.md` for the shared execution flow.
- The authoritative product spec is `docs/design/game-design-spec.md`.
- For any substantial task, read and update `production/active.md`.
- Keep changes scoped; if the spec needs to change, update the spec first.
